# Hawkynt.PhotoManager.Core — API reference

[← Hawkynt.PhotoManager.Core](https://github.com/Hawkynt/PhotoManager/blob/main/PhotoManager.Core/README.md)

<!-- generated by Hawkynt/RepositoryTemplate/package-readme — edit the XML docs in source, not here -->

> Every public and protected type and member, read from the built assembly and merged
> with its XML documentation. Generated — edit the XML docs in source, not this file.

### Namespace `Hawkynt.PhotoManager.Core`

[`AppDataPaths`](#appdatapaths)

#### `AppDataPaths`

Cross-platform resolution for the app's data directory — a settings/cache bucket separate from the user's photo library. Windows: %APPDATA%\PhotoManager macOS: ~/Library/Application Support/PhotoManager Linux: ~/.config/PhotoManager (respects XDG_CONFIG_HOME)

| Member | Signature | Summary |
| --- | --- | --- |
| `ProductName` | `const string ProductName` |  |
| `ModelFile` | `static FileInfo ModelFile(string fileName)` |  |
| `Root` | `static DirectoryInfo Root()` | Returns the root directory for app data, creating it if needed. |
| `SubDirectory` | `static DirectoryInfo SubDirectory(string name)` | Resolves a subdirectory under the app data root (e.g. "models"), creating it. |

### Namespace `Hawkynt.PhotoManager.Core.Detection`

[`CompositeDetector`](#compositedetector) · [`DetectionKind`](#detectionkind) · [`DetectionLabel`](#detectionlabel) · [`DetectionResult`](#detectionresult) · [`DetectionService`](#detectionservice) · [`IDetector`](#idetector) · [`NormalizedBoundingBox`](#normalizedboundingbox) · [`PathDerivedDetector`](#pathderiveddetector) · [`YoloObjectDetector`](#yoloobjectdetector)

#### `CompositeDetector`

Runs a primary detector and, if it returns no labels, tries a fallback. Wired so the app prefers the real ONNX-backed object detector when its model is present, but quietly uses the path-derived heuristic otherwise — so the Auto-Detect button is useful out of the box without forcing the user to download model weights.

Implements `IDetector`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CompositeDetector` | `CompositeDetector(IDetector primary, IDetector fallback)` |  |
| `DetectAsync` | `Task<DetectionResult> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `DetectionKind`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Object` | `0` |  |
| `Face` | `1` |  |

#### `DetectionLabel`

One detected entity in an image — either an object class (car, dog) or a named person (from face clustering). Bounding box is optional and normalized to 0..1 coordinates relative to the image so it survives downstream resizes. Confidence is 0..1.

Implements `IEquatable<DetectionLabel>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectionLabel` | `DetectionLabel(string Name, float Confidence, DetectionKind Kind, NormalizedBoundingBox? Region = null)` | One detected entity in an image — either an object class (car, dog) or a named person (from face clustering). Bounding box is optional and normalized to 0..1 coordinates relative to the image so it survives downstream resizes. Confidence is 0..1. |
| `Confidence` | `float Confidence { get; init; }` |  |
| `Kind` | `DetectionKind Kind { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `Region` | `NormalizedBoundingBox? Region { get; init; }` |  |

#### `DetectionResult`

The set of detections returned from one run of an `IDetector` against a single image.

Implements `IEquatable<DetectionResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectionResult` | `DetectionResult(IReadOnlyList<DetectionLabel> Labels)` | The set of detections returned from one run of an `IDetector` against a single image. |
| `Empty` | `static readonly DetectionResult Empty` |  |
| `Labels` | `IReadOnlyList<DetectionLabel> Labels { get; init; }` |  |
| `DistinctLabelNames` | `IEnumerable<string> DistinctLabelNames()` |  |

#### `DetectionService`

Orchestrates detection + auto-keyword writing. Runs an `IDetector` against a file, reads existing keywords from the sidecar, merges the detected labels on top (case-insensitive dedupe, preserving user-added keywords), and writes the sidecar back through the Phase-1 `IMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectionService` | `DetectionService(IDetector detector, IMetadataReader reader, IMetadataWriter writer)` |  |
| `DetectAndWriteKeywordsAsync` | `Task<DetectionResult> DetectAndWriteKeywordsAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` | Runs detection and merges the resulting labels into the sidecar's keyword list. Returns the detection result so callers can show what was found. |
| `MergeKeywords` | `static IReadOnlyList<string> MergeKeywords(IEnumerable<string> existing, IEnumerable<string> detected)` | Merges detected labels into an existing keyword list, preserving order and case of existing entries. Case-insensitive dedupe so "beach" and "Beach" don't both end up in the sidecar. |

#### `IDetector`

Pluggable entry point for any detector — ONNX models, cloud services, or rule-based heuristics. The detection pipeline (auto-keyword writing, face region storage) is agnostic to which implementation is running.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectAsync` | `Task<DetectionResult> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `NormalizedBoundingBox`

All four values are fractions of the image's width/height so the region is resolution-independent. X/Y is the top-left corner.

Implements `IEquatable<NormalizedBoundingBox>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `NormalizedBoundingBox` | `NormalizedBoundingBox(float X, float Y, float Width, float Height)` | All four values are fractions of the image's width/height so the region is resolution-independent. X/Y is the top-left corner. |
| `Height` | `float Height { get; init; }` |  |
| `Width` | `float Width { get; init; }` |  |
| `X` | `float X { get; init; }` |  |
| `Y` | `float Y { get; init; }` |  |

#### `PathDerivedDetector`

A heuristic detector that extracts keyword-like tokens from the file's directory path. Useful on its own for folder-organized libraries ("Photos/2024/Vacation/Rome" → {vacation, rome}) and as a demoable default until the ONNX-based detector (YOLO, RetinaFace) is wired up. Tokens that look like dates, pure numbers, or common filler words are filtered out; everything else becomes a low-confidence Object label.

Implements `IDetector`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PathDerivedDetector` | `PathDerivedDetector()` |  |
| `DetectAsync` | `Task<DetectionResult> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `YoloObjectDetector`

ONNX-backed YOLO v8 object detector. Looks for `yolov8n.onnx` (or a caller-specified file) at `ModelFile`. If the model is missing, `DetectAsync` returns `Empty` so callers can gracefully fall back to a different detector rather than crashing with "file not found". The detector is thread-safe for concurrent `DetectAsync` calls on different files because `InferenceSession` supports parallel `Run` invocations.

Implements `IDetector`, `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `YoloObjectDetector` | `YoloObjectDetector(FileInfo modelFile = null, float scoreThreshold = 0.25, float iouThreshold = 0.45)` |  |
| `DefaultModelFileName` | `const string DefaultModelFileName` |  |
| `IsAvailable` | `bool IsAvailable { get; }` |  |
| `DetectAsync` | `Task<DetectionResult> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `Dispose` | `void Dispose()` |  |

### Namespace `Hawkynt.PhotoManager.Core.Develop`

[`AppliedEditsCounter`](#appliededitscounter) · [`AutoAdjustOptions`](#autoadjustoptions) · [`AutoDeveloper`](#autodeveloper) · [`BrushDab`](#brushdab) · [`CompareMode`](#comparemode) · [`CompareModeState`](#comparemodestate) · [`CropSuggestion`](#cropsuggestion) · [`CurveInterpolation`](#curveinterpolation) · [`CurvePoint`](#curvepoint) · [`DevelopHistory`](#develophistory) · [`DevelopMetadataStore.BakeThumbnailResult`](#developmetadatastorebakethumbnailresult) · [`DevelopSettings`](#developsettings) · [`DevelopSnapshot`](#developsnapshot) · [`DevelopTemplate`](#developtemplate) · [`DevelopTemplateStore`](#developtemplatestore) · [`FilmPreset`](#filmpreset) · [`HealingBrushStroke.Stamp`](#healingbrushstrokestamp) · [`HslBand`](#hslband) · [`ImageHistogram`](#imagehistogram) · [`LocalAdjustment`](#localadjustment) · [`LocalMask`](#localmask) · [`LocalMaskType`](#localmasktype) · [`LutLoader`](#lutloader) · [`MaskCombineOp`](#maskcombineop) · [`RestorationSettings`](#restorationsettings) · [`SaltAndPepperOptions`](#saltandpepperoptions) · [`ScratchDetectorOptions`](#scratchdetectoroptions) · [`StageProgress`](#stageprogress) · [`ThumbnailFitter.Fit`](#thumbnailfitterfit) · [`VirtualCopyDiscovery`](#virtualcopydiscovery) · [`VirtualCopyInfo`](#virtualcopyinfo) · [`VirtualCopyManager`](#virtualcopymanager)

#### `AppliedEditsCounter`

Counts how many develop knobs are away from their default values for a `DevelopSettings` instance. Drives the "ready (N edits)" pill in the develop window's bottom status bar so the user always sees at a glance how much non-identity work is on the image. Pure logic so it can be unit-tested without spinning up Avalonia. The per-slider counting is deliberate (so each separate adjustment shows as its own +1) rather than collapsed to "any edit at all".

| Member | Signature | Summary |
| --- | --- | --- |
| `Count` | `static int Count(DevelopSettings settings)` | Number of distinct adjustments active on `settings`. Local-adjustment list contributes its `Count`; everything else contributes 0 or 1 per knob. The slider-tolerance is 1e-6. |

#### `AutoAdjustOptions`

Tunable parameters for the auto-correction algorithms. Every constant that was previously hardcoded inside `AutoWhiteBalance`, `AutoTone`, and `AutoChannelStretch` is exposed here so the user (or a per-camera preset) can bias the algorithms away from outliers / noise or toward more aggressive correction. All defaults match the previous hardcoded behavior so existing callers see identical results when passing `new AutoAdjustOptions()`.

Implements `IEquatable<AutoAdjustOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `AutoAdjustOptions` | `AutoAdjustOptions(double WbClipLowPct = 0.02, double WbClipHighPct = 0.02, int WbHighlightLumMin = 200, int WbHighlightLumMax = 250, int WbMinHighlightPixels = 10, double WbHighlightBlendWeight = 0.5, double WbTemperatureSensitivity = 50, double WbTintSensitivity = 50, double ToneBlackClipPct = 0.005, double ToneWhiteClipPct = 0.005, int ToneShadowRecoveryThreshold = 40, int ToneHighlightRecoveryThreshold = 215, double ToneRecoveryStrength = 1.5, double StretchTopPercentile = 0.995)` | Tunable parameters for the auto-correction algorithms. Every constant that was previously hardcoded inside `AutoWhiteBalance`, `AutoTone`, and `AutoChannelStretch` is exposed here so the user (or a per-camera preset) can bias the algorithms away from outliers / noise or toward more aggressive correction. All defaults match the previous hardcoded behavior so existing callers see identical results when passing `new AutoAdjustOptions()`. |
| `StretchTopPercentile` | `double StretchTopPercentile { get; init; }` |  |
| `ToneBlackClipPct` | `double ToneBlackClipPct { get; init; }` |  |
| `ToneHighlightRecoveryThreshold` | `int ToneHighlightRecoveryThreshold { get; init; }` |  |
| `ToneRecoveryStrength` | `double ToneRecoveryStrength { get; init; }` |  |
| `ToneShadowRecoveryThreshold` | `int ToneShadowRecoveryThreshold { get; init; }` |  |
| `ToneWhiteClipPct` | `double ToneWhiteClipPct { get; init; }` |  |
| `WbClipHighPct` | `double WbClipHighPct { get; init; }` |  |
| `WbClipLowPct` | `double WbClipLowPct { get; init; }` |  |
| `WbHighlightBlendWeight` | `double WbHighlightBlendWeight { get; init; }` |  |
| `WbHighlightLumMax` | `int WbHighlightLumMax { get; init; }` |  |
| `WbHighlightLumMin` | `int WbHighlightLumMin { get; init; }` |  |
| `WbMinHighlightPixels` | `int WbMinHighlightPixels { get; init; }` |  |
| `WbTemperatureSensitivity` | `double WbTemperatureSensitivity { get; init; }` |  |
| `WbTintSensitivity` | `double WbTintSensitivity { get; init; }` |  |

#### `AutoDeveloper`

One-click derivations of `DevelopSettings` from image statistics. Each method takes the current settings and returns an updated copy — never overwrites unrelated fields, so the user can stack Auto-Tone + Auto-WB without one wiping the other.

| Member | Signature | Summary |
| --- | --- | --- |
| `AutoChannelStretch` | `static DevelopSettings AutoChannelStretch(DevelopSettings current, ImageHistogram histogram, AutoAdjustOptions options = null)` | Per-channel auto stretch. For each of R / G / B, find the value at which 99.5% of the pixels are darker, and derive a gain that pushes that point up to 255. Result: each channel uses its full dynamic range, which corrects subtle colour casts AND lifts overall contrast in one move. |
| `AutoTone` | `static DevelopSettings AutoTone(DevelopSettings current, ImageHistogram histogram, AutoAdjustOptions options = null)` | "Make it look better" tone preset. Maps the histogram's 0.5 / 99.5 percentiles onto Whites/Blacks (clip the rare extremes), then lifts shadows / pulls highlights based on how compressed each end is. |
| `AutoWhiteBalance` | `static DevelopSettings AutoWhiteBalance(DevelopSettings current, ImageHistogram histogram)` | Grey-world auto white balance. Pushes the average channel ratios toward 1:1:1 by deriving Temperature (red↔blue) and Tint (green vs the others) shifts. Cheap, no scene-classification. |
| `PickBlackPoint` | `static DevelopSettings PickBlackPoint(DevelopSettings current, byte r, byte g, byte b)` | User clicked a pixel they want to be pure black. Compute the exposure / blacks shift that would map that pixel to (0,0,0). Conservative — only adjusts Blacks, leaves white balance alone. |
| `PickBluePoint` | `static DevelopSettings PickBluePoint(DevelopSettings current, byte r, byte g, byte b)` |  |
| `PickGreenPoint` | `static DevelopSettings PickGreenPoint(DevelopSettings current, byte r, byte g, byte b)` |  |
| `PickGreyPoint` | `static DevelopSettings PickGreyPoint(DevelopSettings current, byte r, byte g, byte b)` | User clicked a pixel they want to be neutral grey. Derive Temperature + Tint shifts so the channels equalise on that pixel — the standard grey-card eyedropper move from any real DAM. |
| `PickRedPoint` | `static DevelopSettings PickRedPoint(DevelopSettings current, byte r, byte g, byte b)` | Click a pixel that should saturate the red channel. |
| `PickWhitePoint` | `static DevelopSettings PickWhitePoint(DevelopSettings current, byte r, byte g, byte b)` | User clicked a pixel they want to be pure white. Lift Whites so that pixel saturates the channel scale. |

#### `BrushDab`

One brush stroke dab in normalised image coords.

Implements `IEquatable<BrushDab>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `BrushDab` | `BrushDab(double X, double Y, double Radius, double Flow)` | One brush stroke dab in normalised image coords. |
| `Flow` | `double Flow { get; init; }` |  |
| `Radius` | `double Radius { get; init; }` |  |
| `X` | `double X { get; init; }` |  |
| `Y` | `double Y { get; init; }` |  |

#### `CompareMode`

Which compare layout the editor preview is currently showing. The cycle order — After → Split → Overlay → Slider → After — matches the order the modes appear in the editor's toolbar tooltip.

| Value | Numeric | Summary |
| --- | --- | --- |
| `After` | `0` | Single image: just the developed preview. |
| `Split` | `1` | Side-by-side: baseline on the left, developed on the right. |
| `Overlay` | `2` | Overlay: developed painted over baseline with an alpha slider controlling the developed image's opacity (0 = source, 1 = preview). |
| `Slider` | `3` | Movable wipe: vertical slider clips the developed image so the baseline shows on one side and the developed on the other. |

#### `CompareModeState`

Pure-logic state machine that backs the compare-mode toggle in `EditImageWindow`. UI-free so it can be unit-tested without spinning up Avalonia.

| Member | Signature | Summary |
| --- | --- | --- |
| `CompareModeState` | `CompareModeState(CompareMode initial = 0)` |  |
| `ButtonContent` | `string ButtonContent { get; }` | Toolbar-button label that reflects the active mode. |
| `ButtonTooltip` | `string ButtonTooltip { get; }` | Tooltip text describing what the next click will do. |
| `IsAfterVisible` | `bool IsAfterVisible { get; }` |  |
| `IsOverlayVisible` | `bool IsOverlayVisible { get; }` |  |
| `IsSliderVisible` | `bool IsSliderVisible { get; }` |  |
| `IsSplitVisible` | `bool IsSplitVisible { get; }` |  |
| `Mode` | `CompareMode Mode { get; }` |  |
| `NeedsBaseline` | `bool NeedsBaseline { get; }` | True when the baseline render is needed (i.e. any compare layout is active). |
| `Cycle` | `CompareMode Cycle()` | Advance to the next mode in the cycle After → Split → Overlay → Slider → After. |
| `Set` | `void Set(CompareMode mode)` | Force the state to a specific mode. Idempotent when already in that mode. |

#### `CropSuggestion`

One auto-crop result. Coordinates are normalised 0..1 so callers can drop them straight into `CropLeft` etc.

Implements `IEquatable<CropSuggestion>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CropSuggestion` | `CropSuggestion(double Left, double Top, double Right, double Bottom, double AspectRatio, double Score)` | One auto-crop result. Coordinates are normalised 0..1 so callers can drop them straight into `CropLeft` etc. |
| `AspectRatio` | `double AspectRatio { get; init; }` |  |
| `Bottom` | `double Bottom { get; init; }` |  |
| `Left` | `double Left { get; init; }` |  |
| `Right` | `double Right { get; init; }` |  |
| `Score` | `double Score { get; init; }` |  |
| `Top` | `double Top { get; init; }` |  |

#### `CurveInterpolation`

How the tone curve is interpolated between user-placed points.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Linear` | `0` | Piecewise-linear segments. Cheap, predictable, can produce visible kinks. |
| `CatmullRom` | `1` | Catmull-Rom spline through every control point — smooth, no overshoot at the anchors. |
| `Bezier` | `2` | Cubic Bezier with auto-derived tangents (Catmull-Rom-style). Slightly looser than CatmullRom; can overshoot in steep regions. |

#### `CurvePoint`

A point on the tone curve. Both axes are 0..1.

Implements `IEquatable<CurvePoint>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CurvePoint` | `CurvePoint(double X, double Y)` | A point on the tone curve. Both axes are 0..1. |
| `X` | `double X { get; init; }` |  |
| `Y` | `double Y { get; init; }` |  |

#### `DevelopHistory`

Pure helpers for the develop history stack — kept free of file I/O so the model layer stays trivially testable. The stack is stored newest-first; callers persist the list as XMP via `DevelopMetadataStore`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultMaxDepth` | `const int DefaultMaxDepth` |  |
| `GetAll` | `static IReadOnlyList<DevelopSnapshot> GetAll(IReadOnlyList<DevelopSnapshot> history)` | Return every snapshot in the stack, newest first. Returns an empty list when the history is empty. The returned list is a fresh copy. |
| `Push` | `static IReadOnlyList<DevelopSnapshot> Push(IReadOnlyList<DevelopSnapshot> existing, DevelopSettings settings, string label = null, int maxDepth = 50)` | Push `settings` onto the front of the stack with a UTC timestamp + optional label, capping the result to `maxDepth` (oldest entries fall off the end). The input list is not mutated. |
| `RollbackTo` | `static DevelopSettings RollbackTo(IReadOnlyList<DevelopSnapshot> history, int index)` | Roll back to the snapshot at `index` (0 = most recent). Returns the `DevelopSettings` stored in that entry. Throws `ArgumentOutOfRangeException` if the index is out of bounds. |

#### `DevelopMetadataStore.BakeThumbnailResult`

Outcome of a thumbnail-bake attempt.

Implements `IEquatable<BakeThumbnailResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `BakeThumbnailResult` | `BakeThumbnailResult(bool Success, int Width = 0, int Height = 0, int Quality = 0, int ThumbnailByteCount = 0, int ExifPayloadByteCount = 0, double PsnrDb = 0, int? BytesOverBudget = null, bool DidAutoFit = false, string Error = null)` | Outcome of a thumbnail-bake attempt. |
| `BytesOverBudget` | `int? BytesOverBudget { get; init; }` |  |
| `DidAutoFit` | `bool DidAutoFit { get; init; }` |  |
| `Error` | `string Error { get; init; }` |  |
| `ExifPayloadByteCount` | `int ExifPayloadByteCount { get; init; }` |  |
| `Height` | `int Height { get; init; }` |  |
| `PsnrDb` | `double PsnrDb { get; init; }` |  |
| `Quality` | `int Quality { get; init; }` |  |
| `Success` | `bool Success { get; init; }` |  |
| `ThumbnailByteCount` | `int ThumbnailByteCount { get; init; }` |  |
| `Width` | `int Width { get; init; }` |  |

#### `DevelopSettings`

Non-destructive develop parameters applied by `ImageDeveloper`. Modeled on Lightroom's Basic + Detail panels but kept lightweight so the CPU-bound preview stays interactive on big RAWs. All adjustments default to a no-op so callers and templates can fill in only what they want to override.

Implements `IEquatable<DevelopSettings>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DevelopSettings` | `DevelopSettings(int RotationDegrees = 0, double ExposureStops = 0, double ContrastPercent = 0, double HighlightsPercent = 0, double ShadowsPercent = 0, double WhitesPercent = 0, double BlacksPercent = 0, double SaturationPercent = 0, double VibrancePercent = 0, double ClarityPercent = 0, double TexturePercent = 0, double SmoothnessPercent = 0, double SharpeningAmount = 0, double TemperatureShift = 0, double TintShift = 0, double RedGain = 0, double GreenGain = 0, double BlueGain = 0, IReadOnlyList<CurvePoint> ToneCurvePoints = null, CurveInterpolation ToneCurveInterpolation = 0, IReadOnlyList<CurvePoint> RedCurvePoints = null, IReadOnlyList<CurvePoint> GreenCurvePoints = null, IReadOnlyList<CurvePoint> BlueCurvePoints = null, double DehazePercent = 0, double SharpenRadius = 0, double SharpenDetail = 0, double SharpenMasking = 0, IReadOnlyList<double> HslHueShifts = null, IReadOnlyList<double> HslSaturationShifts = null, IReadOnlyList<double> HslLuminanceShifts = null, double CropAngleDegrees = 0, double CropLeft = 0, double CropTop = 0, double CropRight = 1, double CropBottom = 1, double GradeShadowHue = 0, double GradeShadowSat = 0, double GradeShadowLum = 0, double GradeMidtoneHue = 0, double GradeMidtoneSat = 0, double GradeMidtoneLum = 0, double GradeHighlightHue = 0, double GradeHighlightSat = 0, double GradeHighlightLum = 0, double GradeGlobalHue = 0, double GradeGlobalSat = 0, double GradeGlobalLum = 0, double VignetteAmount = 0, double VignetteMidpoint = 50, double VignetteFeather = 50, double VignetteRoundness = 0, double VignetteHighlightContrast = 0, double GrainAmount = 0, double GrainSize = 25, double GrainFrequency = 50, double LuminanceNrDetail = 0, double LuminanceNrContrast = 0, double ColorNoiseReduction = 0, double ColorNrDetail = 0, double ColorNrSmoothness = 0, bool ConvertToGrayscale = false, double GrayMixerRed = 0, double GrayMixerOrange = 0, double GrayMixerYellow = 0, double GrayMixerGreen = 0, double GrayMixerAqua = 0, double GrayMixerBlue = 0, double GrayMixerPurple = 0, double GrayMixerMagenta = 0, double SplitToningShadowHue = 0, double SplitToningShadowSaturation = 0, double SplitToningHighlightHue = 0, double SplitToningHighlightSaturation = 0, double SplitToningBalance = 0, double ParametricShadows = 0, double ParametricDarks = 0, double ParametricLights = 0, double ParametricHighlights = 0, double ParametricShadowSplit = 25, double ParametricMidtoneSplit = 50, double ParametricHighlightSplit = 75, double LensManualDistortion = 0, double ChromaticAberrationR = 0, double ChromaticAberrationB = 0, double DefringePurpleAmount = 0, double DefringeGreenAmount = 0, double CalibrationRedHue = 0, double CalibrationRedSaturation = 0, double CalibrationGreenHue = 0, double CalibrationGreenSaturation = 0, double CalibrationBlueHue = 0, double CalibrationBlueSaturation = 0, double PerspectiveVertical = 0, double PerspectiveHorizontal = 0, double PerspectiveRotate = 0, double PerspectiveScale = 100, double PerspectiveAspect = 0, double PerspectiveX = 0, double PerspectiveY = 0, IReadOnlyList<LocalAdjustment> LocalAdjustments = null, double ColorEnhancement = 0, string LookName = null, double LookOpacity = 1, string WatermarkText = null, double WatermarkOpacity = 0.5, string WatermarkPosition = "BottomRight", int WatermarkFontSize = 24, double AiDenoiseStrength = 0, string AiDenoiseModel = null, int AiUpscaleFactor = 1, string AiUpscaleModel = null, double AiColorizeAmount = 0, string AiColorizeModel = null)` | Non-destructive develop parameters applied by `ImageDeveloper`. Modeled on Lightroom's Basic + Detail panels but kept lightweight so the CPU-bound preview stays interactive on big RAWs. All adjustments default to a no-op so callers and templates can fill in only what they want to override. |
| `AiColorizeAmount` | `double AiColorizeAmount { get; init; }` |  |
| `AiColorizeModel` | `string AiColorizeModel { get; init; }` |  |
| `AiDenoiseModel` | `string AiDenoiseModel { get; init; }` |  |
| `AiDenoiseStrength` | `double AiDenoiseStrength { get; init; }` |  |
| `AiUpscaleFactor` | `int AiUpscaleFactor { get; init; }` |  |
| `AiUpscaleModel` | `string AiUpscaleModel { get; init; }` |  |
| `BlacksPercent` | `double BlacksPercent { get; init; }` | -100..+100. Anchors / lifts the black point. |
| `BlueCurvePoints` | `IReadOnlyList<CurvePoint> BlueCurvePoints { get; init; }` | Same, blue channel. |
| `BlueGain` | `double BlueGain { get; init; }` | Same for blue. |
| `CalibrationBlueHue` | `double CalibrationBlueHue { get; init; }` |  |
| `CalibrationBlueSaturation` | `double CalibrationBlueSaturation { get; init; }` |  |
| `CalibrationGreenHue` | `double CalibrationGreenHue { get; init; }` |  |
| `CalibrationGreenSaturation` | `double CalibrationGreenSaturation { get; init; }` |  |
| `CalibrationRedHue` | `double CalibrationRedHue { get; init; }` |  |
| `CalibrationRedSaturation` | `double CalibrationRedSaturation { get; init; }` |  |
| `ChromaticAberrationB` | `double ChromaticAberrationB { get; init; }` |  |
| `ChromaticAberrationR` | `double ChromaticAberrationR { get; init; }` |  |
| `ClarityPercent` | `double ClarityPercent { get; init; }` | -100..+100. Local-contrast boost in the midtones (unsharp mask). |
| `ColorEnhancement` | `double ColorEnhancement { get; init; }` |  |
| `ColorNoiseReduction` | `double ColorNoiseReduction { get; init; }` |  |
| `ColorNrDetail` | `double ColorNrDetail { get; init; }` |  |
| `ColorNrSmoothness` | `double ColorNrSmoothness { get; init; }` |  |
| `ContrastPercent` | `double ContrastPercent { get; init; }` | -100..+100 about midgray. +50 boosts S-curve. |
| `ConvertToGrayscale` | `bool ConvertToGrayscale { get; init; }` |  |
| `CropAngleDegrees` | `double CropAngleDegrees { get; init; }` | Free rotation in degrees, applied AFTER `RotationDegrees`. Range ±45 in the UI; ImageSharp does the trig. |
| `CropBottom` | `double CropBottom { get; init; }` | Bottom crop edge (0..1). |
| `CropLeft` | `double CropLeft { get; init; }` | Left crop edge in normalised image coords (0..1). 0 = keep all of left side. |
| `CropRight` | `double CropRight { get; init; }` | Right crop edge (0..1). 1 = keep all of right side. |
| `CropTop` | `double CropTop { get; init; }` | Top crop edge (0..1). |
| `DefringeGreenAmount` | `double DefringeGreenAmount { get; init; }` |  |
| `DefringePurpleAmount` | `double DefringePurpleAmount { get; init; }` |  |
| `DehazePercent` | `double DehazePercent { get; init; }` | -100..+100. Positive removes atmospheric haze (boosts midtone contrast + saturation); negative softens / adds haze. |
| `ExposureStops` | `double ExposureStops { get; init; }` | EV shift; ±1 doubles/halves brightness. Range roughly ±5. |
| `GradeGlobalHue` | `double GradeGlobalHue { get; init; }` |  |
| `GradeGlobalLum` | `double GradeGlobalLum { get; init; }` |  |
| `GradeGlobalSat` | `double GradeGlobalSat { get; init; }` |  |
| `GradeHighlightHue` | `double GradeHighlightHue { get; init; }` |  |
| `GradeHighlightLum` | `double GradeHighlightLum { get; init; }` |  |
| `GradeHighlightSat` | `double GradeHighlightSat { get; init; }` |  |
| `GradeMidtoneHue` | `double GradeMidtoneHue { get; init; }` |  |
| `GradeMidtoneLum` | `double GradeMidtoneLum { get; init; }` |  |
| `GradeMidtoneSat` | `double GradeMidtoneSat { get; init; }` |  |
| `GradeShadowHue` | `double GradeShadowHue { get; init; }` |  |
| `GradeShadowLum` | `double GradeShadowLum { get; init; }` |  |
| `GradeShadowSat` | `double GradeShadowSat { get; init; }` |  |
| `GrainAmount` | `double GrainAmount { get; init; }` |  |
| `GrainFrequency` | `double GrainFrequency { get; init; }` |  |
| `GrainSize` | `double GrainSize { get; init; }` |  |
| `GrayMixerAqua` | `double GrayMixerAqua { get; init; }` |  |
| `GrayMixerBlue` | `double GrayMixerBlue { get; init; }` |  |
| `GrayMixerGreen` | `double GrayMixerGreen { get; init; }` |  |
| `GrayMixerMagenta` | `double GrayMixerMagenta { get; init; }` |  |
| `GrayMixerOrange` | `double GrayMixerOrange { get; init; }` |  |
| `GrayMixerPurple` | `double GrayMixerPurple { get; init; }` |  |
| `GrayMixerRed` | `double GrayMixerRed { get; init; }` |  |
| `GrayMixerYellow` | `double GrayMixerYellow { get; init; }` |  |
| `GreenCurvePoints` | `IReadOnlyList<CurvePoint> GreenCurvePoints { get; init; }` | Same, green channel. |
| `GreenGain` | `double GreenGain { get; init; }` | Same for green. |
| `HighlightsPercent` | `double HighlightsPercent { get; init; }` | -100..+100. Negative recovers blown highlights; positive lifts them further. |
| `HslHueShifts` | `IReadOnlyList<double> HslHueShifts { get; init; }` | Eight per-band hue shifts in degrees, one per `HslBand`. Null / empty = identity. Range -100..+100; ±100 nudges the band hue by ~30°. |
| `HslLuminanceShifts` | `IReadOnlyList<double> HslLuminanceShifts { get; init; }` | Eight per-band luminance shifts -100..+100; brightens or darkens pixels whose hue lands in the band without changing colour. |
| `HslSaturationShifts` | `IReadOnlyList<double> HslSaturationShifts { get; init; }` | Eight per-band saturation shifts -100..+100; -100 desaturates the band, +100 doubles its saturation. |
| `IsCurveIdentity` | `bool IsCurveIdentity { get; }` | True when the tone curve is the identity (or unset). |
| `IsIdentity` | `bool IsIdentity { get; }` |  |
| `LensManualDistortion` | `double LensManualDistortion { get; init; }` |  |
| `LocalAdjustments` | `IReadOnlyList<LocalAdjustment> LocalAdjustments { get; init; }` |  |
| `LookName` | `string LookName { get; init; }` |  |
| `LookOpacity` | `double LookOpacity { get; init; }` |  |
| `LuminanceNrContrast` | `double LuminanceNrContrast { get; init; }` |  |
| `LuminanceNrDetail` | `double LuminanceNrDetail { get; init; }` |  |
| `ParametricDarks` | `double ParametricDarks { get; init; }` |  |
| `ParametricHighlightSplit` | `double ParametricHighlightSplit { get; init; }` |  |
| `ParametricHighlights` | `double ParametricHighlights { get; init; }` |  |
| `ParametricLights` | `double ParametricLights { get; init; }` |  |
| `ParametricMidtoneSplit` | `double ParametricMidtoneSplit { get; init; }` |  |
| `ParametricShadowSplit` | `double ParametricShadowSplit { get; init; }` |  |
| `ParametricShadows` | `double ParametricShadows { get; init; }` |  |
| `PerspectiveAspect` | `double PerspectiveAspect { get; init; }` |  |
| `PerspectiveHorizontal` | `double PerspectiveHorizontal { get; init; }` |  |
| `PerspectiveRotate` | `double PerspectiveRotate { get; init; }` |  |
| `PerspectiveScale` | `double PerspectiveScale { get; init; }` |  |
| `PerspectiveVertical` | `double PerspectiveVertical { get; init; }` |  |
| `PerspectiveX` | `double PerspectiveX { get; init; }` |  |
| `PerspectiveY` | `double PerspectiveY { get; init; }` |  |
| `RedCurvePoints` | `IReadOnlyList<CurvePoint> RedCurvePoints { get; init; }` | Per-channel tone curve for the red channel; null = identity. Applied AFTER the master curve so users can tune individual colors without disturbing luminance. |
| `RedGain` | `double RedGain { get; init; }` | -100..+100. Per-channel multiplier on the red channel (after exposure / WB). |
| `RotationDegrees` | `int RotationDegrees { get; init; }` | Multiples of 90° (0, 90, 180, 270). |
| `SaturationPercent` | `double SaturationPercent { get; init; }` | -100 (grayscale) to +100 (double saturation), uniform. |
| `ShadowsPercent` | `double ShadowsPercent { get; init; }` | -100..+100. Positive lifts dark areas; negative crushes them. |
| `SharpenDetail` | `double SharpenDetail { get; init; }` | 0..100. Strength of an extra fine-radius sharpening pass for high-frequency detail. |
| `SharpenMasking` | `double SharpenMasking { get; init; }` | 0..100. How aggressively edge masking suppresses sharpening on flat areas. Currently emitted to crs: but not yet applied to pixels. |
| `SharpenRadius` | `double SharpenRadius { get; init; }` | 0..3 pixels. 0 = derive from `SharpeningAmount` for backwards compat; otherwise the explicit Gaussian sigma. |
| `SharpeningAmount` | `double SharpeningAmount { get; init; }` | 0..150. Edge enhancement applied last. |
| `SmoothnessPercent` | `double SmoothnessPercent { get; init; }` | 0..100. Gaussian-blur strength applied before sharpening; doubles as a basic noise reducer. |
| `SplitToningBalance` | `double SplitToningBalance { get; init; }` |  |
| `SplitToningHighlightHue` | `double SplitToningHighlightHue { get; init; }` |  |
| `SplitToningHighlightSaturation` | `double SplitToningHighlightSaturation { get; init; }` |  |
| `SplitToningShadowHue` | `double SplitToningShadowHue { get; init; }` |  |
| `SplitToningShadowSaturation` | `double SplitToningShadowSaturation { get; init; }` |  |
| `TemperatureShift` | `double TemperatureShift { get; init; }` | -100..+100. Negative pushes blue (cooler), positive red (warmer). |
| `TexturePercent` | `double TexturePercent { get; init; }` | -100..+100. High-frequency detail boost; smaller spatial scale than Clarity. |
| `TintShift` | `double TintShift { get; init; }` | -100..+100. Negative pushes green, positive magenta. |
| `ToneCurveInterpolation` | `CurveInterpolation ToneCurveInterpolation { get; init; }` | Interpolation between tone-curve points: piecewise `Linear`, smooth `CatmullRom` through every point, or `Bezier` with auto-derived tangents. |
| `ToneCurvePoints` | `IReadOnlyList<CurvePoint> ToneCurvePoints { get; init; }` | User-placed control points for the master tone curve, normalized 0..1. Empty / one-point / null = identity. |
| `VibrancePercent` | `double VibrancePercent { get; init; }` | -100..+100. Boosts low-saturation colors more than already-saturated ones. |
| `VignetteAmount` | `double VignetteAmount { get; init; }` |  |
| `VignetteFeather` | `double VignetteFeather { get; init; }` |  |
| `VignetteHighlightContrast` | `double VignetteHighlightContrast { get; init; }` |  |
| `VignetteMidpoint` | `double VignetteMidpoint { get; init; }` |  |
| `VignetteRoundness` | `double VignetteRoundness { get; init; }` |  |
| `WatermarkFontSize` | `int WatermarkFontSize { get; init; }` |  |
| `WatermarkOpacity` | `double WatermarkOpacity { get; init; }` |  |
| `WatermarkPosition` | `string WatermarkPosition { get; init; }` |  |
| `WatermarkText` | `string WatermarkText { get; init; }` |  |
| `WhitesPercent` | `double WhitesPercent { get; init; }` | -100..+100. Anchors / lifts the white point. |

#### `DevelopSnapshot`

One entry on the develop edit-history stack. Each Save snapshots the in-flight `DevelopSettings` here before overwriting the current settings, so users can roll back to any prior state.

Implements `IEquatable<DevelopSnapshot>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DevelopSnapshot` | `DevelopSnapshot(DateTime TimestampUtc, string Label, DevelopSettings Settings)` | One entry on the develop edit-history stack. Each Save snapshots the in-flight `DevelopSettings` here before overwriting the current settings, so users can roll back to any prior state. |
| `Label` | `string Label { get; init; }` |  |
| `Settings` | `DevelopSettings Settings { get; init; }` |  |
| `TimestampUtc` | `DateTime TimestampUtc { get; init; }` |  |

#### `DevelopTemplate`

One reusable develop preset. Just a name + the settings — no preview thumbnail, no auto-update logic. Saved to `%AppData%/PhotoManager/develop-templates/` as one JSON file per template so users can hand-edit, share, or version-control them.

Implements `IEquatable<DevelopTemplate>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DevelopTemplate` | `DevelopTemplate(string Name, DevelopSettings Settings)` | One reusable develop preset. Just a name + the settings — no preview thumbnail, no auto-update logic. Saved to `%AppData%/PhotoManager/develop-templates/` as one JSON file per template so users can hand-edit, share, or version-control them. |
| `Name` | `string Name { get; init; }` |  |
| `Settings` | `DevelopSettings Settings { get; init; }` |  |

#### `DevelopTemplateStore`

Reads / writes `DevelopTemplate`s as JSON files in the app data folder. No database, no synchronization — re-listing reads the directory each call. Good enough at the human scale of "tens of templates" the user will realistically maintain.

| Member | Signature | Summary |
| --- | --- | --- |
| `DevelopTemplateStore` | `DevelopTemplateStore()` |  |
| `DevelopTemplateStore` | `DevelopTemplateStore(DirectoryInfo root)` |  |
| `Delete` | `void Delete(string name)` |  |
| `List` | `IReadOnlyList<DevelopTemplate> List()` | Every template currently on disk, sorted by name (case-insensitive). |
| `Save` | `void Save(DevelopTemplate template)` | Writes the template, overwriting any existing entry with the same name. |

#### `FilmPreset`

A named film-simulation preset that maps to a pre-configured `DevelopSettings`. Selecting a preset overlays its characteristic look (contrast, saturation, colour grading, tone curve) on top of the user's current exposure / white-balance settings via `with` expressions on the record.

Implements `IEquatable<FilmPreset>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FilmPreset` | `FilmPreset(string Name, string Description, DevelopSettings Adjustments)` | A named film-simulation preset that maps to a pre-configured `DevelopSettings`. Selecting a preset overlays its characteristic look (contrast, saturation, colour grading, tone curve) on top of the user's current exposure / white-balance settings via `with` expressions on the record. |
| `Adjustments` | `DevelopSettings Adjustments { get; init; }` |  |
| `All` | `static IReadOnlyList<FilmPreset> All { get; }` | Registry of all built-in film presets. |
| `Description` | `string Description { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `MergeOnto` | `static DevelopSettings MergeOnto(DevelopSettings userBase, DevelopSettings preset)` | Merge a film preset's adjustments onto a user's base settings. User exposure, white balance, crop, rotation, AI, LUT, and local adjustments are preserved; the preset's tone / colour / effects overlay on top. |

#### `HealingBrushStroke.Stamp`

Single stamp within a stroke.

Implements `IEquatable<Stamp>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Stamp` | `Stamp(int DstX, int DstY, int Radius)` | Single stamp within a stroke. |
| `DstX` | `int DstX { get; init; }` |  |
| `DstY` | `int DstY { get; init; }` |  |
| `Radius` | `int Radius { get; init; }` |  |

#### `HslBand`

The eight HSL adjustment bands Adobe uses (Red / Orange / Yellow / Green / Aqua / Blue / Purple / Magenta). Centre hues are evenly distributed around the wheel; pixels get weighted contributions from the two nearest bands so adjacent-band tweaks blend smoothly.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Red` | `0` |  |
| `Orange` | `1` |  |
| `Yellow` | `2` |  |
| `Green` | `3` |  |
| `Aqua` | `4` |  |
| `Blue` | `5` |  |
| `Purple` | `6` |  |
| `Magenta` | `7` |  |

#### `ImageHistogram`

Per-channel histogram counts and percentile lookups. The 4 channels are sized 256 each — 8-bit precision is plenty for the editor's preview- driven UI; full-bit-depth analysis isn't worth the memory footprint when the source is already an 8-bit Rgba32.

Implements `IEquatable<ImageHistogram>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImageHistogram` | `ImageHistogram(int[] Red, int[] Green, int[] Blue, int[] Luminance, int TotalPixels)` | Per-channel histogram counts and percentile lookups. The 4 channels are sized 256 each — 8-bit precision is plenty for the editor's preview- driven UI; full-bit-depth analysis isn't worth the memory footprint when the source is already an 8-bit Rgba32. |
| `Blue` | `int[] Blue { get; init; }` |  |
| `Green` | `int[] Green { get; init; }` |  |
| `Luminance` | `int[] Luminance { get; init; }` |  |
| `Red` | `int[] Red { get; init; }` |  |
| `TotalPixels` | `int TotalPixels { get; init; }` |  |
| `Percentile` | `static int Percentile(int[] channel, int totalPixels, double fraction)` | Find the channel value at which `fraction` of the pixels are at or below it. Used to detect blown / crushed regions for Auto-Tone (clip 0.5%, recover 5%, etc.). |

#### `LocalAdjustment`

One local adjustment: a mask (Linear / Radial) plus the per-mask develop sliders. The slider set mirrors Adobe's "local"-prefixed crs: tags (Local Exposure 2012, Local Contrast 2012, etc.) so values emitted by PhotoManager show up in Lightroom and vice versa.

Implements `IEquatable<LocalAdjustment>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LocalAdjustment` | `LocalAdjustment(LocalMask Mask, string Name = "", double Amount = 1, double Exposure = 0, double Contrast = 0, double Highlights = 0, double Shadows = 0, double Saturation = 0, double Temperature = 0, double Tint = 0, double Clarity = 0, double Luminance = 0, double ToningHue = 0, double ToningSaturation = 0, double Defringe = 0, IReadOnlyList<LocalMask> SubMasks = null)` | One local adjustment: a mask (Linear / Radial) plus the per-mask develop sliders. The slider set mirrors Adobe's "local"-prefixed crs: tags (Local Exposure 2012, Local Contrast 2012, etc.) so values emitted by PhotoManager show up in Lightroom and vice versa. |
| `Amount` | `double Amount { get; init; }` |  |
| `Clarity` | `double Clarity { get; init; }` |  |
| `Contrast` | `double Contrast { get; init; }` |  |
| `Defringe` | `double Defringe { get; init; }` |  |
| `Exposure` | `double Exposure { get; init; }` |  |
| `Highlights` | `double Highlights { get; init; }` |  |
| `IsZero` | `bool IsZero { get; }` |  |
| `Luminance` | `double Luminance { get; init; }` |  |
| `Mask` | `LocalMask Mask { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `Saturation` | `double Saturation { get; init; }` |  |
| `Shadows` | `double Shadows { get; init; }` |  |
| `SubMasks` | `IReadOnlyList<LocalMask> SubMasks { get; init; }` |  |
| `Temperature` | `double Temperature { get; init; }` |  |
| `Tint` | `double Tint { get; init; }` |  |
| `ToningHue` | `double ToningHue { get; init; }` |  |
| `ToningSaturation` | `double ToningSaturation { get; init; }` |  |

#### `LocalMask`

Mask geometry for a local adjustment. A flat record (rather than a type hierarchy) so System.Text.Json round-trips cleanly through pm:developSettings without polymorphism attributes. Coordinates are normalised image-space (0..1), so masks survive resizing the preview vs the full image. Linear gradient: a line from (X0,Y0) to (X1,Y1). Mask weight is 0 at the "Zero" point projecting onto the line and 1 at the "Full" point — pixels off either end clamp to the nearest end's weight. Radial gradient: an ellipse centred at (CenterX, CenterY) with half-axes (RadiusX, RadiusY), optionally rotated by Angle degrees. Feather (0..100) controls the falloff curve; Invert flips inside vs outside.

Implements `IEquatable<LocalMask>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LocalMask` | `LocalMask(LocalMaskType Type = 0, double X0 = 0.5, double Y0 = 0, double X1 = 0.5, double Y1 = 1, double CenterX = 0.5, double CenterY = 0.5, double RadiusX = 0.25, double RadiusY = 0.25, double Feather = 50, double Angle = 0, bool Invert = false, IReadOnlyList<BrushDab> BrushDabs = null, double LuminanceRangeMin = 0, double LuminanceRangeMax = 1, double LuminanceRangeFeather = 0.1, double HueRangeMin = 0, double HueRangeMax = 1, double HueRangeFeather = 0.05, MaskCombineOp Combine = 0)` | Mask geometry for a local adjustment. A flat record (rather than a type hierarchy) so System.Text.Json round-trips cleanly through pm:developSettings without polymorphism attributes. Coordinates are normalised image-space (0..1), so masks survive resizing the preview vs the full image. Linear gradient: a line from (X0,Y0) to (X1,Y1). Mask weight is 0 at the "Zero" point projecting onto the line and 1 at the "Full" point — pixels off either end clamp to the nearest end's weight. Radial gradient: an ellipse centred at (CenterX, CenterY) with half-axes (RadiusX, RadiusY), optionally rotated by Angle degrees. Feather (0..100) controls the falloff curve; Invert flips inside vs outside. |
| `Angle` | `double Angle { get; init; }` |  |
| `BrushDabs` | `IReadOnlyList<BrushDab> BrushDabs { get; init; }` |  |
| `CenterX` | `double CenterX { get; init; }` |  |
| `CenterY` | `double CenterY { get; init; }` |  |
| `Combine` | `MaskCombineOp Combine { get; init; }` |  |
| `Feather` | `double Feather { get; init; }` |  |
| `HueRangeFeather` | `double HueRangeFeather { get; init; }` |  |
| `HueRangeMax` | `double HueRangeMax { get; init; }` |  |
| `HueRangeMin` | `double HueRangeMin { get; init; }` |  |
| `Invert` | `bool Invert { get; init; }` |  |
| `LuminanceRangeFeather` | `double LuminanceRangeFeather { get; init; }` |  |
| `LuminanceRangeMax` | `double LuminanceRangeMax { get; init; }` |  |
| `LuminanceRangeMin` | `double LuminanceRangeMin { get; init; }` |  |
| `RadiusX` | `double RadiusX { get; init; }` |  |
| `RadiusY` | `double RadiusY { get; init; }` |  |
| `Type` | `LocalMaskType Type { get; init; }` |  |
| `X0` | `double X0 { get; init; }` |  |
| `X1` | `double X1 { get; init; }` |  |
| `Y0` | `double Y0 { get; init; }` |  |
| `Y1` | `double Y1 { get; init; }` |  |

#### `LocalMaskType`

Discriminator for `Type`.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Linear` | `0` | Two-point gradient (Adobe `Mask/Gradient`). |
| `Radial` | `1` | Centred ellipse gradient (Adobe `Mask/CircularGradient`). |
| `Brush` | `2` | User-painted dab cloud (Adobe `Mask/Brush`). |
| `Inpaint` | `3` | Content-aware fill / object removal. The brush dabs define the mask region to remove; at render time the mask is dilated by a small feather margin and fed to `OnnxInpainter` which replaces those pixels with surrounding content. |

#### `LutLoader`

Parses Adobe `.cube` and Autodesk `.3dl` 3D LUT files into a `Lut3D`. Both formats use the same N³ RGB-triplet payload — only the header preamble differs: `.cube`: `LUT_3D_SIZE N`, optional `DOMAIN_MIN/MAX`, optional `TITLE`, then triplets in 0..1.`.3dl`: integer triplets scaled to a power-of-two max value (e.g. 0..1023). Cube size is inferred from the row count when no `Mesh` directive is present. Comments start with `#`; blank lines are ignored. Both Unix and Windows newlines work since we tokenise on whitespace.

| Member | Signature | Summary |
| --- | --- | --- |
| `Load` | `static Lut3D Load(FileInfo file)` |  |
| `Parse3dl` | `static Lut3D Parse3dl(string text)` |  |
| `ParseCube` | `static Lut3D ParseCube(string text)` |  |

#### `MaskCombineOp`

How a sub-mask combines with the primary mask weight.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Add` | `0` | Add this mask's weight to the running total (primary's default). |
| `Subtract` | `1` | Subtract this mask's weight from the running total — carves a hole. |
| `Intersect` | `2` | Multiply with this mask's weight — narrows to the intersection. |

#### `RestorationSettings`

User-facing knobs for the restoration window. Each value is 0..1 (slider range / 100) and gets applied as a strength against the corresponding ONNX model — internal pipeline machinery converts to the model-specific parameters so inexperienced users can leave the presets alone and still get superior results. `UpscaleFactor` is the only non-strength knob: 1 = off, 2 / 4 / 16 / 64 follow the same set as the develop-window upscaler.

Implements `IEquatable<RestorationSettings>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `RestorationSettings` | `RestorationSettings(double FaceRestoreStrength = 0, double DenoiseStrength = 0, double ArtifactRemoveStrength = 0, double RecolourStrength = 0, bool AutoTone = false, int UpscaleFactor = 1, string FaceRestoreModel = null, string DenoiseModel = null, string ArtifactRemoveModel = null, string ColorizeModel = null, string UpscaleModel = null, double ChromaBoost = 1.6, bool AutoScratchRemoval = true, int AutoScratchMaxIterations = 1, double AutoScratchThresholdPct = 0.3, double DespeckleStrength = 0, double AutoScratchDetectorThreshold = 0.4)` | User-facing knobs for the restoration window. Each value is 0..1 (slider range / 100) and gets applied as a strength against the corresponding ONNX model — internal pipeline machinery converts to the model-specific parameters so inexperienced users can leave the presets alone and still get superior results. `UpscaleFactor` is the only non-strength knob: 1 = off, 2 / 4 / 16 / 64 follow the same set as the develop-window upscaler. |
| `ArtifactRemoveModel` | `string ArtifactRemoveModel { get; init; }` |  |
| `ArtifactRemoveStrength` | `double ArtifactRemoveStrength { get; init; }` |  |
| `AutoScratchDetectorThreshold` | `double AutoScratchDetectorThreshold { get; init; }` |  |
| `AutoScratchMaxIterations` | `int AutoScratchMaxIterations { get; init; }` |  |
| `AutoScratchRemoval` | `bool AutoScratchRemoval { get; init; }` |  |
| `AutoScratchThresholdPct` | `double AutoScratchThresholdPct { get; init; }` |  |
| `AutoTone` | `bool AutoTone { get; init; }` |  |
| `ChromaBoost` | `double ChromaBoost { get; init; }` |  |
| `ColorizeModel` | `string ColorizeModel { get; init; }` |  |
| `DamagedColour` | `static RestorationSettings DamagedColour { get; }` | "Damaged colour photo" preset — keeps the existing colour, denoises and restores faces. |
| `DenoiseModel` | `string DenoiseModel { get; init; }` |  |
| `DenoiseStrength` | `double DenoiseStrength { get; init; }` |  |
| `DespeckleStrength` | `double DespeckleStrength { get; init; }` |  |
| `FaceRestoreModel` | `string FaceRestoreModel { get; init; }` |  |
| `FaceRestoreStrength` | `double FaceRestoreStrength { get; init; }` |  |
| `FadedSlide` | `static RestorationSettings FadedSlide { get; }` | "Faded slide / VHS frame" preset — gentle, preserves character. |
| `IsIdentity` | `bool IsIdentity { get; }` |  |
| `OldBlackAndWhite` | `static RestorationSettings OldBlackAndWhite { get; }` |  |
| `RecolourStrength` | `double RecolourStrength { get; init; }` |  |
| `SubtleCleanup` | `static RestorationSettings SubtleCleanup { get; }` | "Subtle clean-up" preset — light touch for already-decent originals. |
| `UpscaleFactor` | `int UpscaleFactor { get; init; }` |  |
| `UpscaleModel` | `string UpscaleModel { get; init; }` |  |

#### `SaltAndPepperOptions`

Tunables for `Filter`. Defaults are calibrated against typical dust / speckle severity in scanned old photos.

Implements `IEquatable<SaltAndPepperOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `SaltAndPepperOptions` | `SaltAndPepperOptions()` |  |
| `MaxWindow` | `int MaxWindow { get; init; }` | Largest window (in pixels) the adaptive pass tries. Bigger windows catch larger speckles but risk smoothing genuine small features. 7 catches 1–3-pixel dust; 9 catches up to 5-pixel speckles but starts touching small detail. |
| `NoiseThreshold` | `int NoiseThreshold { get; init; }` | Pixels deviating from their local median by more than this many luma units get replaced with the median. Higher = only replace extreme outliers (preserves more detail); lower = more aggressive despeckling. |

#### `ScratchDetectorOptions`

Tunables for `Detect`. Defaults are calibrated against typical 1024–2000-pixel-edge scanned old photos.

Implements `IEquatable<ScratchDetectorOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ScratchDetectorOptions` | `ScratchDetectorOptions()` |  |
| `Beta` | `double Beta { get; init; }` | Frangi β — controls how strongly the filter discriminates between blob-like and tube-like structures. Smaller = stricter (only very elongated structures pass). Frangi's original paper uses 0.5. |
| `C` | `double C { get; init; }` | Frangi c — controls sensitivity to "structureness". Smaller = more sensitive to faint structures (more detections, more false positives). Pixel intensities here are in [0..255], so c values in 5–25 are sensible. |
| `ClaheClipLimit` | `double ClaheClipLimit { get; init; }` | CLAHE clip limit (per-bin amplification ceiling, in multiples of the uniform per-bin count). Larger = stronger contrast boost, more noise amplification. |
| `ClaheTileSize` | `int ClaheTileSize { get; init; }` | CLAHE tile size in pixels. Smaller = more localized contrast (catches subtler scratches but boosts noise). |
| `DetectDarkScratches` | `bool DetectDarkScratches { get; init; }` | Also detect dark scratches (dark lines on lighter background) via bottom-hat. Off by default because it catches dark hair strands / eyebrows / shadow lines as false positives. Turn on for landscape or document scans where damage tends to leave darker marks. |
| `HighPassSigma` | `double HighPassSigma { get; init; }` | Sigma for the high-pass blur (large = preserves more detail; smaller = stronger flattening). |
| `HighPassStrength` | `double HighPassStrength { get; init; }` | How much of the high-pass to add back. 1.0 = identity high-pass, >1 boosts local detail. |
| `MinAspectRatio` | `double MinAspectRatio { get; init; }` | Drop connected components whose bounding-box aspect ratio (long-edge / short-edge) is below this threshold. Real scratches have ratio ≫ 1 (long-thin lines); blobs (eyes, hair clumps) sit closer to 1. 3.0 = component must be at least 3× longer than wide. |
| `MinComponentArea` | `int MinComponentArea { get; init; }` | Drop connected components smaller than this many pixels — filters out dust speckles and paper grain that survive the threshold. 0 = keep everything. |
| `MultiScale` | `bool MultiScale { get; init; }` | Run Frangi at multiple sigmas (1.0, 1.5, 2.5) and take the per-pixel maximum. Catches scratches of varying widths in a single pass. Off = single-scale at `Sigma`. |
| `ResponseCutoff` | `double ResponseCutoff { get; init; }` | Threshold cutoff as a fraction of the 99th-percentile response. 0.20 = anything ≥ 20% of the strong-response level survives. Lower = more thorough at the cost of false positives. Note: very faint scratches (under ~10 luma delta from surrounding pixels) are essentially undetectable by any classical algorithm without massive false positives — the brush is the right tool for those. |
| `Sigma` | `double Sigma { get; init; }` | Gaussian smoothing scale (in pixels), used when `MultiScale` is off. Picks the width of features the Frangi filter responds to most strongly. ≈1.0 catches 1–2-px hairline scratches; bump to 2-3 for chunkier paper-crease tears. |
| `SkinToneSuppress` | `bool SkinToneSuppress { get; init; }` | Suppress detections in skin-toned regions so hair / eyebrow lines don't end up in the mask. On = safer for portraits, off = more thorough on landscapes. No effect on B&W sources (R = G = B). |
| `UseClahe` | `bool UseClahe { get; init; }` | Apply CLAHE (Contrast-Limited Adaptive Histogram Equalization) before Frangi runs. Stronger contrast lift than the high-pass but amplifies fine texture (curtains, fabric, paper grain) into false positives. Off by default. |
| `UseHighPass` | `bool UseHighPass { get; init; }` | Apply a heavy-Gaussian high-pass before Frangi (subtract a strongly-blurred copy from the original, recombine around 128). Flattens large-scale luminance variation so scratches across bright skin / sky become as visible to Frangi as scratches on dark backgrounds. Default on; turn off for already-flat sources. |

#### `StageProgress`

Progress event emitted by long-running restoration stages so the UI can show an accurate "patch X / Y · ~M:SS remaining" instead of a pure stopwatch readout. `StageName` identifies the stage (so the UI can label "Denoise" / "Upscale (1/2)" / etc.); `DoneUnits` over `TotalUnits` is the within-stage fraction. Tile-based stages report tile counts; non-tile stages can report (0,1) at start and (1,1) at completion so the UI still gets a stage transition signal.`EstimatedTotalSeconds` is a conservative time estimate the stage emits when it knows roughly how long it'll take — usually computed from input pixel count and a known per-stage throughput rate. The UI uses it as the initial ETA before the first tile completes (= before the actual elapsed/done ratio becomes meaningful). Set to 0 when the stage has no estimate to offer; the UI then falls back to per-key history.

Implements `IEquatable<StageProgress>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `StageProgress` | `StageProgress(string StageName, int DoneUnits, int TotalUnits, double EstimatedTotalSeconds = 0, double EstimatedRemainingPipelineSeconds = 0)` | Progress event emitted by long-running restoration stages so the UI can show an accurate "patch X / Y · ~M:SS remaining" instead of a pure stopwatch readout. `StageName` identifies the stage (so the UI can label "Denoise" / "Upscale (1/2)" / etc.); `DoneUnits` over `TotalUnits` is the within-stage fraction. Tile-based stages report tile counts; non-tile stages can report (0,1) at start and (1,1) at completion so the UI still gets a stage transition signal.`EstimatedTotalSeconds` is a conservative time estimate the stage emits when it knows roughly how long it'll take — usually computed from input pixel count and a known per-stage throughput rate. The UI uses it as the initial ETA before the first tile completes (= before the actual elapsed/done ratio becomes meaningful). Set to 0 when the stage has no estimate to offer; the UI then falls back to per-key history. |
| `DoneUnits` | `int DoneUnits { get; init; }` |  |
| `EstimatedRemainingPipelineSeconds` | `double EstimatedRemainingPipelineSeconds { get; init; }` |  |
| `EstimatedTotalSeconds` | `double EstimatedTotalSeconds { get; init; }` |  |
| `StageName` | `string StageName { get; init; }` |  |
| `TotalUnits` | `int TotalUnits { get; init; }` |  |

#### `ThumbnailFitter.Fit`

One scored candidate that fits the byte budget.

Implements `IEquatable<Fit>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Fit` | `Fit(byte[] JpegBytes, int Width, int Height, int Quality, double PsnrDb)` | One scored candidate that fits the byte budget. |
| `Height` | `int Height { get; init; }` |  |
| `JpegBytes` | `byte[] JpegBytes { get; init; }` |  |
| `PsnrDb` | `double PsnrDb { get; init; }` |  |
| `Quality` | `int Quality { get; init; }` |  |
| `Width` | `int Width { get; init; }` |  |

#### `VirtualCopyDiscovery`

Resolves the on-disk locations of "virtual copy" XMP sidecars next to a source image. Copy 0 is the embedded XMP packet inside the source file (no sidecar). Copies 1, 2, … live as `basename.copyN.xmp` alongside the source so the original pixels are never duplicated. The numbering is sparse-tolerant: copies 1 and 3 can exist without copy 2.

| Member | Signature | Summary |
| --- | --- | --- |
| `EnumerateIndices` | `static IReadOnlyList<int> EnumerateIndices(FileInfo sourceFile)` | The copy indices currently present on disk (sorted ascending). |
| `Enumerate` | `static IReadOnlyList<ValueTuple<int, FileInfo>> Enumerate(FileInfo sourceFile)` | Returns the copy-sidecar paths next to `sourceFile` in ascending index order. Copy 0 (embedded XMP) is NOT returned — only stand-alone sidecars on disk. |
| `NextAvailableIndex` | `static int NextAvailableIndex(FileInfo sourceFile)` | Smallest positive integer N such that `basename.copyN.xmp` is not already present. Gap-tolerant: with copies {1, 3} the next index is 2. |
| `SidecarFor` | `static FileInfo SidecarFor(FileInfo sourceFile, int copyIndex)` | The on-disk sidecar path for copy `copyIndex` next to `sourceFile`. Throws when `copyIndex` is < 1 — copy 0 is the embedded packet and has no sidecar. |

#### `VirtualCopyInfo`

Represents one virtual copy of a source image — a numbered XMP sidecar that stores an independent set of develop settings without duplicating the pixel data.

Implements `IEquatable<VirtualCopyInfo>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `VirtualCopyInfo` | `VirtualCopyInfo(FileInfo SourceFile, FileInfo SidecarFile, int CopyNumber)` | Represents one virtual copy of a source image — a numbered XMP sidecar that stores an independent set of develop settings without duplicating the pixel data. |
| `CopyNumber` | `int CopyNumber { get; init; }` | Positive integer (1, 2, …); 0 is the embedded/original. |
| `SidecarFile` | `FileInfo SidecarFile { get; init; }` | The sidecar on disk (e.g. IMG_001.copy2.xmp). |
| `SourceFile` | `FileInfo SourceFile { get; init; }` | The original image (e.g. IMG_001.jpg). |

#### `VirtualCopyManager`

Discovery + CRUD for virtual-copy XMP sidecars. Each virtual copy stores its own `pm:developSettings` / `crs:` tags in a `{stem}.copy{N}.xmp` file next to the source image. The original's sidecar remains `{stem}.xmp` (or embedded XMP for JPEGs).

| Member | Signature | Summary |
| --- | --- | --- |
| `CreateCopy` | `static VirtualCopyInfo CreateCopy(FileInfo sourceFile, DevelopSettings baseSettings = null)` | Creates the next available virtual copy for `sourceFile`, writing an initial XMP sidecar with the given (or default) develop settings. Returns the `VirtualCopyInfo` describing the new copy. |
| `DeleteCopy` | `static void DeleteCopy(VirtualCopyInfo copy)` | Deletes the sidecar file for the given virtual copy. Does nothing if the sidecar does not exist. |
| `Discover` | `static IReadOnlyList<VirtualCopyInfo> Discover(FileInfo sourceFile)` | Scans the directory containing `sourceFile` for `{stem}.copy{N}.xmp` siblings and returns them in ascending copy-number order. |

### Namespace `Hawkynt.PhotoManager.Core.Enhance`

[`CropAspect`](#cropaspect) · [`MagicEnhanceOptions`](#magicenhanceoptions)

#### `CropAspect`

Standard print/post aspect ratio (e.g. Square 1:1, Portrait 4:5).

Implements `IEquatable<CropAspect>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CropAspect` | `CropAspect(string Name, double Width, double Height)` | Standard print/post aspect ratio (e.g. Square 1:1, Portrait 4:5). |
| `Height` | `double Height { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `Width` | `double Width { get; init; }` |  |

#### `MagicEnhanceOptions`

Thresholds + opt-ins for `EnhanceAsync`. Defaults reflect "smartphone snap" expectations — adjust when running against pro/already-clean sources.

Implements `IEquatable<MagicEnhanceOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MagicEnhanceOptions` | `MagicEnhanceOptions(double LowLightThreshold = 0.3, double NoiseThreshold = 0.3, double HazeThreshold = 0.3, double LowResolutionThreshold = 0.4, bool RunAutoCorrections = true, bool RunClahe = true, bool RunUpscale = true, bool RunArtifactRemoval = false, bool ComputeNimaScore = true, string UpscaleModelFileName = null)` | Thresholds + opt-ins for `EnhanceAsync`. Defaults reflect "smartphone snap" expectations — adjust when running against pro/already-clean sources. |
| `ComputeNimaScore` | `bool ComputeNimaScore { get; init; }` |  |
| `HazeThreshold` | `double HazeThreshold { get; init; }` |  |
| `LowLightThreshold` | `double LowLightThreshold { get; init; }` |  |
| `LowResolutionThreshold` | `double LowResolutionThreshold { get; init; }` |  |
| `NoiseThreshold` | `double NoiseThreshold { get; init; }` |  |
| `RunArtifactRemoval` | `bool RunArtifactRemoval { get; init; }` |  |
| `RunAutoCorrections` | `bool RunAutoCorrections { get; init; }` |  |
| `RunClahe` | `bool RunClahe { get; init; }` |  |
| `RunUpscale` | `bool RunUpscale { get; init; }` |  |
| `UpscaleModelFileName` | `string UpscaleModelFileName { get; init; }` |  |

### Namespace `Hawkynt.PhotoManager.Core.Enums`

[`DateTimeSource`](#datetimesource) · [`DuplicateHandling`](#duplicatehandling) · [`FileOperationResult`](#fileoperationresult)

#### `DateTimeSource`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Unknown` | `0` |  |
| `FileCreatedAt` | `1` |  |
| `FileModifiedAt` | `2` |  |
| `Gps` | `3` |  |
| `ExifIfd0` | `4` |  |
| `ExifSubIfd` | `5` |  |
| `FileName` | `6` |  |

#### `DuplicateHandling`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Rename` | `0` | Rename the file with a number suffix (default behavior) |
| `Skip` | `1` | Skip the file if destination exists |
| `Overwrite` | `2` | Overwrite the existing file |
| `Smart` | `3` | Smart handling: Skip if identical, rename if different |

#### `FileOperationResult`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Success` | `0` | File operation completed successfully |
| `Skipped` | `1` | File was skipped (already exists and skip policy applied) |
| `DuplicateRemoved` | `2` | Source file is identical to target, source was deleted |
| `Failed` | `3` | Operation failed due to an error |

### Namespace `Hawkynt.PhotoManager.Core.Faces`

[`DetectedFace`](#detectedface) · [`FaceCluster`](#facecluster) · [`FaceClusterIndex`](#faceclusterindex) · [`FaceRecognitionService`](#facerecognitionservice) · [`FaceRecognitionService.DetectionResult`](#facerecognitionservicedetectionresult) · [`IFaceDetector`](#ifacedetector) · [`LibraryFaceScanner`](#libraryfacescanner) · [`NullFaceDetector`](#nullfacedetector) · [`OnnxFaceDetector`](#onnxfacedetector) · [`OnnxFaceEmbedder`](#onnxfaceembedder) · [`PeopleRegistry`](#peopleregistry) · [`PersonTimeline`](#persontimeline) · [`PersonTimelineData`](#persontimelinedata) · [`ScannedFace`](#scannedface) · [`YearBucket`](#yearbucket)

#### `DetectedFace`

A face as returned from a detector. `Region` carries the bounding box (and optionally a pre-assigned name, e.g. from a prior tag). `Embedding` is the face descriptor — null if the detector doesn't produce one.

Implements `IEquatable<DetectedFace>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectedFace` | `DetectedFace(FaceRegion Region, float Confidence, float[] Embedding = null)` | A face as returned from a detector. `Region` carries the bounding box (and optionally a pre-assigned name, e.g. from a prior tag). `Embedding` is the face descriptor — null if the detector doesn't produce one. |
| `Confidence` | `float Confidence { get; init; }` |  |
| `Embedding` | `float[] Embedding { get; init; }` |  |
| `Region` | `FaceRegion Region { get; init; }` |  |

#### `FaceCluster`

A group of faces that appear to belong to the same person, derived by agglomerative clustering over embedding vectors. `Label` is the first non-blank name encountered in the cluster; unnamed clusters surface as "Unknown-N" in the gallery so the user can select and name them.

Implements `IEquatable<FaceCluster>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FaceCluster` | `FaceCluster(int Id, IReadOnlyList<ScannedFace> Members)` | A group of faces that appear to belong to the same person, derived by agglomerative clustering over embedding vectors. `Label` is the first non-blank name encountered in the cluster; unnamed clusters surface as "Unknown-N" in the gallery so the user can select and name them. |
| `Count` | `int Count { get; }` |  |
| `DisplayName` | `string DisplayName { get; }` |  |
| `Id` | `int Id { get; init; }` |  |
| `IsNamed` | `bool IsNamed { get; }` |  |
| `Label` | `string Label { get; }` |  |
| `Members` | `IReadOnlyList<ScannedFace> Members { get; init; }` |  |

#### `FaceClusterIndex`

Single-linkage agglomerative clustering over face embeddings using cosine similarity. Merges any two clusters if they contain a pair of faces whose similarity meets `SimilarityThreshold`. This is the Picasa behavior — aggressive merging keeps the gallery short; the user cleans up wrong merges by splitting manually. Kept fully in-memory and rebuilt each scan — per the no-local-DB rule, XMP is the truth and the cluster index is a view over it.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultSimilarityThreshold` | `const float DefaultSimilarityThreshold` |  |
| `Clusters` | `IReadOnlyList<FaceCluster> Clusters { get; }` |  |
| `SimilarityThreshold` | `float SimilarityThreshold { get; }` |  |
| `ApplyName` | `static IReadOnlyList<ScannedFace> ApplyName(FaceCluster cluster, string name)` | Propagate `name` onto every face in the cluster by updating each ScannedFace's Region.Label. Returns the updated (file, region) pairs so the caller can write them back through the metadata writer pipeline. Doesn't touch disk — keeps IO in the caller. |
| `BuildWithEmbeddingClustering` | `static FaceClusterIndex BuildWithEmbeddingClustering(IReadOnlyList<ScannedFace> faces, float similarityThreshold = 0.6)` | Build an index with embedding-similarity-based clustering for unnamed faces. Named faces still group by exact name. Unnamed faces that have embeddings are clustered using single-linkage agglomerative clustering: two clusters merge when ANY pair across them has cosine similarity >= `similarityThreshold`. Unnamed faces without embeddings remain singletons. Named faces with different names NEVER merge — embeddings only govern unnamed-to-unnamed grouping. |
| `Build` | `static FaceClusterIndex Build(IReadOnlyList<ScannedFace> faces, bool groupByName = true)` | Build an index from the scanned faces. Two modes: `groupByName` = true (default): faces that share the EXACT same non-blank name merge into one cluster. Unnamed faces stay as singletons. Two faces that happen to have similar embeddings but different names NEVER merge — the previous similarity-based path could silently join unrelated faces, especially when embeddings picked up shared lighting / pose from a single photo.`groupByName` = false: every face is its own cluster. Useful when the user wants to review each face individually before any auto-grouping. |
| `ComputeCentroid` | `static float[] ComputeCentroid(FaceCluster cluster)` | Compute the centroid (mean embedding) of a cluster's members that have embeddings. Returns null if no member has an embedding or if dimension lengths are inconsistent. |
| `FindNearestNamedClusters` | `IReadOnlyList<ValueTuple<FaceCluster, float>> FindNearestNamedClusters(FaceCluster target, int topN = 5)` | Find the top-N named clusters nearest to `target` by centroid-to-centroid cosine similarity. Returns pairs of (cluster, similarity) sorted by descending similarity. Only considers named clusters that have at least one member with an embedding. |

#### `FaceRecognitionService`

Runs face detection and reconciles the results with the sidecar: - Existing face regions in XMP stay put (user-tagged names are never lost). - New detected regions are added, named via registry match when possible. - Named faces contribute their person names as keywords for search. Face data lives in the broader `TaggedRegion` store alongside animals, items, and places — faces are just regions with `Person`. Non-face regions in the sidecar are preserved unchanged on every write.

| Member | Signature | Summary |
| --- | --- | --- |
| `FaceRecognitionService` | `FaceRecognitionService(IFaceDetector detector, IMetadataReader reader, IMetadataWriter writer, PeopleRegistry registry, OnnxFaceEmbedder embedder = null, float iouMergeThreshold = 0.5)` |  |
| `DetectAndWriteAsync` | `Task<IReadOnlyList<FaceRegion>> DetectAndWriteAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `DetectAsync` | `Task<DetectionResult> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` | Detect + reconcile + name-assign without touching the file. Returns the merged regions + keywords ready to feed into a MetadataEdit. Use `DetectAndWriteAsync` when you do want the file written straight away. |
| `TagFaceAsync` | `Task TagFaceAsync(FileInfo imageFile, int faceIndex, string name, float[] embedding = null, CancellationToken cancellationToken = null)` | Updates the Nth face region in the sidecar to have `name`, and (if an embedding is supplied) adds that embedding as a new reference for the person in the registry. |

#### `FaceRecognitionService.DetectionResult`

Outcome of a face-detection pass that hasn't been written to disk yet. Lets callers preview the merged region/keyword set before committing (e.g. defer the file write to the editor's Save button).

Implements `IEquatable<DetectionResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectionResult` | `DetectionResult(IReadOnlyList<TaggedRegion> Regions, IReadOnlyList<string> Keywords, IReadOnlyList<FaceRegion> Faces)` | Outcome of a face-detection pass that hasn't been written to disk yet. Lets callers preview the merged region/keyword set before committing (e.g. defer the file write to the editor's Save button). |
| `Faces` | `IReadOnlyList<FaceRegion> Faces { get; init; }` |  |
| `Keywords` | `IReadOnlyList<string> Keywords { get; init; }` |  |
| `Regions` | `IReadOnlyList<TaggedRegion> Regions { get; init; }` |  |

#### `IFaceDetector`

Detects faces in an image. Implementations may return naked regions (no embedding) or regions with `Embedding` populated, in which case callers can match them against `PeopleRegistry` to auto-assign names.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectAsync` | `Task<IReadOnlyList<DetectedFace>> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `LibraryFaceScanner`

Walks a directory tree, reads each supported image's metadata via `IMetadataReader`, and yields every face region. Picasa-style clustering builds on top of this — the scanner is IO-bound, the cluster index in `FaceClusterIndex` is the CPU-bound step. Deliberately does not persist anything. The architectural rule is "XMP + sidecar are the truth"; rebuild the in-memory index on demand.

| Member | Signature | Summary |
| --- | --- | --- |
| `LibraryFaceScanner` | `LibraryFaceScanner(IMetadataReader reader, ISupportedFormatsService formats)` |  |
| `ScanAsync` | `Task<IReadOnlyList<ScannedFace>> ScanAsync(DirectoryInfo root, bool recursive = true, bool onlyEmbedded = false, IProgress<FileInfo> progress = null, CancellationToken cancellationToken = null)` | Scans `root` recursively (by default) and reports every face-category region. `progress` receives the currently processing file; `onlyEmbedded` filters to faces that carry a persisted embedding (the ones clustering can use). |
| `ScanFilesAsync` | `Task<IReadOnlyList<ScannedFace>> ScanFilesAsync(IEnumerable<FileInfo> files, bool onlyEmbedded = false, IProgress<FileInfo> progress = null, CancellationToken cancellationToken = null)` | Scans an explicit list of files rather than walking a folder — used when the caller (e.g. the main window) already has a file grid populated and wants to reuse that selection instead of re-enumerating the tree. Extensions are NOT filtered here; the caller is expected to hand over already-filtered files. |

#### `NullFaceDetector`

No-op detector — always returns an empty list. The default wiring when an ONNX face-detection model has not been plugged in, so the rest of the face pipeline (XMP read/write, registry, CLI tagging) stays functional without a detector. Drop in an ONNX-backed implementation later without touching the service or CLI.

Implements `IFaceDetector`.

| Member | Signature | Summary |
| --- | --- | --- |
| `NullFaceDetector` | `NullFaceDetector()` |  |
| `DetectAsync` | `Task<IReadOnlyList<DetectedFace>> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `OnnxFaceDetector`

ONNX-backed face detector. Expects an UltraFace-style model that takes a 320×240 NCHW tensor normalized with (input - 127.0) / 128.0 and returns two outputs: scores [1, N, 2] and boxes [1, N, 4]. Works with both the `version-RFB-320.onnx` and `version-slim-320.onnx` variants. Returns an empty list when the model file is missing, following the same degrade-gracefully pattern as `YoloObjectDetector`. Drop the model at `ModelFile`("face-detector.onnx") to turn detection on.

Implements `IDisposable`, `IFaceDetector`.

| Member | Signature | Summary |
| --- | --- | --- |
| `OnnxFaceDetector` | `OnnxFaceDetector(FileInfo modelFile = null, float scoreThreshold = 0.7, float iouThreshold = 0.3)` |  |
| `DefaultModelFileName` | `const string DefaultModelFileName` |  |
| `IsAvailable` | `bool IsAvailable { get; }` |  |
| `DetectAsync` | `Task<IReadOnlyList<DetectedFace>> DetectAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `Dispose` | `void Dispose()` |  |

#### `OnnxFaceEmbedder`

Produces a fixed-length embedding vector for a face crop using an ONNX model (MobileFaceNet or ArcFace-compatible). The embedding is what `FindMatch` uses to auto-recognize a face against named references. Expected model: input `[1, 3, 112, 112]` float32 NCHW, pixels normalized to the [-1, 1] range (the MobileFaceNet convention). Output is either a flat 512-D vector or a `[1, N]` tensor; we L2-normalize the result so cosine similarity against the registry is well-defined. Drop a compatible model at `AppDataPaths.ModelFile("face-embedder.onnx")`. If missing, embedding requests return null and the caller's registry match simply doesn't fire.

Implements `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `OnnxFaceEmbedder` | `OnnxFaceEmbedder(FileInfo modelFile = null)` |  |
| `DefaultModelFileName` | `const string DefaultModelFileName` |  |
| `IsAvailable` | `bool IsAvailable { get; }` |  |
| `Dispose` | `void Dispose()` |  |
| `EmbedFaceAsync` | `Task<float[]> EmbedFaceAsync(FileInfo imageFile, NormalizedBoundingBox normalizedBox, CancellationToken cancellationToken = null)` | Produces the embedding for a face cropped from `imageFile` at the `normalizedBox`. Returns null when the model isn't loaded, the file doesn't exist, or decoding fails. |

#### `PeopleRegistry`

Persistent mapping from person name → one or more reference embedding vectors. Stored as a single JSON file in the app-data directory (per the no-local-DB principle: this is app-scoped state, not library truth — the file can be deleted without losing per-photo face tags in XMP sidecars). Match is by cosine similarity against any reference embedding for the person; threshold is caller-configurable (0.5-0.6 typical for ArcFace, tighter for MobileFaceNet).

| Member | Signature | Summary |
| --- | --- | --- |
| `PeopleRegistry` | `PeopleRegistry(FileInfo file = null)` |  |
| `KnownNames` | `IEnumerable<string> KnownNames { get; }` |  |
| `AddReference` | `void AddReference(string name, float[] embedding)` |  |
| `EmbeddingCount` | `int EmbeddingCount(string name)` |  |
| `FindBestMatch` | `ValueTuple<string, float>? FindBestMatch(float[] embedding, float minSimilarity = 0.65)` | Same match as `FindMatch` but also returns the similarity score. Callers that need to rank multiple detections against each other (e.g. to pick the best face for a name when several match) use this overload. |
| `FindMatch` | `string FindMatch(float[] embedding, float minSimilarity = 0.65)` | Returns the registered name whose closest reference has the highest cosine similarity against `embedding`, provided that similarity meets `minSimilarity`. Returns null if no registered person is close enough. The default threshold (`0.65`) is deliberately strict for single-reference matching. Lower thresholds like 0.55 start spreading names to different people in the same photo because ArcFace cosine similarity between unrelated faces can drift above 0.55 under matched lighting/pose. Callers who want looser matching can override, or populate multiple references per person via `AddReference`. |
| `Remove` | `void Remove(string name)` |  |
| `Save` | `void Save()` |  |

#### `PersonTimeline`

Pure query that builds a per-year histogram for one person's appearances in the library. No IO, no side effects — callers supply the already-known (date, file) pairs and get back a sorted breakdown.

| Member | Signature | Summary |
| --- | --- | --- |
| `Build` | `static PersonTimelineData Build(string personName, IEnumerable<ValueTuple<DateTime, FileInfo>> photosWithPerson)` | Build timeline data for a single person from their photo appearances. Returns `null` when `photosWithPerson` is empty. Year buckets are sorted ascending by year. |

#### `PersonTimelineData`

Aggregated timeline data for a single person: when they first and last appeared in the library, plus a per-year photo count breakdown.

Implements `IEquatable<PersonTimelineData>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PersonTimelineData` | `PersonTimelineData(string PersonName, DateOnly FirstSeen, DateOnly LastSeen, IReadOnlyList<YearBucket> YearBuckets)` | Aggregated timeline data for a single person: when they first and last appeared in the library, plus a per-year photo count breakdown. |
| `FirstSeen` | `DateOnly FirstSeen { get; init; }` |  |
| `LastSeen` | `DateOnly LastSeen { get; init; }` |  |
| `PersonName` | `string PersonName { get; init; }` |  |
| `YearBuckets` | `IReadOnlyList<YearBucket> YearBuckets { get; init; }` |  |

#### `ScannedFace`

A single face region found on disk during a library scan, paired with the file it came from. The full `TaggedRegion` is retained so callers can propagate names, re-render boxes, or write updated metadata back through the existing writer pipeline.

Implements `IEquatable<ScannedFace>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ScannedFace` | `ScannedFace(FileInfo File, TaggedRegion Region)` | A single face region found on disk during a library scan, paired with the file it came from. The full `TaggedRegion` is retained so callers can propagate names, re-render boxes, or write updated metadata back through the existing writer pipeline. |
| `Embedding` | `float[] Embedding { get; }` |  |
| `File` | `FileInfo File { get; init; }` |  |
| `HasEmbedding` | `bool HasEmbedding { get; }` |  |
| `Name` | `string Name { get; }` |  |
| `Region` | `TaggedRegion Region { get; init; }` |  |

#### `YearBucket`

A single year bucket in a person's timeline, recording how many photos that person appeared in during that year.

Implements `IEquatable<YearBucket>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `YearBucket` | `YearBucket(int Year, int PhotoCount)` | A single year bucket in a person's timeline, recording how many photos that person appeared in during that year. |
| `PhotoCount` | `int PhotoCount { get; init; }` |  |
| `Year` | `int Year { get; init; }` |  |

### Namespace `Hawkynt.PhotoManager.Core.Geo`

[`GeoCluster`](#geocluster) · [`GeoClusterResult`](#geoclusterresult)

#### `GeoCluster`

Pure clustering engine that groups geographic points into a bounded number of clusters using k-means on normalized lat/lon (Euclidean distance on degree coordinates — sufficient for display clustering, not geodesic precision). The algorithm: 1. If the number of input points is already <= maxClusters, each point becomes its own cluster (no aggregation needed). 2. Otherwise, run k-means with `maxClusters` centroids, seeded via k-means++ initialization, for up to 50 iterations or until centroids converge.

| Member | Signature | Summary |
| --- | --- | --- |
| `ClusterByDistance` | `static IReadOnlyList<GeoClusterResult> ClusterByDistance(IReadOnlyList<ValueTuple<double, double, FileInfo>> points, double thresholdDegrees)` | Cluster points using a viewport-aware distance threshold instead of a fixed cluster count. Two points that are closer together than `thresholdDegrees` (in lat/lon space) get merged. This is the overload the map UI calls on every zoom change — the threshold is derived from the current map resolution so clusters split apart as the user zooms in. |
| `Cluster` | `static IReadOnlyList<GeoClusterResult> Cluster(IReadOnlyList<ValueTuple<double, double, FileInfo>> points, int maxClusters = 50)` | Cluster the supplied geographic points into at most `maxClusters` groups. |

#### `GeoClusterResult`

Result of geographic clustering: a center point with all member files. When `Members` has exactly one entry the cluster represents a single pin; when it has more, it represents a cluster pin that should display the count.

Implements `IEquatable<GeoClusterResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GeoClusterResult` | `GeoClusterResult(double CenterLat, double CenterLon, IReadOnlyList<FileInfo> Members)` | Result of geographic clustering: a center point with all member files. When `Members` has exactly one entry the cluster represents a single pin; when it has more, it represents a cluster pin that should display the count. |
| `CenterLat` | `double CenterLat { get; init; }` |  |
| `CenterLon` | `double CenterLon { get; init; }` |  |
| `IsSinglePin` | `bool IsSinglePin { get; }` | True when this cluster represents a single photo pin. |
| `Members` | `IReadOnlyList<FileInfo> Members { get; init; }` |  |

### Namespace `Hawkynt.PhotoManager.Core.Geocoding`

[`GeoDistance`](#geodistance) · [`GeocodingResult`](#geocodingresult) · [`GeofenceBatchService`](#geofencebatchservice) · [`GeofenceBatchService.FileOutcome`](#geofencebatchservicefileoutcome) · [`GeofenceBatchService.Options`](#geofencebatchserviceoptions) · [`GeofenceBatchService.Progress`](#geofencebatchserviceprogress) · [`GeofenceMatcher`](#geofencematcher) · [`GreatCircle`](#greatcircle) · [`IElevationService`](#ielevationservice) · [`IReverseGeocoder`](#ireversegeocoder) · [`KmlExportOptions`](#kmlexportoptions) · [`KmlExporter`](#kmlexporter) · [`MapBookmark`](#mapbookmark) · [`MapBookmarkApplier`](#mapbookmarkapplier) · [`MapBookmarkStore`](#mapbookmarkstore) · [`NominatimReverseGeocoder`](#nominatimreversegeocoder) · [`OpenTopoElevationService`](#opentopoelevationservice) · [`PhotoResection`](#photoresection) · [`PhotoResection.LandmarkObservation`](#photoresectionlandmarkobservation) · [`PhotoResection.Result`](#photoresectionresult) · [`ReverseGeocodeBatchEntry`](#reversegeocodebatchentry) · [`ReverseGeocodeBatchOptions`](#reversegeocodebatchoptions) · [`ReverseGeocodeBatchOutcome`](#reversegeocodebatchoutcome) · [`ReverseGeocodeBatchService`](#reversegeocodebatchservice) · [`SolarLunarCalculator`](#solarlunarcalculator) · [`SunMoonArrowComputer`](#sunmoonarrowcomputer) · [`SunMoonArrowData`](#sunmoonarrowdata) · [`Triangulation`](#triangulation) · [`Triangulation.Landmark`](#triangulationlandmark)

#### `GeoDistance`

Bare-bones haversine helper that operates on raw `double` latitude / longitude pairs — handy for radius-search filtering loops where the caller is already holding raw doubles (e.g. the WorldMapWindow nearby-search). For coordinate-bearing geodesy use `GreatCircle`.

| Member | Signature | Summary |
| --- | --- | --- |
| `EarthRadiusMeters` | `const double EarthRadiusMeters` | Mean Earth radius in meters; matches `EarthRadiusMeters`. |
| `HaversineMeters` | `static double HaversineMeters(double lat1, double lon1, double lat2, double lon2)` | Great-circle distance in meters between two lat/lon pairs (decimal degrees) using the haversine formula. Same accuracy class as `DistanceMeters` — sub-kilometer over thousands of km — but with a primitive-doubles signature suitable for tight loops. |

#### `GeocodingResult`

A reverse-geocoded place for a GPS coordinate. Fields map roughly to the XMP location schema (`Iptc4xmpCore:Location` + `photoshop:City/State/Country`) so the result can flow straight into a `MetadataEdit`. Any field may be null — geocoders sometimes only resolve a subset.

Implements `IEquatable<GeocodingResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GeocodingResult` | `GeocodingResult(string Location, string City, string State, string Country, string CountryCode)` | A reverse-geocoded place for a GPS coordinate. Fields map roughly to the XMP location schema (`Iptc4xmpCore:Location` + `photoshop:City/State/Country`) so the result can flow straight into a `MetadataEdit`. Any field may be null — geocoders sometimes only resolve a subset. |
| `City` | `string City { get; init; }` |  |
| `CountryCode` | `string CountryCode { get; init; }` |  |
| `Country` | `string Country { get; init; }` |  |
| `HasAny` | `bool HasAny { get; }` |  |
| `Location` | `string Location { get; init; }` |  |
| `State` | `string State { get; init; }` |  |

#### `GeofenceBatchService`

Walks a list of files, looks up each photo's GPS, and applies the first matching `MapBookmark`'s place fields via `MapBookmarkApplier`. Mirrors the dry-run + cancellable + progress-reporting shape of the reverse-geocode batch flow so the UI can host both with the same plumbing.

| Member | Signature | Summary |
| --- | --- | --- |
| `GeofenceBatchService` | `GeofenceBatchService(IMetadataReader reader, IMetadataWriter writer)` | Walks a list of files, looks up each photo's GPS, and applies the first matching `MapBookmark`'s place fields via `MapBookmarkApplier`. Mirrors the dry-run + cancellable + progress-reporting shape of the reverse-geocode batch flow so the UI can host both with the same plumbing. |
| `RunAsync` | `Task<IReadOnlyList<FileOutcome>> RunAsync(IReadOnlyList<FileInfo> files, IReadOnlyList<MapBookmark> bookmarks, Options options, IProgress<Progress> progress = null, CancellationToken cancellationToken = null)` |  |

#### `GeofenceBatchService.FileOutcome`

Implements `IEquatable<FileOutcome>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FileOutcome` | `FileOutcome(FileInfo File, IReadOnlyList<MapBookmark> Matches, bool WouldWrite, string Error)` |  |
| `Error` | `string Error { get; init; }` |  |
| `File` | `FileInfo File { get; init; }` |  |
| `Matches` | `IReadOnlyList<MapBookmark> Matches { get; init; }` |  |
| `WouldWrite` | `bool WouldWrite { get; init; }` |  |

#### `GeofenceBatchService.Options`

Implements `IEquatable<Options>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Options` | `Options(bool DryRun = false, bool OnlyFillEmpty = true)` |  |
| `DryRun` | `bool DryRun { get; init; }` |  |
| `OnlyFillEmpty` | `bool OnlyFillEmpty { get; init; }` |  |

#### `GeofenceBatchService.Progress`

Implements `IEquatable<Progress>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Progress` | `Progress(int Processed, int Total, FileOutcome Latest)` |  |
| `Latest` | `FileOutcome Latest { get; init; }` |  |
| `Processed` | `int Processed { get; init; }` |  |
| `Total` | `int Total { get; init; }` |  |

#### `GeofenceMatcher`

Pure-logic helper that decides which user-saved bookmarks contain a given GPS coordinate inside their radius. No I/O, no metadata reads — callers feed in the bookmarks they want considered. Multiple overlapping bookmarks all match; ordering follows the input list so the first hit can be treated as canonical when callers want a single result.

| Member | Signature | Summary |
| --- | --- | --- |
| `MatchAll` | `static IReadOnlyList<MapBookmark> MatchAll(IReadOnlyList<MapBookmark> bookmarks, GpsCoordinate coordinate)` |  |

#### `GreatCircle`

Great-circle (spherical-earth) geodesy helpers. Accuracy is plenty for any photo-related workflow (sub-meter over short distances, sub-kilometer over thousands); we don't need WGS84-ellipsoid precision for tagging which direction a camera was pointing.

| Member | Signature | Summary |
| --- | --- | --- |
| `EarthRadiusMeters` | `const double EarthRadiusMeters` | Mean Earth radius in meters. |
| `BearingDegrees` | `static double BearingDegrees(GpsCoordinate from, GpsCoordinate to)` | Initial bearing (forward azimuth) from `from` to `to` in degrees, normalized to 0..360 where 0 is north and 90 is east. |
| `Destination` | `static GpsCoordinate Destination(GpsCoordinate from, double bearingDegrees, double distanceMeters)` | Destination coordinate given a start, initial bearing (degrees, 0 = north) and distance in meters along the great-circle path. |
| `DistanceMeters` | `static double DistanceMeters(GpsCoordinate a, GpsCoordinate b)` | Haversine distance in meters between two coordinates. |
| `NormalizeDegrees` | `static double NormalizeDegrees(double degrees)` | Wraps a degree value into the [0, 360) range. Handles both sides of the wrap (e.g. -0.1 → 359.9, 360 → 0, 720.5 → 0.5) including the float edge case where a tiny-negative input rounds up to exactly 360 after adding 360. |

#### `IElevationService`

Resolves an elevation (meters above sea level) for a given GPS coordinate. Backed by an online DEM (e.g. `OpenTopoElevationService`) but the interface is deliberately abstract so callers can plug a cached, an offline, or a test service transparently.

| Member | Signature | Summary |
| --- | --- | --- |
| `GetAltitudeMetersAsync` | `Task<double?> GetAltitudeMetersAsync(GpsCoordinate coordinate, CancellationToken cancellationToken = null)` |  |

#### `IReverseGeocoder`

| Member | Signature | Summary |
| --- | --- | --- |
| `ResolveAsync` | `Task<GeocodingResult> ResolveAsync(GpsCoordinate coordinate, CancellationToken cancellationToken = null)` |  |

#### `KmlExportOptions`

User-tunable knobs for KML output. Defaults match the most common Google Earth / GPX-viewer expectations: altitude when available, a per-pin timestamp, and a description string built from the photo's place fields.

Implements `IEquatable<KmlExportOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `KmlExportOptions` | `KmlExportOptions(string DocumentName = "PhotoManager photo locations", bool IncludeAltitude = true, bool IncludeTimestamp = true, bool IncludePlaceFields = true)` | User-tunable knobs for KML output. Defaults match the most common Google Earth / GPX-viewer expectations: altitude when available, a per-pin timestamp, and a description string built from the photo's place fields. |
| `DocumentName` | `string DocumentName { get; init; }` |  |
| `IncludeAltitude` | `bool IncludeAltitude { get; init; }` |  |
| `IncludePlaceFields` | `bool IncludePlaceFields { get; init; }` |  |
| `IncludeTimestamp` | `bool IncludeTimestamp { get; init; }` |  |

#### `KmlExporter`

Writes a single KML 2.2 document — one `<Placemark>` per geotagged photo — that any KML reader (Google Earth, gpx.studio, QGIS…) can ingest. Coordinates are emitted in the KML quirk order `lon,lat,alt` with invariant-culture decimals so a German locale doesn't sneak commas into the numbers.

| Member | Signature | Summary |
| --- | --- | --- |
| `BuildDocument` | `static XDocument BuildDocument(IReadOnlyList<ValueTuple<FileInfo, FullMetadata>> entries, KmlExportOptions options)` | Build the KML document in memory. Pure function so tests can assert on the document tree without ever touching disk. |
| `ExportAsync` | `static Task ExportAsync(IReadOnlyList<ValueTuple<FileInfo, FullMetadata>> entries, FileInfo destination, KmlExportOptions options = null, CancellationToken cancellationToken = null)` | Build the document and persist it to `destination`. Writes to a sibling `.tmp` file first then atomically replaces the target so an interrupted export can never truncate an existing file. |

#### `MapBookmark`

A user-curated map favourite — typically a place they revisit (e.g. a venue, a landmark) that they want to geotag photos with in one click. Holds the GPS pin plus an optional radius hint and the place fields (city / state / country / etc.) so applying the bookmark also writes the location text into the photo's XMP metadata. Identity is by `Id` (a stable Guid created when the bookmark is first added) so the user can rename without losing the entry.

Implements `IEquatable<MapBookmark>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MapBookmark` | `MapBookmark()` |  |
| `City` | `string City { get; init; }` |  |
| `CountryCode` | `string CountryCode { get; init; }` |  |
| `Country` | `string Country { get; init; }` |  |
| `Id` | `string Id { get; init; }` |  |
| `Latitude` | `double Latitude { get; init; }` |  |
| `Location` | `string Location { get; init; }` |  |
| `Longitude` | `double Longitude { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `RadiusMeters` | `double? RadiusMeters { get; init; }` | Optional informational radius in metres — used today only for display. |
| `State` | `string State { get; init; }` |  |
| `ToGps` | `GpsCoordinate ToGps()` |  |

#### `MapBookmarkApplier`

Pure-logic builder that converts a `MapBookmark` into the `MetadataEdit` patch we hand the writer. Lives separately from the window so it's trivial to unit-test the field projection without spinning up Avalonia. Place fields are only emitted when the bookmark actually carries them — blank strings stay default so existing values on the photo aren't stomped with empty patches. The GPS pin always wins (the whole point of applying a bookmark is to set the coordinates).

| Member | Signature | Summary |
| --- | --- | --- |
| `BuildEdit` | `static MetadataEdit BuildEdit(MapBookmark bookmark)` |  |
| `BuildEdit` | `static MetadataEdit BuildEdit(MapBookmark bookmark, FullMetadata existing, bool onlyFillEmpty)` |  |

#### `MapBookmarkStore`

JSON-backed store for `MapBookmark`s. Single file, one array, loaded on demand and rewritten atomically on every mutation. The default file lives under `AppDataPaths` so it survives reinstalls but stays out of the photo library — bookmarks are user preferences, not per-photo metadata. Tests inject their own `FileInfo`. Tolerates a missing file (returns empty), a corrupt file (returns empty, next save overwrites), and out-of-process edits (each call re-reads). No locking — Photo Manager is single-process.

| Member | Signature | Summary |
| --- | --- | --- |
| `MapBookmarkStore` | `MapBookmarkStore()` |  |
| `MapBookmarkStore` | `MapBookmarkStore(FileInfo file)` |  |
| `DefaultFile` | `static FileInfo DefaultFile()` |  |
| `Load` | `IReadOnlyList<MapBookmark> Load()` | Loads bookmarks from disk. Empty list when no file exists or the file is unreadable. |
| `Save` | `void Save(IEnumerable<MapBookmark> bookmarks)` | Replaces the on-disk list with `bookmarks`. |

#### `NominatimReverseGeocoder`

Reverse geocoder backed by OpenStreetMap's Nominatim service (`https://nominatim.openstreetmap.org/reverse`). No API key, free-to-use, subject to a 1-request-per-second rate limit that we enforce locally. Results are cached in memory by rounded lat/lon so moving around a single location doesn't re-query. Per Nominatim's usage policy we identify ourselves with a User-Agent that names the application — callers can override it if PhotoManager is being embedded somewhere else.

Implements `IDisposable`, `IReverseGeocoder`.

| Member | Signature | Summary |
| --- | --- | --- |
| `NominatimReverseGeocoder` | `NominatimReverseGeocoder(HttpClient httpClient = null, string endpoint = "https://nominatim.openstreetmap.org/reverse", string userAgent = "PhotoManager/1.0 (https://github.com/Hawkynt/PhotoManager)")` |  |
| `Dispose` | `void Dispose()` |  |
| `ResolveAsync` | `Task<GeocodingResult> ResolveAsync(GpsCoordinate coordinate, CancellationToken cancellationToken = null)` |  |

#### `OpenTopoElevationService`

Elevation lookup backed by the free OpenTopoData service (`https://api.opentopodata.org/v1/<dataset>`). No API key, rate-limited to 1 request/second — we honor that locally. Results cached in memory by lat/lon rounded to 5 decimals (~1 m resolution). Default dataset is `srtm30m` (NASA SRTM 1-arcsec, global except polar); callers can switch via `Dataset` if they need `eu-dem25m` or another higher-resolution regional DEM.

Implements `IDisposable`, `IElevationService`.

| Member | Signature | Summary |
| --- | --- | --- |
| `OpenTopoElevationService` | `OpenTopoElevationService(HttpClient httpClient = null, string endpointBase = "https://api.opentopodata.org/v1/", string userAgent = "PhotoManager/1.0 (https://github.com/Hawkynt/PhotoManager)")` |  |
| `Dataset` | `string Dataset { get; init; }` |  |
| `Dispose` | `void Dispose()` |  |
| `GetAltitudeMetersAsync` | `Task<double?> GetAltitudeMetersAsync(GpsCoordinate coordinate, CancellationToken cancellationToken = null)` |  |

#### `PhotoResection`

Three-point photogrammetric resection: given three landmarks visible in a photo with known world GPS and known horizontal pixel positions, and a known camera horizontal FOV, compute the unknown camera GPS AND heading in one pass. `Triangulation` requires the camera GPS up front; this solves for it from three landmarks. Approach: project the three landmarks onto a local tangent plane (flat-earth around the first landmark's latitude — valid to sub-meter over several km). The inscribed-angle theorem says an observer who sees two landmarks at angle α lies on a circular arc through those landmarks; intersecting two such arcs (one for pair AB, one for BC) yields the observer up to a two-fold ambiguity, which we resolve by picking the candidate whose predicted bearings match the observed pixel angles. Works well for landmarks that aren't colinear and aren't all near the same direction from the camera. Degenerates when the camera sits on the circumcircle through the three landmarks (the classic "dangerous circle" in surveying) — we return null in that case for the caller to surface.

| Member | Signature | Summary |
| --- | --- | --- |
| `Solve` | `static Result Solve(IReadOnlyList<LandmarkObservation> landmarks, double imageWidth, double horizontalFovDegrees)` | Solve camera GPS + heading from three or more landmarks. Uses the first three landmarks; extras are ignored (the inscribed-angle method is an exact closed form for 3 points). Returns null when the geometry is degenerate (landmarks too close together in pixels, camera on the dangerous circle, or observations internally inconsistent). |

#### `PhotoResection.LandmarkObservation`

One landmark visible in the photo: real-world GPS + pixel X.

Implements `IEquatable<LandmarkObservation>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LandmarkObservation` | `LandmarkObservation(GpsCoordinate Gps, double PixelX)` | One landmark visible in the photo: real-world GPS + pixel X. |
| `Gps` | `GpsCoordinate Gps { get; init; }` |  |
| `PixelX` | `double PixelX { get; init; }` |  |

#### `PhotoResection.Result`

Result of a successful resection.

Implements `IEquatable<Result>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Result` | `Result(GpsCoordinate CameraGps, double HeadingDegrees, double RmsAngularErrorDegrees)` | Result of a successful resection. |
| `CameraGps` | `GpsCoordinate CameraGps { get; init; }` |  |
| `HeadingDegrees` | `double HeadingDegrees { get; init; }` |  |
| `RmsAngularErrorDegrees` | `double RmsAngularErrorDegrees { get; init; }` |  |

#### `ReverseGeocodeBatchEntry`

Outcome of reverse-geocoding one file. The UI uses this to summarise the run ("Resolved 12 of 15; 2 had no GPS, 1 lookup failed") and to decide which rows in the grid need their search index rebuilt.

Implements `IEquatable<ReverseGeocodeBatchEntry>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ReverseGeocodeBatchEntry` | `ReverseGeocodeBatchEntry(FileInfo File, ReverseGeocodeBatchOutcome Outcome, GeocodingResult Result = null, string ErrorMessage = null)` | Outcome of reverse-geocoding one file. The UI uses this to summarise the run ("Resolved 12 of 15; 2 had no GPS, 1 lookup failed") and to decide which rows in the grid need their search index rebuilt. |
| `ErrorMessage` | `string ErrorMessage { get; init; }` |  |
| `File` | `FileInfo File { get; init; }` |  |
| `Outcome` | `ReverseGeocodeBatchOutcome Outcome { get; init; }` |  |
| `Result` | `GeocodingResult Result { get; init; }` |  |

#### `ReverseGeocodeBatchOptions`

Implements `IEquatable<ReverseGeocodeBatchOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ReverseGeocodeBatchOptions` | `ReverseGeocodeBatchOptions()` |  |
| `DryRun` | `bool DryRun { get; init; }` | When true, don't actually write — just return what would happen. Useful for the preview step in the dialog before the user commits. |
| `OnlyFillEmptyFields` | `bool OnlyFillEmptyFields { get; init; }` | When true, skip files that already have any place text. Matches what the per-photo button does: `PickResolveField` only fills holes. |

#### `ReverseGeocodeBatchOutcome`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Resolved` | `0` |  |
| `AlreadyTagged` | `1` |  |
| `NoGps` | `2` |  |
| `NoMatch` | `3` |  |
| `Error` | `4` |  |

#### `ReverseGeocodeBatchService`

Iterates a list of files, reads each one's metadata, asks the geocoder for an address, and writes the result back via the metadata writer. Sequential by design — Nominatim's 1 req/sec rate limit is enforced inside the geocoder itself, so parallelising here would just queue.

| Member | Signature | Summary |
| --- | --- | --- |
| `ReverseGeocodeBatchService` | `ReverseGeocodeBatchService(IReverseGeocoder geocoder, IMetadataReader reader, IMetadataWriter writer)` |  |
| `BuildEdit` | `static MetadataEdit BuildEdit(FullMetadata existing, GeocodingResult result, bool onlyFillEmptyFields)` | Maps a geocoder result onto a `MetadataEdit`, honouring the "only fill empty fields" flag exactly the way the per-photo Resolve Address button does. Public so the dialog can preview the patch. |
| `RunAsync` | `Task<IReadOnlyList<ReverseGeocodeBatchEntry>> RunAsync(IReadOnlyList<FileInfo> files, ReverseGeocodeBatchOptions options = null, IProgress<FileInfo> progress = null, CancellationToken cancellationToken = null)` |  |

#### `SolarLunarCalculator`

Sun & moon sky positions for a UTC instant + ground location. The sun routine implements the NOAA spreadsheet algorithm (Meeus Astronomical Algorithms low-accuracy formulation): obliquity, equation of time, declination, hour angle. Accurate to better than 0.1° for any photographic context. The moon routine uses the Astronomical Almanac low-precision formula (good to ~0.3° in position) — plenty for indicating which way the moon was hanging when the shutter clicked. Both methods return azimuth in degrees clockwise from true north (0 = N, 90 = E, 180 = S, 270 = W) and altitude in degrees above the horizon (negative = below).

| Member | Signature | Summary |
| --- | --- | --- |
| `DescribeTwilight` | `static string DescribeTwilight(double sunAltitudeDegrees)` | Convenience helper: returns a short description of the current solar twilight regime ("blue hour", "golden hour", "civil twilight"…) given the sun's altitude. Useful for the photo properties dialog hint. |
| `MoonPosition` | `static ValueTuple<double, double> MoonPosition(DateTime utc, double lat, double lon)` |  |
| `SunPosition` | `static ValueTuple<double, double> SunPosition(DateTime utc, double lat, double lon)` |  |

#### `SunMoonArrowComputer`

Computes `SunMoonArrowData` for both sun and moon given a GPS coordinate, a UTC timestamp, and a base arrow length in pixels. This is pure computation with no UI dependencies -- easy to unit test.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultBaseLength` | `const double DefaultBaseLength` | Default arrow length in pixels when the body sits exactly at the horizon. |
| `BuildArrow` | `static SunMoonArrowData BuildArrow(double azimuthDegrees, double altitudeDegrees, double baseLength)` | Convert an azimuth (compass degrees) and altitude (degrees above horizon) into a pixel-space arrow. The arrow points in the azimuth direction with length scaled by `cos(altitude)`: body at horizon gives full length; body overhead shrinks to zero. Screen coordinates: X positive = right, Y positive = up. The caller must negate Y for screen-down conventions if needed. |
| `Compute` | `static ValueTuple<SunMoonArrowData, SunMoonArrowData> Compute(double latitude, double longitude, DateTime utc, double baseLength = 80)` | Compute both sun and moon arrows for a given position and time. |

#### `SunMoonArrowData`

Pure-data result describing a sun or moon direction arrow to be drawn on a map overlay. All geometry is expressed as pixel offsets from the pin center so the UI layer can position the arrow without knowing anything about the underlying astronomy.

Implements `IEquatable<SunMoonArrowData>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `SunMoonArrowData` | `SunMoonArrowData(double DeltaX, double DeltaY, double AzimuthDegrees, double AltitudeDegrees, bool IsVisible)` | Pure-data result describing a sun or moon direction arrow to be drawn on a map overlay. All geometry is expressed as pixel offsets from the pin center so the UI layer can position the arrow without knowing anything about the underlying astronomy. |
| `AltitudeDegrees` | `double AltitudeDegrees { get; init; }` |  |
| `AzimuthDegrees` | `double AzimuthDegrees { get; init; }` |  |
| `DeltaX` | `double DeltaX { get; init; }` |  |
| `DeltaY` | `double DeltaY { get; init; }` |  |
| `IsVisible` | `bool IsVisible { get; init; }` |  |

#### `Triangulation`

Computes camera heading and a derived "target" coordinate from two landmarks visible in a photo whose positions the user has pinned on a map. The idea: for a pinhole camera with horizontal field of view `horizontalFovDegrees`, a pixel column X in an image `imageWidth` pixels wide corresponds to a horizontal angle (relative to the camera's centre axis) of `(X / imageWidth - 0.5) × FOV`. If we know the real-world bearing from camera to a landmark (compute via `BearingDegrees`) and the landmark's in-image angle, the camera heading is the difference. Averaging two landmarks' solutions cancels out small pixel-picking errors. Full resection (unknown camera position) needs at least 3 points or an extra constraint; the methods here all require the camera GPS up front.

| Member | Signature | Summary |
| --- | --- | --- |
| `CameraHeadingFromTwoLandmarks` | `static double CameraHeadingFromTwoLandmarks(GpsCoordinate cameraGps, Landmark landmarkA, Landmark landmarkB, double imageWidth, double horizontalFovDegrees)` | Camera heading (0..360°, north = 0) given two landmarks visible in the photo and the camera's own GPS. Averages the two per-landmark solutions in a way that wraps correctly across the 0°/360° seam. |
| `HorizontalFovFromFocalLength35` | `static double HorizontalFovFromFocalLength35(double focalLengthMm)` | Derives a best-effort horizontal field of view from a 35-mm equivalent focal length. For a FF frame (36 mm wide): `2·atan(18/focal)`. |
| `PixelToHorizontalAngle` | `static double PixelToHorizontalAngle(double pixelX, double imageWidth, double horizontalFovDegrees)` | Converts a horizontal pixel position to an angle relative to the image centre. Negative angles are left of centre, positive right, in degrees. |
| `SinglePointHeading` | `static double SinglePointHeading(GpsCoordinate cameraGps, Landmark landmark, double imageWidth, double horizontalFovDegrees)` | Heading solution from a single landmark. Exposed so callers can inspect per-point results (e.g. to warn when the two solutions diverge wildly, which usually means a mis-picked pixel or landmark). |
| `TargetFromCameraAndHeading` | `static GpsCoordinate TargetFromCameraAndHeading(GpsCoordinate cameraGps, double headingDegrees, double distanceMeters = 100)` | A conceptual "target" point — where the camera was pointing, projected `distanceMeters` along the heading ray. Useful for populating `GPSDestLatitude`/`Longitude` when the user wants a recorded gaze point and doesn't care about the exact subject distance. |

#### `Triangulation.Landmark`

One landmark visible in the photo: where it really is (GPS) and where it appears in pixels (horizontal X).

Implements `IEquatable<Landmark>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Landmark` | `Landmark(GpsCoordinate Gps, double PixelX)` | One landmark visible in the photo: where it really is (GPS) and where it appears in pixels (horizontal X). |
| `Gps` | `GpsCoordinate Gps { get; init; }` |  |
| `PixelX` | `double PixelX { get; init; }` |  |

### Namespace `Hawkynt.PhotoManager.Core.Gpx`

[`GpxGeotagResult`](#gpxgeotagresult) · [`GpxGeotaggingService`](#gpxgeotaggingservice) · [`GpxParser`](#gpxparser) · [`GpxTimelineMatcher`](#gpxtimelinematcher) · [`GpxTrack`](#gpxtrack) · [`GpxTrackPoint`](#gpxtrackpoint) · [`PhotoTimestampReader`](#phototimestampreader)

#### `GpxGeotagResult`

Outcome per file for `ApplyToFilesAsync`.

Implements `IEquatable<GpxGeotagResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpxGeotagResult` | `GpxGeotagResult(FileInfo File, GpsCoordinate? Matched, DateTime? CameraLocalTime, string Reason = null)` | Outcome per file for `ApplyToFilesAsync`. |
| `CameraLocalTime` | `DateTime? CameraLocalTime { get; init; }` |  |
| `File` | `FileInfo File { get; init; }` |  |
| `Matched` | `GpsCoordinate? Matched { get; init; }` |  |
| `Reason` | `string Reason { get; init; }` |  |
| `Success` | `bool Success { get; }` |  |

#### `GpxGeotaggingService`

Applies GPX trackpoints to a batch of photos: reads each file's local camera time, adjusts by the user-supplied clock offset to get UTC, asks the `GpxTimelineMatcher` for the best-fit coordinate, and writes it through the injected `IMetadataWriter`. The clock offset is what GeoSetter calls the "time shift": camera clocks drift, and on trips you often discover your camera says 12:00 while the GPS logger says 12:07. `cameraOffsetFromUtc` bundles that drift in with the TZ offset: `cameraLocalTime - cameraOffsetFromUtc = trackUtcTime`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpxGeotaggingService` | `GpxGeotaggingService(IMetadataWriter writer)` |  |
| `ApplyToFilesAsync` | `Task<IReadOnlyList<GpxGeotagResult>> ApplyToFilesAsync(IReadOnlyList<FileInfo> files, GpxTrack track, TimeSpan cameraOffsetFromUtc, GpxTimelineMatcher matcher = null, bool overwriteExistingGps = false, IProgress<FileInfo> progress = null, IMetadataReader reader = null, CancellationToken cancellationToken = null)` |  |
| `CalibrateOffset` | `static TimeSpan? CalibrateOffset(FileInfo calibrationPhoto, DateTime knownUtc)` | Derive `cameraOffsetFromUtc` from a single calibration shot: the user picks a photo taken at a place whose GPS-timestamp is known (e.g. photographed the GPS receiver's display, or took the shot while the logger was at a specific known waypoint). Returns the offset such that `cameraLocal - offset = knownUtc`. |

#### `GpxParser`

Parses GPX 1.0 and 1.1 into a flat, time-ordered list of trackpoints. Handles both `<trkpt>` (tracks) and `<rtept>` (routes); ignores waypoints that have no timestamp since the whole workflow hinges on matching by time.

| Member | Signature | Summary |
| --- | --- | --- |
| `ParseFileAsync` | `static Task<GpxTrack> ParseFileAsync(FileInfo file, CancellationToken cancellationToken = null)` |  |
| `Parse` | `static GpxTrack Parse(string xml)` |  |

#### `GpxTimelineMatcher`

Given a photo's capture time, find the best-fitting GPS position in a `GpxTrack`. Two strategies: nearest-in-time (snap to the closest recorded point), or linear interpolation between the two straddling points when both sides are within `MaxToleranceSeconds`. Interpolation matches what GeoSetter does — more accurate when the track has sparse points (e.g. a logger set to 10-second intervals).

| Member | Signature | Summary |
| --- | --- | --- |
| `GpxTimelineMatcher` | `GpxTimelineMatcher()` |  |
| `Interpolate` | `bool Interpolate { get; init; }` | Interpolate between straddling points when both are within tolerance. |
| `MaxToleranceSeconds` | `int MaxToleranceSeconds { get; init; }` | Maximum time gap we'll accept before declining to geotag (default 60 s). |
| `Match` | `GpsCoordinate? Match(GpxTrack track, DateTime photoTimeUtc)` |  |

#### `GpxTrack`

A complete imported GPX track or set of tracks. Points are held flat in time order so timestamp-matching can binary-search.

Implements `IEquatable<GpxTrack>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpxTrack` | `GpxTrack(IReadOnlyList<GpxTrackPoint> Points)` | A complete imported GPX track or set of tracks. Points are held flat in time order so timestamp-matching can binary-search. |
| `EndUtc` | `DateTime? EndUtc { get; }` |  |
| `PointCount` | `int PointCount { get; }` |  |
| `Points` | `IReadOnlyList<GpxTrackPoint> Points { get; init; }` |  |
| `StartUtc` | `DateTime? StartUtc { get; }` |  |

#### `GpxTrackPoint`

A single point along a GPS track. `TimeUtc` is authoritative — GPX timestamps are always in UTC per the spec.

Implements `IEquatable<GpxTrackPoint>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpxTrackPoint` | `GpxTrackPoint(DateTime TimeUtc, GpsCoordinate Coordinate)` | A single point along a GPS track. `TimeUtc` is authoritative — GPX timestamps are always in UTC per the spec. |
| `Coordinate` | `GpsCoordinate Coordinate { get; init; }` |  |
| `TimeUtc` | `DateTime TimeUtc { get; init; }` |  |

#### `PhotoTimestampReader`

Reads the capture timestamp from a photo's EXIF data. Preference order: EXIF SubIFD DateTimeOriginal (when the shutter closed — the right signal for geotagging), then IFD0 DateTime (last-edited), then file mtime. Returns the timestamp as-if-local — EXIF DateTimeOriginal is written in the camera's local time with no timezone, so the caller has to apply an offset to convert it to UTC for GPX matching.

| Member | Signature | Summary |
| --- | --- | --- |
| `ReadLocalCameraTime` | `static DateTime? ReadLocalCameraTime(FileInfo file)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Hdr`

[`HdrBracketEntry`](#hdrbracketentry) · [`HdrMerger`](#hdrmerger) · [`HdrOptions`](#hdroptions) · [`HdrRadianceMap`](#hdrradiancemap) · [`ToneMapOperator`](#tonemapoperator) · [`Translation`](#translation)

#### `HdrBracketEntry`

Implements `IEquatable<HdrBracketEntry>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `HdrBracketEntry` | `HdrBracketEntry(FileInfo File, double ExposureSeconds)` |  |
| `ExposureSeconds` | `double ExposureSeconds { get; init; }` |  |
| `File` | `FileInfo File { get; init; }` |  |

#### `HdrMerger`

End-to-end HDR pipeline: per-file decode → optional MTB alignment → Debevec response recovery + radiance build → tone-mapped LDR. Files must be supplied in any order; the orchestrator sorts by exposure time (longest exposure last so the median bracket is used as alignment ref).

| Member | Signature | Summary |
| --- | --- | --- |
| `MergeAsync` | `static Task<HdrMergeResult> MergeAsync(IReadOnlyList<FileInfo> bracketFiles, HdrOptions options, CancellationToken cancellationToken = null)` |  |
| `MergeAsync` | `static Task<HdrMergeResult> MergeAsync(IReadOnlyList<HdrBracketEntry> entries, HdrOptions options, CancellationToken cancellationToken = null)` |  |
| `ReadExposureSeconds` | `static double? ReadExposureSeconds(FileInfo file)` | Pulls EXIF ExposureTime (preferred) or ShutterSpeedValue (fallback, APEX-encoded) from a JPEG/RAW. Returns null if neither is present so the caller can decide whether to default or surface the gap. |

#### `HdrOptions`

Implements `IEquatable<HdrOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `HdrOptions` | `HdrOptions(bool AlignBeforeMerge = true, ToneMapOperator Operator = 0, double WhitePoint = 0, double DragoBias = 0.85, double Saturation = 1, int? PreviewLongEdge = null)` |  |
| `AlignBeforeMerge` | `bool AlignBeforeMerge { get; init; }` |  |
| `DragoBias` | `double DragoBias { get; init; }` |  |
| `Operator` | `ToneMapOperator Operator { get; init; }` |  |
| `PreviewLongEdge` | `int? PreviewLongEdge { get; init; }` |  |
| `Saturation` | `double Saturation { get; init; }` |  |
| `WhitePoint` | `double WhitePoint { get; init; }` |  |

#### `HdrRadianceMap`

Per-channel camera response curves recovered from a bracket plus the per-pixel HDR radiance map (R, G, B planes, linear-light, log-exposure values returned in radiance directly via Exp).

Implements `IEquatable<HdrRadianceMap>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `HdrRadianceMap` | `HdrRadianceMap(int Width, int Height, float[] Red, float[] Green, float[] Blue, double[] ResponseRed, double[] ResponseGreen, double[] ResponseBlue)` | Per-channel camera response curves recovered from a bracket plus the per-pixel HDR radiance map (R, G, B planes, linear-light, log-exposure values returned in radiance directly via Exp). |
| `Blue` | `float[] Blue { get; init; }` |  |
| `Green` | `float[] Green { get; init; }` |  |
| `Height` | `int Height { get; init; }` |  |
| `Red` | `float[] Red { get; init; }` |  |
| `ResponseBlue` | `double[] ResponseBlue { get; init; }` |  |
| `ResponseGreen` | `double[] ResponseGreen { get; init; }` |  |
| `ResponseRed` | `double[] ResponseRed { get; init; }` |  |
| `Width` | `int Width { get; init; }` |  |

#### `ToneMapOperator`

| Value | Numeric | Summary |
| --- | --- | --- |
| `Reinhard` | `0` |  |
| `Drago` | `1` |  |

#### `Translation`

Pure (Dx, Dy) shift in pixels of one bracket frame relative to a reference frame. Positive Dx shifts the candidate to the right, positive Dy shifts it down (i.e. the candidate sits Dx px right and Dy px below where it should be).

Implements `IEquatable<Translation>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Translation` | `Translation(int Dx, int Dy)` | Pure (Dx, Dy) shift in pixels of one bracket frame relative to a reference frame. Positive Dx shifts the candidate to the right, positive Dy shifts it down (i.e. the candidate sits Dx px right and Dy px below where it should be). |
| `Dx` | `int Dx { get; init; }` |  |
| `Dy` | `int Dy { get; init; }` |  |

### Namespace `Hawkynt.PhotoManager.Core.Interfaces`

[`IDateTimeParser`](#idatetimeparser) · [`IFileOrganizer`](#ifileorganizer) · [`IImportManager`](#iimportmanager) · [`ISupportedFormatsService`](#isupportedformatsservice)

#### `IDateTimeParser`

| Member | Signature | Summary |
| --- | --- | --- |
| `GetSupportedDateFormats` | `string[] GetSupportedDateFormats()` |  |
| `ParseDateFromFileName` | `IAsyncEnumerable<DateTime> ParseDateFromFileName(FileToImport fileToImport, ImportSettings settings)` |  |

#### `IFileOrganizer`

| Member | Signature | Summary |
| --- | --- | --- |
| `AreFilesIdenticalAsync` | `Task<bool> AreFilesIdenticalAsync(FileInfo filePath1, FileInfo filePath2)` |  |
| `CopyFileAsync` | `Task<bool> CopyFileAsync(FileInfo sourcePath, FileInfo targetPath, bool overwrite = false)` |  |
| `GenerateTargetPath` | `Task<FileInfo> GenerateTargetPath(FileToImport file, DateTime dateTime, ImportSettings settings)` |  |
| `MoveFileAsync` | `Task<bool> MoveFileAsync(FileInfo sourcePath, FileInfo targetPath, bool overwrite = false)` |  |
| `ProcessFileAsync` | `Task<ValueTuple<FileOperationResult, FileInfo, string>> ProcessFileAsync(FileToImport file, DateTime dateTime, ImportSettings settings)` |  |

#### `IImportManager`

| Member | Signature | Summary |
| --- | --- | --- |
| `EnumerateDateTimes` | `IAsyncEnumerable<ValueTuple<DateTimeSource, DateTime>> EnumerateDateTimes(FileToImport fileToImport)` |  |
| `EnumerateDirectory` | `IAsyncEnumerable<FileToImport> EnumerateDirectory(DirectoryInfo root, bool recursive)` |  |
| `GetMostLogicalCreationDateAsync` | `Task<DateTime?> GetMostLogicalCreationDateAsync(FileToImport fileToImport)` |  |
| `GetMostLogicalCreationDateWithSourceAsync` | `Task<ValueTuple<DateTime?, DateTimeSource>> GetMostLogicalCreationDateWithSourceAsync(FileToImport fileToImport)` |  |
| `ProcessDirectoryAsync` | `Task<ImportResult> ProcessDirectoryAsync(ImportSettings settings, IProgress<ImportProgress> progress = null, CancellationToken cancellationToken = null)` |  |

#### `ISupportedFormatsService`

| Member | Signature | Summary |
| --- | --- | --- |
| `GetExtensionsByFormatAsync` | `Task<Dictionary<string, string[]>> GetExtensionsByFormatAsync()` | Gets supported extensions grouped by format type |
| `GetSupportedExtensionsAsync` | `Task<string[]> GetSupportedExtensionsAsync()` | Gets all supported file extensions for metadata extraction |
| `GetSupportedExtensionsWithoutWildcardsAsync` | `Task<string[]> GetSupportedExtensionsWithoutWildcardsAsync()` | Gets all supported file extensions for metadata extraction without wildcards |
| `IsExtensionSupported` | `bool IsExtensionSupported(string extension)` | Checks if a file extension is supported for metadata extraction |

### Namespace `Hawkynt.PhotoManager.Core.Library`

[`BurstGroup`](#burstgroup) · [`BurstGrouper`](#burstgrouper) · [`ColorLabelClause`](#colorlabelclause) · [`DateRangeClause`](#daterangeclause) · [`DuplicateFinder`](#duplicatefinder) · [`DuplicateGroup`](#duplicategroup) · [`GpsBoxClause`](#gpsboxclause) · [`HashedFile`](#hashedfile) · [`KeywordClause`](#keywordclause) · [`KeywordHierarchy`](#keywordhierarchy) · [`KeywordNode`](#keywordnode) · [`LibraryIndex`](#libraryindex) · [`LibrarySearchQuery`](#librarysearchquery) · [`LocationClause`](#locationclause) · [`LogicalOp`](#logicalop) · [`MemoriesFilter`](#memoriesfilter) · [`MemoriesFinder`](#memoriesfinder) · [`MemoryGroup`](#memorygroup) · [`MetadataCache`](#metadatacache) · [`MinRatingClause`](#minratingclause) · [`PerceptualHash`](#perceptualhash) · [`PerceptualHashCache`](#perceptualhashcache) · [`PersonClause`](#personclause) · [`PhotoDateShifter`](#photodateshifter) · [`PhotoDateShifter.ShiftPlan`](#photodateshiftershiftplan) · [`PickRejectFilterMode`](#pickrejectfiltermode) · [`PickStateClause`](#pickstateclause) · [`QualityFlagger`](#qualityflagger) · [`QualityResult`](#qualityresult) · [`QuickCollection`](#quickcollection) · [`RenameTokenExpander`](#renametokenexpander) · [`RuleClause`](#ruleclause) · [`SearchHit`](#searchhit) · [`SmartAlbumEvaluator`](#smartalbumevaluator) · [`SmartAlbumRule`](#smartalbumrule) · [`SmartAlbumRuleJson`](#smartalbumrulejson) · [`TimeWindow`](#timewindow) · [`TimelineBar`](#timelinebar) · [`TimelineGranularity`](#timelinegranularity) · [`TimelineHistogram`](#timelinehistogram)

#### `BurstGroup`

One detected burst — photos taken in rapid succession with similar filenames. `SuggestedName` is built from the shared prefix of the members' filenames plus the numeric range when possible (e.g. "IMG_1234..1240"), otherwise just the first member's stem.

Implements `IEquatable<BurstGroup>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `BurstGroup` | `BurstGroup(IReadOnlyList<FileInfo> Members, DateTime From, DateTime To, string SuggestedName)` | One detected burst — photos taken in rapid succession with similar filenames. `SuggestedName` is built from the shared prefix of the members' filenames plus the numeric range when possible (e.g. "IMG_1234..1240"), otherwise just the first member's stem. |
| `From` | `DateTime From { get; init; }` |  |
| `Members` | `IReadOnlyList<FileInfo> Members { get; init; }` |  |
| `SuggestedName` | `string SuggestedName { get; init; }` |  |
| `To` | `DateTime To { get; init; }` |  |

#### `BurstGrouper`

Pure logic for grouping photo bursts. Sorts entries by capture date, then walks them and starts a new burst whenever the gap to the previous shot exceeds `windowSize` OR the filename stem differs from the running burst's prefix by more than `filenameSimilarityThreshold` edit-stem characters. No IO, fully deterministic.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultFilenameSimilarityThreshold` | `const int DefaultFilenameSimilarityThreshold` |  |
| `DefaultWindowSize` | `static readonly TimeSpan DefaultWindowSize` |  |
| `GroupBursts` | `static IReadOnlyList<BurstGroup> GroupBursts(IEnumerable<ValueTuple<FileInfo, FullMetadata>> entries, TimeSpan? windowSize = null, int? filenameSimilarityThreshold = null)` |  |

#### `ColorLabelClause`

Inherits `RuleClause`. Implements `IEquatable<ColorLabelClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ColorLabelClause` | `ColorLabelClause(string Label)` |  |
| `Label` | `string Label { get; init; }` |  |

#### `DateRangeClause`

Inherits `RuleClause`. Implements `IEquatable<DateRangeClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DateRangeClause` | `DateRangeClause(DateTime? From, DateTime? To)` |  |
| `From` | `DateTime? From { get; init; }` |  |
| `To` | `DateTime? To { get; init; }` |  |

#### `DuplicateFinder`

Walks a directory, populates a `PerceptualHashCache`, and returns clusters of similar files using Hamming distance on the cached pHashes. O(N²) — fine for libraries up to ~10k images; rebuild instead of incrementing for larger collections.

| Member | Signature | Summary |
| --- | --- | --- |
| `DuplicateFinder` | `DuplicateFinder(PerceptualHashCache cache = null, ISupportedFormatsService formats = null)` |  |
| `DefaultThreshold` | `const int DefaultThreshold` |  |
| `Cache` | `PerceptualHashCache Cache { get; }` |  |
| `FindGroups` | `IReadOnlyList<DuplicateGroup> FindGroups(int threshold = 6)` | Group cached files whose pHashes are within `threshold` bits of each other. Greedy single-link clustering against the first unclaimed file in each iteration — same shape as the chained-diff dedup most desktop DAMs ship. |
| `FindGroups` | `static IReadOnlyList<DuplicateGroup> FindGroups(IReadOnlyList<HashedFile> files, int threshold)` | Pure-data grouping; takes a snapshot of (file, hash) pairs so callers holding their own list (e.g. tests) can drive the cluster step directly without going through a cache. |
| `ScanAsync` | `Task<int> ScanAsync(DirectoryInfo root, bool recursive = true, IProgress<FileInfo> progress = null, CancellationToken cancellationToken = null)` | Walk `root`, hash each supported image, and report progress so long scans don't look hung. Skips unreadable files silently. Returns the number of files actually hashed. |

#### `DuplicateGroup`

A group of near-duplicate files sharing pHashes within the requested Hamming threshold. The first item is the "anchor" — every other item's distance is measured against it.

Implements `IEquatable<DuplicateGroup>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DuplicateGroup` | `DuplicateGroup(IReadOnlyList<HashedFile> Files, IReadOnlyList<int> Distances)` | A group of near-duplicate files sharing pHashes within the requested Hamming threshold. The first item is the "anchor" — every other item's distance is measured against it. |
| `Count` | `int Count { get; }` |  |
| `Distances` | `IReadOnlyList<int> Distances { get; init; }` |  |
| `Files` | `IReadOnlyList<HashedFile> Files { get; init; }` |  |

#### `GpsBoxClause`

Inherits `RuleClause`. Implements `IEquatable<GpsBoxClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpsBoxClause` | `GpsBoxClause(double MinLat, double MaxLat, double MinLon, double MaxLon)` |  |
| `MaxLat` | `double MaxLat { get; init; }` |  |
| `MaxLon` | `double MaxLon { get; init; }` |  |
| `MinLat` | `double MinLat { get; init; }` |  |
| `MinLon` | `double MinLon { get; init; }` |  |

#### `HashedFile`

One file plus its 64-bit pHash; what the duplicate finder buckets together.

Implements `IEquatable<HashedFile>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `HashedFile` | `HashedFile(FileInfo File, ulong Hash)` | One file plus its 64-bit pHash; what the duplicate finder buckets together. |
| `File` | `FileInfo File { get; init; }` |  |
| `Hash` | `ulong Hash { get; init; }` |  |

#### `KeywordClause`

Inherits `RuleClause`. Implements `IEquatable<KeywordClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `KeywordClause` | `KeywordClause(string Keyword, bool CaseInsensitive = true)` |  |
| `CaseInsensitive` | `bool CaseInsensitive { get; init; }` |  |
| `Keyword` | `string Keyword { get; init; }` |  |

#### `KeywordHierarchy`

Pure logic for the hierarchical keyword tree. The internal model lets the user organise tags as `Travel > Italy > Rome`, but on the photo only flat `dc:subject` values are written so any reader picks them up — selecting "Rome" implicitly also writes "Italy" and "Travel". Lookup is case-insensitive; selections that don't appear in the tree are passed through unchanged so users can still add ad-hoc keywords without touching the tree.

| Member | Signature | Summary |
| --- | --- | --- |
| `Expand` | `static IReadOnlyList<string> Expand(IReadOnlyList<KeywordNode> roots, IEnumerable<string> selectedLeafNames)` | Expand a set of selected leaf names into the full keyword set that should be written to the photo: the selections themselves, plus every ancestor of every match found in the tree, with duplicates removed (case-insensitive) but preserving the first-seen casing for each name. Names that don't exist in the tree are kept as-is. |
| `FindPaths` | `static IReadOnlyList<IReadOnlyList<string>> FindPaths(IReadOnlyList<KeywordNode> roots, string name)` | Return the full path (root → … → match) for every match. Used by the UI to render hierarchical keyword display ("Travel/Italy/Rome"). |
| `Find` | `static IEnumerable<KeywordNode> Find(IReadOnlyList<KeywordNode> roots, string name)` | Find every node whose `Name` matches `name` (case-insensitive). The same name may appear in several places in the tree — all matches are returned. |

#### `KeywordNode`

One node in the user's hierarchical keyword tree. Children is mutable so the UI can drag-and-drop / add / remove subtrees in place; the tree itself is persisted as JSON in `UserSettingsData.KeywordTreeRoots`. On write to a photo's XMP a selected leaf flattens to its full ancestor chain so other tools see standard `dc:subject` keywords.

Implements `IEquatable<KeywordNode>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `KeywordNode` | `KeywordNode()` |  |
| `KeywordNode` | `KeywordNode(string name)` |  |
| `KeywordNode` | `KeywordNode(string name, IEnumerable<KeywordNode> children)` |  |
| `Children` | `List<KeywordNode> Children { get; set; }` |  |
| `Name` | `string Name { get; set; }` |  |

#### `LibraryIndex`

Walks a directory, reads metadata via `MetadataCache`, and serves searches from memory. Rebuilt on demand via `ScanAsync`; re-running the scan is cheap because the cache skips unchanged files by `LastWriteTimeUtc`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LibraryIndex` | `LibraryIndex(MetadataCache cache, ISupportedFormatsService formats = null)` |  |
| `Cache` | `MetadataCache Cache { get; }` |  |
| `DistinctKeywords` | `IReadOnlyList<string> DistinctKeywords()` | Every unique keyword across the cached library. |
| `DistinctLocations` | `IReadOnlyList<string> DistinctLocations()` | Every unique location/city/state/country across the cached library. |
| `DistinctPeople` | `IReadOnlyList<string> DistinctPeople()` | Every unique person name (from Person regions with a non-blank label) across the cached library. Handy for driving a "Who's in my photos" dropdown in the search UI. |
| `ScanAsync` | `Task<int> ScanAsync(DirectoryInfo root, bool recursive = true, IProgress<FileInfo> progress = null, CancellationToken cancellationToken = null)` | Populates the cache by walking `root`. Reports the file currently being processed via `progress` so long scans don't look hung. Returns the number of files visited. |
| `Search` | `IReadOnlyList<SearchHit> Search(LibrarySearchQuery query)` | Run the query against the cached library. Sync because the data is all in memory; the caller decides whether to dispatch on a background thread. |

#### `LibrarySearchQuery`

What the user is searching for. Each field that's non-null contributes an AND clause; fields that are null are ignored. Keyword/person/location do substring matches case-insensitively; `AnyText` searches across title, caption, keywords, person names, and all location fields — the fallback when the user doesn't care where the term lives.

Implements `IEquatable<LibrarySearchQuery>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LibrarySearchQuery` | `LibrarySearchQuery(string AnyText = null, string Keyword = null, string Person = null, string Location = null, int? MinRating = null, string ColorLabel = null)` | What the user is searching for. Each field that's non-null contributes an AND clause; fields that are null are ignored. Keyword/person/location do substring matches case-insensitively; `AnyText` searches across title, caption, keywords, person names, and all location fields — the fallback when the user doesn't care where the term lives. |
| `AnyText` | `string AnyText { get; init; }` |  |
| `ColorLabel` | `string ColorLabel { get; init; }` |  |
| `IsEmpty` | `bool IsEmpty { get; }` |  |
| `Keyword` | `string Keyword { get; init; }` |  |
| `Location` | `string Location { get; init; }` |  |
| `MinRating` | `int? MinRating { get; init; }` |  |
| `Person` | `string Person { get; init; }` |  |

#### `LocationClause`

Inherits `RuleClause`. Implements `IEquatable<LocationClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `LocationClause` | `LocationClause(string CityOrCountry)` |  |
| `CityOrCountry` | `string CityOrCountry { get; init; }` |  |

#### `LogicalOp`

How clauses are combined. Currently only `And` is honoured by the evaluator — `Or` is reserved for a future stretch where nested groups can be expressed.

| Value | Numeric | Summary |
| --- | --- | --- |
| `And` | `0` |  |
| `Or` | `1` |  |

#### `MemoriesFilter`

Pure-logic predicates powering the Memories window: photos taken on the same calendar day in past years ("On this day") and photos within a distance / time radius of an anchor photo ("On this trip"). The UI layer renders the results — these helpers just say which files qualify.

| Member | Signature | Summary |
| --- | --- | --- |
| `OnThisDay` | `static IEnumerable<ValueTuple<FileInfo, FullMetadata>> OnThisDay(IEnumerable<ValueTuple<FileInfo, FullMetadata>> photos, DateTime today)` | Photos whose `DateCreated` matches today's month + day in any year other than the anchor's year. Items without a capture date are skipped. |
| `OnThisTrip` | `static IEnumerable<ValueTuple<FileInfo, FullMetadata>> OnThisTrip(IEnumerable<ValueTuple<FileInfo, FullMetadata>> photos, GpsCoordinate anchor, DateTime anchorTime, double radiusKm = 5, TimeSpan? window = null)` | Photos within `radiusKm` kilometres of `anchor` AND captured within ±`window` of `anchorTime`. Photos missing GPS or capture date are skipped. The anchor itself is excluded (zero distance + zero time delta = same photo). |

#### `MemoriesFinder`

Pure query engine that surfaces "memories" — photos taken on the same calendar date (or week/month/year/decade) in past years. No side effects, no IO, fully deterministic given its inputs.

| Member | Signature | Summary |
| --- | --- | --- |
| `Filter` | `static IReadOnlyList<MemoryGroup> Filter(IReadOnlyList<MemoryGroup> groups, TimeWindow window)` | Filter groups to only those matching the specified time window. |
| `Find` | `static IReadOnlyList<MemoryGroup> Find(IEnumerable<ValueTuple<DateTime, FileInfo>> allPhotos, DateTime referenceDate)` | Find memory groups from a flat list of (capturedDate, file) pairs, evaluated against `referenceDate` (typically today). Returns groups sorted by recency (most recent matching year first), then by time-window granularity (day before week before month...). Empty groups are omitted. |

#### `MemoryGroup`

A group of photos sharing the same time-window match against a reference date. For example: "On this day, 3 years ago" or "This month, 5 years ago".

| Member | Signature | Summary |
| --- | --- | --- |
| `MemoryGroup` | `MemoryGroup()` |  |
| `Header` | `string Header { get; }` | Human-readable header for display. |
| `MatchDate` | `DateOnly MatchDate { get; init; }` |  |
| `Photos` | `IReadOnlyList<FileInfo> Photos { get; init; }` |  |
| `TimeWindow` | `TimeWindow TimeWindow { get; init; }` |  |
| `YearsAgo` | `int YearsAgo { get; init; }` |  |

#### `MetadataCache`

In-memory cache of `FullMetadata` keyed by absolute file path. Validity is tied to the file's `LastWriteTimeUtc` — if the file on disk has changed since the entry was stored we re-read. Consistent with the no-local-DB principle: memory only, rebuilt on demand, no persistence. Threadsafe so a background index scan and a foreground search can share the same cache without locking at the call site.

| Member | Signature | Summary |
| --- | --- | --- |
| `MetadataCache` | `MetadataCache(IMetadataReader reader = null)` |  |
| `Count` | `int Count { get; }` |  |
| `Clear` | `void Clear()` | Wipe the cache entirely. |
| `GetAsync` | `Task<FullMetadata> GetAsync(FileInfo file, CancellationToken cancellationToken = null)` | Returns the cached metadata for `file`, re-reading if the file's mtime has changed since the cached entry was stored. Missing files return an empty `FullMetadata` rather than throwing. |
| `Invalidate` | `void Invalidate(FileInfo file)` | Forget a specific file's entry — used after a write so the next lookup re-reads. |
| `Snapshot` | `IEnumerable<ValueTuple<FileInfo, FullMetadata>> Snapshot()` | Every currently cached (file, metadata) pair. Snapshots the keys to avoid mutation-while-iterating. |

#### `MinRatingClause`

Inherits `RuleClause`. Implements `IEquatable<MinRatingClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MinRatingClause` | `MinRatingClause(int MinStars)` |  |
| `MinStars` | `int MinStars { get; init; }` |  |

#### `PerceptualHash`

Classic 64-bit pHash. The pipeline is: resize to 32×32 grayscale, run a 2D DCT-II on the luminance matrix, take the top-left 8×8 low-frequency block (skipping the DC term when computing the median so a constant offset doesn't bias the output), then emit one bit per coefficient — 1 if the coefficient is at least the median, else 0. Two hashes are similar iff their Hamming distance (popcount of XOR) is small. Robust to JPEG re-encoding, mild scaling, and small color/exposure shifts; insensitive to rotation by 90°+ (which is by design — that's a different transform). 64 bits → up to ~6-bit Hamming distance is "the same photo" in practice.

| Member | Signature | Summary |
| --- | --- | --- |
| `ComputeAsync` | `static Task<ulong> ComputeAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `HammingDistance` | `static int HammingDistance(ulong a, ulong b)` | Hamming distance between two 64-bit pHashes — number of bits that differ. |

#### `PerceptualHashCache`

In-memory cache of 64-bit perceptual hashes keyed by absolute file path. Validity is tied to the file's `LastWriteTimeUtc` — if the file on disk has changed since the entry was stored we recompute. Mirrors the no-local-DB principle of `MetadataCache`: memory only, rebuilt on demand, no persistence.

| Member | Signature | Summary |
| --- | --- | --- |
| `PerceptualHashCache` | `PerceptualHashCache()` |  |
| `Count` | `int Count { get; }` |  |
| `Clear` | `void Clear()` |  |
| `GetAsync` | `Task<ulong> GetAsync(FileInfo file, CancellationToken cancellationToken = null)` | Returns the cached pHash for `file`, recomputing if the file's mtime has changed since the cached entry was stored. Throws on missing files — the duplicate finder filters those out before calling so it's a real error if it ever bubbles up. |
| `Invalidate` | `void Invalidate(FileInfo file)` |  |
| `Snapshot` | `IEnumerable<ValueTuple<FileInfo, ulong>> Snapshot()` | Every currently cached (file, hash) pair. |

#### `PersonClause`

Inherits `RuleClause`. Implements `IEquatable<PersonClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PersonClause` | `PersonClause(string Person)` |  |
| `Person` | `string Person { get; init; }` |  |

#### `PhotoDateShifter`

Shifts EXIF capture timestamps by a fixed offset — the workflow you reach for when the camera clock was set wrong (DST, timezone, drift). Reads `DateTimeOriginal` via `PhotoTimestampReader`, applies the offset, and writes `DateTimeOriginal` + `DateTimeDigitized` + IFD0 `DateTime` back through `ApplyExifPatch`. Pixel data is not touched and the filesystem mtime/ctime/atime are preserved by `AtomicMetadataWrite`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ApplyAsync` | `static Task<bool> ApplyAsync(FileInfo file, TimeSpan offset, CancellationToken cancellationToken = null)` | Apply `offset` to `file`'s EXIF dates. Returns true on success, false when the container isn't a JPEG / has no readable date / the rewrite fails. |
| `Plan` | `static IReadOnlyList<ShiftPlan> Plan(IEnumerable<FileInfo> files, TimeSpan offset)` | Build a per-file plan without touching disk — useful for driving a preview list before the user clicks Apply. |
| `SupportsContainer` | `static bool SupportsContainer(FileInfo file)` |  |

#### `PhotoDateShifter.ShiftPlan`

Implements `IEquatable<ShiftPlan>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ShiftPlan` | `ShiftPlan(FileInfo File, DateTime? Current, DateTime? Shifted)` |  |
| `Current` | `DateTime? Current { get; init; }` |  |
| `File` | `FileInfo File { get; init; }` |  |
| `HasChange` | `bool HasChange { get; }` |  |
| `Shifted` | `DateTime? Shifted { get; init; }` |  |

#### `PickRejectFilterMode`

Pick / reject filter as used by Lightroom-style flags. Maps onto the XMP rating convention where -1 means rejected; 0+ means picked-or-unset.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Any` | `0` | Don't filter on pick state (anything passes). |
| `Picked` | `1` | Only files with rating >= 0 (i.e. not rejected). |
| `Rejected` | `2` | Only files with rating == -1. |
| `Unflagged` | `3` | Only unrated files (rating is null or 0). |

#### `PickStateClause`

Inherits `RuleClause`. Implements `IEquatable<PickStateClause>`, `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PickStateClause` | `PickStateClause(PickRejectFilterMode Mode)` |  |
| `Mode` | `PickRejectFilterMode Mode { get; init; }` |  |

#### `QualityFlagger`

| Member | Signature | Summary |
| --- | --- | --- |
| `QualityFlagger` | `QualityFlagger()` |  |
| `AnalysisLongEdgePixels` | `const int AnalysisLongEdgePixels` |  |
| `BlurVarianceThreshold` | `const double BlurVarianceThreshold` | Laplacian-variance threshold — below this value the image is flagged as blurry. |
| `ExposureClipThreshold` | `const double ExposureClipThreshold` | Fraction-of-pixels threshold for the over/under-exposure flags. |
| `HighlightClipValue` | `const byte HighlightClipValue` | Pixel-channel value at or above which a pixel is considered a clipped highlight. |
| `ShadowClipValue` | `const byte ShadowClipValue` | Pixel-channel value at or below which a pixel is considered a crushed shadow. |
| `AnalyseAsync` | `Task<QualityResult> AnalyseAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `Analyse` | `QualityResult Analyse(FileInfo file)` | Synchronous, FileInfo-only entry point per the public contract. |

#### `QualityResult`

Aggregated quality result for a single image. Each boolean is what the UI surfaces directly; the raw scores are kept for tooltips / sorting / future thresholds without a re-analysis pass.

Implements `IEquatable<QualityResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `QualityResult` | `QualityResult(bool IsBlurry, bool IsOverexposed, bool IsUnderexposed, double SharpnessScore, double ClippedHighlightFraction, double ClippedShadowFraction)` | Aggregated quality result for a single image. Each boolean is what the UI surfaces directly; the raw scores are kept for tooltips / sorting / future thresholds without a re-analysis pass. |
| `ClippedHighlightFraction` | `double ClippedHighlightFraction { get; init; }` |  |
| `ClippedShadowFraction` | `double ClippedShadowFraction { get; init; }` |  |
| `IsBlurry` | `bool IsBlurry { get; init; }` |  |
| `IsOverexposed` | `bool IsOverexposed { get; init; }` |  |
| `IsUnderexposed` | `bool IsUnderexposed { get; init; }` |  |
| `SharpnessScore` | `double SharpnessScore { get; init; }` |  |

#### `QuickCollection`

Lightroom-style "Quick Collection" — a per-session unsorted bucket of file paths the user is curating. Backed by a `HashSet` of absolute file paths so membership survives across grid refreshes. Per-session only — no persistence to disk; clears when the app closes.

| Member | Signature | Summary |
| --- | --- | --- |
| `QuickCollection` | `QuickCollection()` |  |
| `Count` | `int Count { get; }` | Number of files currently in the quick collection. |
| `Add` | `void Add(FileInfo file)` | Add a file to the collection. Idempotent — duplicate adds are no-ops. |
| `Clear` | `void Clear()` | Remove all files from the collection. |
| `Contains` | `bool Contains(FileInfo file)` | Check whether the file is in the collection. |
| `Contains` | `bool Contains(string fullPath)` | Check whether the absolute path is in the collection. |
| `GetFiles` | `IReadOnlySet<string> GetFiles()` | Return a snapshot of the current file paths. |
| `Remove` | `void Remove(FileInfo file)` | Remove a file from the collection. No-op if the file is not a member. |
| `Toggle` | `bool Toggle(FileInfo file)` | Toggle the file's membership. Returns `true` if the file was added, `false` if it was removed. |

#### `RenameTokenExpander`

Renders a metadata-aware filename template (no path component — output is a filename within the same directory). Supports brace-delimited tokens with an optional `:format` suffix. Unknown tokens pass through verbatim so the user can spot typos in the preview. Token values are sanitised — path separators and reserved characters in metadata strings are replaced with `_` so a city like "Mont-Saint-Michel" stays intact but a stray slash can't accidentally create subdirectories. Supported tokens (case-insensitive name): `{name}` — current filename without extension`{ext}` — current extension without leading dot`{date:fmt}` — capture date (default `yyyy-MM-dd`)`{time:fmt}` — capture time (default `HHmmss`)`{year} / {month} / {day} / {hour} / {minute} / {second}` — capture-date components`{rating}` — XMP star rating (0–5, or empty)`{label}` — XMP color label`{city} / {country} / {countrycode} / {state} / {location}` — place names`{title} / {caption} / {creator}` — text fields`{index:fmt}` — sequential counter (1-based, default `0`)`{total:fmt}` — total file count

| Member | Signature | Summary |
| --- | --- | --- |
| `Expand` | `static string Expand(string template, FileInfo file, FullMetadata metadata, DateTime? captureDate, int index, int totalCount)` | Expand `template` to a sanitised filename. The `captureDate` is the photo's local capture timestamp (typically from `PhotoTimestampReader`); pass null when missing — date tokens then expand to empty strings. |
| `SanitizeForFilename` | `static string SanitizeForFilename(string raw)` | Replace characters illegal in filenames (path separators, reserved chars on Windows) with `_`. Trims surrounding whitespace. |

#### `RuleClause`

One clause inside a `SmartAlbumRule`. Sealed records by concrete type — JSON discriminator is the type name (see `SmartAlbumRuleJson` for round-trip details).

Implements `IEquatable<RuleClause>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `RuleClause` | `protected RuleClause()` |  |

#### `SearchHit`

One hit from a library search.

Implements `IEquatable<SearchHit>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `SearchHit` | `SearchHit(FileInfo File, FullMetadata Metadata)` | One hit from a library search. |
| `File` | `FileInfo File { get; init; }` |  |
| `Metadata` | `FullMetadata Metadata { get; init; }` |  |

#### `SmartAlbumEvaluator`

Pure-logic engine for smart-album rules. No IO; every input arrives via the snapshot enumerable from `Snapshot`.

| Member | Signature | Summary |
| --- | --- | --- |
| `Evaluate` | `static IReadOnlyList<ValueTuple<FileInfo, FullMetadata>> Evaluate(IEnumerable<ValueTuple<FileInfo, FullMetadata>> source, SmartAlbumRule rule)` | Filter the supplied (file, metadata) snapshot against `rule`. An empty rule matches everything; clauses are AND-combined when `LogicOp` is `And`, OR-combined otherwise. Order is preserved; the caller decides sorting. |
| `Matches` | `static bool Matches(FullMetadata metadata, IReadOnlyList<RuleClause> clauses, LogicalOp op = 0)` | True when `metadata` satisfies the supplied clauses under the chosen logic. |

#### `SmartAlbumRule`

A named, multi-clause rule. Evaluated by `SmartAlbumEvaluator`; persisted as JSON inside the user settings so a saved smart album survives across sessions. `LogicOp` defaults to `And`; nested OR groups are a future enhancement.

Implements `IEquatable<SmartAlbumRule>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `SmartAlbumRule` | `SmartAlbumRule()` |  |
| `Clauses` | `RuleClause[] Clauses { get; init; }` |  |
| `LogicOp` | `LogicalOp LogicOp { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |

#### `SmartAlbumRuleJson`

Convenience helpers for the polymorphic JSON round-trip. Lives on the rule itself so callers don't have to wire `JsonSerializerOptions` for every read/write.

| Member | Signature | Summary |
| --- | --- | --- |
| `Options` | `static readonly JsonSerializerOptions Options` |  |
| `Deserialize` | `static SmartAlbumRule Deserialize(string json)` |  |
| `Serialize` | `static string Serialize(SmartAlbumRule rule)` |  |

#### `TimeWindow`

Time window granularity for a memory group.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Day` | `0` |  |
| `Week` | `1` |  |
| `Month` | `2` |  |
| `Year` | `3` |  |
| `Decade` | `4` |  |

#### `TimelineBar`

One bar on the timeline scrubber. `BucketStart` is the first instant of the bucket (inclusive); `Count` is how many photos fall in [BucketStart, next bucket start).

Implements `IEquatable<TimelineBar>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `TimelineBar` | `TimelineBar(DateTime BucketStart, int Count, TimelineGranularity Granularity)` | One bar on the timeline scrubber. `BucketStart` is the first instant of the bucket (inclusive); `Count` is how many photos fall in [BucketStart, next bucket start). |
| `BucketStart` | `DateTime BucketStart { get; init; }` |  |
| `Count` | `int Count { get; init; }` |  |
| `Granularity` | `TimelineGranularity Granularity { get; init; }` |  |

#### `TimelineGranularity`

Granularity of a timeline histogram bucket. Auto-picked from the span of the input dates: tight spans get a daily breakdown, wider spans roll up to weeks or months so the bar count stays manageable.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Day` | `0` |  |
| `Week` | `1` |  |
| `Month` | `2` |  |

#### `TimelineHistogram`

Pure-logic helper that rolls a list of photo timestamps into bucketed counts for the timeline scrubber control. Granularity auto-scales: short libraries get daily detail, long ones get monthly bars so the strip doesn't explode into thousands of pixels per year.

| Member | Signature | Summary |
| --- | --- | --- |
| `Build` | `static IReadOnlyList<TimelineBar> Build(IEnumerable<ValueTuple<DateTime, FileInfo>> photos, TimelineGranularity? forcedGranularity = null)` | Build a histogram from photo timestamps. Granularity is auto-picked from the data span: < 1 year = Day, < 5 years = Week, otherwise Month. Buckets without photos are still emitted (count 0) so the scrubber renders a continuous strip with visible gaps. |
| `PickGranularity` | `static TimelineGranularity PickGranularity(DateTime first, DateTime last)` | Auto-pick a granularity. < 1 year span = Day, < 5 years = Week, otherwise Month. Picked so the resulting bar count stays in the hundreds — too few looks sparse, too many overflows the scrubber. |

### Namespace `Hawkynt.PhotoManager.Core.Metadata`

[`CompositeMetadataWriter`](#compositemetadatawriter) · [`DirectionReference`](#directionreference) · [`FaceRegion`](#faceregion) · [`FileSignatureDetector`](#filesignaturedetector) · [`FileSignatureDetector.DetectionResult`](#filesignaturedetectordetectionresult) · [`FullMetadata`](#fullmetadata) · [`GpsCoordinate`](#gpscoordinate) · [`IMetadataReader`](#imetadatareader) · [`IMetadataWriter`](#imetadatawriter) · [`ImageDirection`](#imagedirection) · [`MetadataEdit`](#metadataedit) · [`MetadataEditDto`](#metadataeditdto) · [`MetadataReader`](#metadatareader) · [`Optional<T>`](#optionalt) · [`SidecarPath`](#sidecarpath) · [`WriteBackItem`](#writebackitem) · [`WriteBackQueue`](#writebackqueue) · [`XmpSidecarFormatter`](#xmpsidecarformatter) · [`XmpSidecarWriter`](#xmpsidecarwriter)

#### `CompositeMetadataWriter`

Routes metadata writes to the best target for each file: 1. If a sidecar already exists, keep writing the sidecar (respects workflows from Lightroom / darktable / earlier PhotoManager sessions). 2. Otherwise, if the format supports in-file XMP (JPEG today), embed the XMP directly in the image so the metadata travels with the file. 3. Fall back to writing a sidecar — the old behaviour, correct for RAW files where in-place modification would be destructive. This keeps the user-facing contract ("my edits don't get lost, and editable photos have metadata in the file") while never touching a RAW file's bytes.

Implements `IMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `CompositeMetadataWriter` | `CompositeMetadataWriter()` |  |
| `CompositeMetadataWriter` | `CompositeMetadataWriter(params IContainerMetadataWriter[] containerWriters)` |  |
| `ApplyAsync` | `Task<FileInfo> ApplyAsync(FileInfo imageFile, MetadataEdit edit, CancellationToken cancellationToken = null)` |  |

#### `DirectionReference`

| Value | Numeric | Summary |
| --- | --- | --- |
| `True` | `0` |  |
| `Magnetic` | `1` |  |

#### `FaceRegion`

A face detected or tagged in an image, stored in the sidecar via the MWG-RS (Metadata Working Group Regions) schema — the same schema Lightroom and digiKam use. `Box` is normalized to the image (0..1 center-based per MWG spec). `PersonName` is null when the face is not yet attributed to a person.

Implements `IEquatable<FaceRegion>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FaceRegion` | `FaceRegion(NormalizedBoundingBox Box, string PersonName = null)` | A face detected or tagged in an image, stored in the sidecar via the MWG-RS (Metadata Working Group Regions) schema — the same schema Lightroom and digiKam use. `Box` is normalized to the image (0..1 center-based per MWG spec). `PersonName` is null when the face is not yet attributed to a person. |
| `Box` | `NormalizedBoundingBox Box { get; init; }` |  |
| `PersonName` | `string PersonName { get; init; }` |  |

#### `FileSignatureDetector`

Detect a file's actual format from its leading bytes (magic number) and compare to its declared extension. Used to flag obvious mislabels — e.g. PNG bytes living in `.jpg` after a sloppy export. Read-only: never renames anything itself; surfaces the suggested extension and lets the caller decide.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectAsync` | `static Task<DetectionResult> DetectAsync(FileInfo file, CancellationToken cancellationToken = null)` |  |

#### `FileSignatureDetector.DetectionResult`

Implements `IEquatable<DetectionResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DetectionResult` | `DetectionResult(string DetectedExtension, bool MatchesDeclared, string SuggestedRenameExtension)` |  |
| `DetectedExtension` | `string DetectedExtension { get; init; }` |  |
| `MatchesDeclared` | `bool MatchesDeclared { get; init; }` |  |
| `SuggestedRenameExtension` | `string SuggestedRenameExtension { get; init; }` |  |

#### `FullMetadata`

A fully-resolved snapshot of a file's metadata: EXIF values plus any XMP sidecar overrides merged on top. Sidecar values win for fields the user has edited; EXIF fills in the rest. All fields are nullable so callers can tell "not present" apart from "empty".

Implements `IEquatable<FullMetadata>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FullMetadata` | `FullMetadata()` |  |
| `AltTextAccessibility` | `string AltTextAccessibility { get; init; }` | Alternative text for assistive technology — `Iptc4xmpCore:AltTextAccessibility`. |
| `ArtworkCopyright` | `string ArtworkCopyright { get; init; }` |  |
| `ArtworkCreator` | `string ArtworkCreator { get; init; }` |  |
| `ArtworkDateCreated` | `string ArtworkDateCreated { get; init; }` |  |
| `ArtworkSource` | `string ArtworkSource { get; init; }` |  |
| `ArtworkTitle` | `string ArtworkTitle { get; init; }` |  |
| `Caption` | `string Caption { get; init; }` |  |
| `City` | `string City { get; init; }` |  |
| `ColorLabel` | `string ColorLabel { get; init; }` | Adobe-style color label: "Red", "Yellow", "Green", "Blue", "Purple", or user-defined. |
| `CopyrightIptc` | `string CopyrightIptc { get; init; }` | IPTC 2:116 Copyright Notice value as read from the file. |
| `CopyrightOwnerId` | `string CopyrightOwnerId { get; init; }` |  |
| `CopyrightOwnerName` | `string CopyrightOwnerName { get; init; }` |  |
| `CopyrightXmp` | `string CopyrightXmp { get; init; }` | XMP `dc:rights` value as read from the file. |
| `Copyright` | `string Copyright { get; init; }` | Copyright notice — unified "best available" value preferring XMP over IPTC. For per-source viewing/editing see `CopyrightXmp` and `CopyrightIptc`. |
| `CountryCode` | `string CountryCode { get; init; }` | ISO 3166-1 alpha-2, e.g. "DE", "US". |
| `Country` | `string Country { get; init; }` |  |
| `CreatorContactAddress` | `string CreatorContactAddress { get; init; }` |  |
| `CreatorContactCity` | `string CreatorContactCity { get; init; }` |  |
| `CreatorContactCountry` | `string CreatorContactCountry { get; init; }` |  |
| `CreatorContactEmail` | `string CreatorContactEmail { get; init; }` |  |
| `CreatorContactPhone` | `string CreatorContactPhone { get; init; }` |  |
| `CreatorContactPostalCode` | `string CreatorContactPostalCode { get; init; }` |  |
| `CreatorContactState` | `string CreatorContactState { get; init; }` |  |
| `CreatorContactWebsite` | `string CreatorContactWebsite { get; init; }` |  |
| `CreatorIptc` | `string CreatorIptc { get; init; }` | IPTC 2:80 By-line value as read from the file. |
| `CreatorJobTitle` | `string CreatorJobTitle { get; init; }` |  |
| `CreatorXmp` | `string CreatorXmp { get; init; }` | XMP `dc:creator` value as read from the file — may differ from `CreatorIptc` when another tool wrote only one container. |
| `Creator` | `string Creator { get; init; }` | Person or organisation who made the image — unified "best available" value preferring XMP over IPTC. For per-source viewing/editing see `CreatorXmp` and `CreatorIptc`. |
| `Credit` | `string Credit { get; init; }` | Who to credit when publishing — XMP `photoshop:Credit`, IPTC 2:110. |
| `DataMining` | `string DataMining { get; init; }` | Data-mining permission URI (PLUS vocabulary) — `Iptcext:DataMining`. |
| `DateCreated` | `DateTime? DateCreated { get; init; }` | Original capture date/time (separate from file mtime) — XMP `photoshop:DateCreated`, IPTC 2:55/2:60. |
| `DescriptionWriter` | `string DescriptionWriter { get; init; }` | Who wrote the description/caption — `Iptc4xmpCore:DescriptionWriter`, IPTC 2:122. |
| `DigitalSourceType` | `string DigitalSourceType { get; init; }` | Digital source type URI (AI-generated disclosure) — `Iptcext:DigitalSourceType`. |
| `EventId` | `string EventId { get; init; }` | Unique identifier for the event — `Iptcext:EventId`. |
| `Event` | `string Event { get; init; }` | Event associated with the image — `Iptcext:Event`. |
| `ExtendedDescriptionAccessibility` | `string ExtendedDescriptionAccessibility { get; init; }` | Longer description for accessibility — `Iptc4xmpCore:ExtDescrAccessibility`. |
| `Faces` | `IReadOnlyList<FaceRegion> Faces { get; }` | Face regions — convenience view over `Regions` filtered to accepted Person entries. Interoperable with Lightroom/digiKam MWG-RS face tagging. |
| `Genre` | `string Genre { get; init; }` | Genre classification URI — `Iptcext:Genre`. |
| `Gps` | `GpsCoordinate? Gps { get; init; }` |  |
| `Headline` | `string Headline { get; init; }` | Short headline summarising the image — XMP `photoshop:Headline`, IPTC 2:105. |
| `ImageDirection` | `ImageDirection? ImageDirection { get; init; }` | Direction the camera was pointing (compass heading 0..360°). |
| `ImageSupplierId` | `string ImageSupplierId { get; init; }` |  |
| `ImageSupplierName` | `string ImageSupplierName { get; init; }` |  |
| `Instructions` | `string Instructions { get; init; }` | Special instructions for the image — XMP `photoshop:Instructions`, IPTC 2:40. |
| `IptcImageRating` | `double? IptcImageRating { get; init; }` | IPTC's own image rating (separate from the XMP `Rating`) — `Iptcext:ImageRating`. |
| `IsPick` | `bool? IsPick { get; init; }` | Picasa-style cull flag: separate from `Rating` so a user can triage with P/X (Pick / Reject) without nuking a deliberate star rating. Maps to xmp:Pick = "true". Null = not flagged. |
| `IsReject` | `bool? IsReject { get; init; }` | Picasa-style cull flag: distinct from `IsPick` so a user can triage with P/X without overloading XMP `xmp:Label` (which we use for the Adobe color label). Maps to xmp:Reject = "true". Null = not flagged. |
| `JobIdentifier` | `string JobIdentifier { get; init; }` | Job / transmission reference — `Iptc4xmpCore:TransmissionReference`, IPTC 2:103. |
| `Keywords` | `IReadOnlyList<string> Keywords { get; init; }` |  |
| `LicensorId` | `string LicensorId { get; init; }` |  |
| `LicensorName` | `string LicensorName { get; init; }` | Licensor (first entry, if multiple) — `Iptcext:Licensor`. |
| `LocationCreatedId` | `string LocationCreatedId { get; init; }` | Identifier (e.g. Wikidata URL) for the Location Created — `Iptcext:LocationCreated/Iptcext:LocationId`. |
| `LocationShownCity` | `string LocationShownCity { get; init; }` |  |
| `LocationShownCountryCode` | `string LocationShownCountryCode { get; init; }` |  |
| `LocationShownCountry` | `string LocationShownCountry { get; init; }` |  |
| `LocationShownGps` | `GpsCoordinate? LocationShownGps { get; init; }` |  |
| `LocationShownId` | `string LocationShownId { get; init; }` |  |
| `LocationShownState` | `string LocationShownState { get; init; }` |  |
| `LocationShownSublocation` | `string LocationShownSublocation { get; init; }` | Sublocation visible/depicted in the image — `Iptcext:LocationShown/Iptcext:Sublocation`. |
| `LocationShownWorldRegion` | `string LocationShownWorldRegion { get; init; }` |  |
| `Location` | `string Location { get; init; }` | Free-form sublocation (neighborhood, venue, road) — Iptc4xmpCore:Location. |
| `ModelReleaseId` | `string ModelReleaseId { get; init; }` |  |
| `ModelReleaseStatus` | `string ModelReleaseStatus { get; init; }` | Model release status URI (PLUS vocabulary) — `Iptcext:ModelReleaseStatus`. |
| `PersonsShown` | `IReadOnlyList<string> PersonsShown { get; init; }` | Names of recognizable people shown in the image — `Iptcext:PersonInImage`. |
| `ProductDescription` | `string ProductDescription { get; init; }` |  |
| `ProductGtin` | `string ProductGtin { get; init; }` |  |
| `ProductName` | `string ProductName { get; init; }` |  |
| `PropertyReleaseId` | `string PropertyReleaseId { get; init; }` |  |
| `PropertyReleaseStatus` | `string PropertyReleaseStatus { get; init; }` |  |
| `Rating` | `int? Rating { get; init; }` | 0–5 (5 best), -1 means rejected per XMP convention. |
| `Regions` | `IReadOnlyList<TaggedRegion> Regions { get; init; }` | All tagged regions — people, animals, items, places — in any status. Proposed regions come from detectors awaiting user review; Accepted regions are confirmed and contribute labels to keywords. |
| `RightsUsage` | `string RightsUsage { get; init; }` | Rights usage terms — XMP `xmpRights:UsageTerms`. |
| `Source` | `string Source { get; init; }` | Original source of the image — XMP `photoshop:Source`, IPTC 2:115. |
| `State` | `string State { get; init; }` |  |
| `SupplierImageId` | `string SupplierImageId { get; init; }` |  |
| `TargetGps` | `GpsCoordinate? TargetGps { get; init; }` | GPS location of the photographed subject — distinct from `Gps` which is the camera's position. Maps to exif:GPSDestLatitude/Longitude. |
| `Title` | `string Title { get; init; }` |  |
| `WebStatementOfRights` | `string WebStatementOfRights { get; init; }` | URL pointing to rights statement — `xmpRights:WebStatement`. |
| `WorldRegionCreated` | `string WorldRegionCreated { get; init; }` | World region of the Location Created — `Iptcext:LocationCreated/Iptcext:WorldRegion`. |

#### `GpsCoordinate`

A geographic point in decimal degrees. Latitude is positive north of the equator; longitude is positive east of the prime meridian. Altitude is in meters above (positive) or below (negative) mean sea level.

Implements `IEquatable<GpsCoordinate>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `GpsCoordinate` | `GpsCoordinate(double Latitude, double Longitude, double? AltitudeMeters = null)` | A geographic point in decimal degrees. Latitude is positive north of the equator; longitude is positive east of the prime meridian. Altitude is in meters above (positive) or below (negative) mean sea level. |
| `AltitudeMeters` | `double? AltitudeMeters { get; init; }` |  |
| `IsValid` | `bool IsValid { get; }` |  |
| `Latitude` | `double Latitude { get; init; }` |  |
| `Longitude` | `double Longitude { get; init; }` |  |
| `LatitudeAsXmpString` | `string LatitudeAsXmpString()` | Formats latitude as the XMP-style `<degrees>,<minutes>N\|S` string. E.g. 37.8054° → "37,48.324N". Matches the format exiftool produces for XMP sidecars. |
| `LongitudeAsXmpString` | `string LongitudeAsXmpString()` | Formats longitude as the XMP-style `<degrees>,<minutes>E\|W` string. |
| `TryParseXmpLatitude` | `static bool TryParseXmpLatitude(string value, out double degrees)` |  |
| `TryParseXmpLongitude` | `static bool TryParseXmpLongitude(string value, out double degrees)` |  |

#### `IMetadataReader`

| Member | Signature | Summary |
| --- | --- | --- |
| `ReadAsync` | `Task<FullMetadata> ReadAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` | Returns the full metadata snapshot for `imageFile`: EXIF values first, then XMP sidecar values overlaid on top. Returns an empty `FullMetadata` for files without readable metadata (and never throws for missing sidecars). |

#### `IMetadataWriter`

| Member | Signature | Summary |
| --- | --- | --- |
| `ApplyAsync` | `Task<FileInfo> ApplyAsync(FileInfo imageFile, MetadataEdit edit, CancellationToken cancellationToken = null)` | Applies the `edit` patch to `imageFile`'s sidecar. Reads the existing sidecar (if any), overlays the patch, and writes the result back. Fields not mentioned in the patch are preserved. Returns the `FileInfo` of the sidecar that was written. |

#### `ImageDirection`

Direction the camera was pointing when the photo was taken. `Degrees` is in the 0..360 range (0 = north, clockwise). `Reference` distinguishes true-north ("T") from magnetic-north ("M"); GPS devices without a compass usually omit the reference, in which case callers can treat it as true-north.

Implements `IEquatable<ImageDirection>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImageDirection` | `ImageDirection(double Degrees, DirectionReference Reference = 0)` | Direction the camera was pointing when the photo was taken. `Degrees` is in the 0..360 range (0 = north, clockwise). `Reference` distinguishes true-north ("T") from magnetic-north ("M"); GPS devices without a compass usually omit the reference, in which case callers can treat it as true-north. |
| `Degrees` | `double Degrees { get; init; }` |  |
| `IsValid` | `bool IsValid { get; }` |  |
| `ReferenceTag` | `string ReferenceTag { get; }` |  |
| `Reference` | `DirectionReference Reference { get; init; }` |  |
| `ParseReference` | `static DirectionReference ParseReference(string tag)` |  |

#### `MetadataEdit`

An immutable patch to apply to a file's sidecar metadata. Each field has three states: null means "leave this field alone", a present value means "write this value", and `Clear` values mean "delete this field from the sidecar". Callers build a patch like `new MetadataEdit { Gps = new(...), Rating = 5 }` and the writer merges it on top of the existing sidecar.

Implements `IEquatable<MetadataEdit>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MetadataEdit` | `MetadataEdit()` |  |
| `AltTextAccessibility` | `Optional<string> AltTextAccessibility { get; init; }` |  |
| `ArtworkCopyright` | `Optional<string> ArtworkCopyright { get; init; }` |  |
| `ArtworkCreator` | `Optional<string> ArtworkCreator { get; init; }` |  |
| `ArtworkDateCreated` | `Optional<string> ArtworkDateCreated { get; init; }` |  |
| `ArtworkSource` | `Optional<string> ArtworkSource { get; init; }` |  |
| `ArtworkTitle` | `Optional<string> ArtworkTitle { get; init; }` |  |
| `Caption` | `Optional<string> Caption { get; init; }` |  |
| `City` | `Optional<string> City { get; init; }` |  |
| `ColorLabel` | `Optional<string> ColorLabel { get; init; }` |  |
| `CopyrightIptc` | `Optional<string> CopyrightIptc { get; init; }` | Per-source override for IPTC 2:116 Copyright Notice. See `CreatorXmp` for the semantic. |
| `CopyrightOwnerId` | `Optional<string> CopyrightOwnerId { get; init; }` |  |
| `CopyrightOwnerName` | `Optional<string> CopyrightOwnerName { get; init; }` |  |
| `CopyrightXmp` | `Optional<string> CopyrightXmp { get; init; }` | Per-source override for XMP `dc:rights`. See `CreatorXmp` for the semantic. |
| `Copyright` | `Optional<string> Copyright { get; init; }` |  |
| `CountryCode` | `Optional<string> CountryCode { get; init; }` |  |
| `Country` | `Optional<string> Country { get; init; }` |  |
| `CreatorContactAddress` | `Optional<string> CreatorContactAddress { get; init; }` |  |
| `CreatorContactCity` | `Optional<string> CreatorContactCity { get; init; }` |  |
| `CreatorContactCountry` | `Optional<string> CreatorContactCountry { get; init; }` |  |
| `CreatorContactEmail` | `Optional<string> CreatorContactEmail { get; init; }` |  |
| `CreatorContactPhone` | `Optional<string> CreatorContactPhone { get; init; }` |  |
| `CreatorContactPostalCode` | `Optional<string> CreatorContactPostalCode { get; init; }` |  |
| `CreatorContactState` | `Optional<string> CreatorContactState { get; init; }` |  |
| `CreatorContactWebsite` | `Optional<string> CreatorContactWebsite { get; init; }` |  |
| `CreatorIptc` | `Optional<string> CreatorIptc { get; init; }` | Per-source override for IPTC 2:80 By-line. See `CreatorXmp` for the semantic. |
| `CreatorJobTitle` | `Optional<string> CreatorJobTitle { get; init; }` |  |
| `CreatorXmp` | `Optional<string> CreatorXmp { get; init; }` | Per-source override for the XMP `dc:creator` value. When set, it wins over `Creator` for the XMP write path only — the IPTC By-line still tracks `Creator` (or its own per-source override) so callers can intentionally diverge the two containers. |
| `Creator` | `Optional<string> Creator { get; init; }` |  |
| `Credit` | `Optional<string> Credit { get; init; }` |  |
| `DataMining` | `Optional<string> DataMining { get; init; }` |  |
| `DateCreated` | `Optional<DateTime?> DateCreated { get; init; }` |  |
| `DescriptionWriter` | `Optional<string> DescriptionWriter { get; init; }` |  |
| `DigitalSourceType` | `Optional<string> DigitalSourceType { get; init; }` |  |
| `EventId` | `Optional<string> EventId { get; init; }` |  |
| `Event` | `Optional<string> Event { get; init; }` |  |
| `ExtendedDescriptionAccessibility` | `Optional<string> ExtendedDescriptionAccessibility { get; init; }` |  |
| `Genre` | `Optional<string> Genre { get; init; }` |  |
| `Gps` | `Optional<GpsCoordinate?> Gps { get; init; }` |  |
| `Headline` | `Optional<string> Headline { get; init; }` |  |
| `ImageDirection` | `Optional<ImageDirection?> ImageDirection { get; init; }` |  |
| `ImageSupplierId` | `Optional<string> ImageSupplierId { get; init; }` |  |
| `ImageSupplierName` | `Optional<string> ImageSupplierName { get; init; }` |  |
| `Instructions` | `Optional<string> Instructions { get; init; }` |  |
| `IptcImageRating` | `Optional<double?> IptcImageRating { get; init; }` |  |
| `IsPick` | `Optional<bool?> IsPick { get; init; }` |  |
| `IsReject` | `Optional<bool?> IsReject { get; init; }` |  |
| `JobIdentifier` | `Optional<string> JobIdentifier { get; init; }` |  |
| `Keywords` | `Optional<IReadOnlyList<string>> Keywords { get; init; }` |  |
| `LicensorId` | `Optional<string> LicensorId { get; init; }` |  |
| `LicensorName` | `Optional<string> LicensorName { get; init; }` |  |
| `LocationCreatedId` | `Optional<string> LocationCreatedId { get; init; }` |  |
| `LocationShownCity` | `Optional<string> LocationShownCity { get; init; }` |  |
| `LocationShownCountryCode` | `Optional<string> LocationShownCountryCode { get; init; }` |  |
| `LocationShownCountry` | `Optional<string> LocationShownCountry { get; init; }` |  |
| `LocationShownGps` | `Optional<GpsCoordinate?> LocationShownGps { get; init; }` |  |
| `LocationShownId` | `Optional<string> LocationShownId { get; init; }` |  |
| `LocationShownState` | `Optional<string> LocationShownState { get; init; }` |  |
| `LocationShownSublocation` | `Optional<string> LocationShownSublocation { get; init; }` |  |
| `LocationShownWorldRegion` | `Optional<string> LocationShownWorldRegion { get; init; }` |  |
| `Location` | `Optional<string> Location { get; init; }` |  |
| `ModelReleaseId` | `Optional<string> ModelReleaseId { get; init; }` |  |
| `ModelReleaseStatus` | `Optional<string> ModelReleaseStatus { get; init; }` |  |
| `PersonsShown` | `Optional<IReadOnlyList<string>> PersonsShown { get; init; }` |  |
| `ProductDescription` | `Optional<string> ProductDescription { get; init; }` |  |
| `ProductGtin` | `Optional<string> ProductGtin { get; init; }` |  |
| `ProductName` | `Optional<string> ProductName { get; init; }` |  |
| `PropertyReleaseId` | `Optional<string> PropertyReleaseId { get; init; }` |  |
| `PropertyReleaseStatus` | `Optional<string> PropertyReleaseStatus { get; init; }` |  |
| `Rating` | `Optional<int?> Rating { get; init; }` |  |
| `Regions` | `Optional<IReadOnlyList<TaggedRegion>> Regions { get; init; }` |  |
| `RightsUsage` | `Optional<string> RightsUsage { get; init; }` |  |
| `Source` | `Optional<string> Source { get; init; }` |  |
| `State` | `Optional<string> State { get; init; }` |  |
| `SupplierImageId` | `Optional<string> SupplierImageId { get; init; }` |  |
| `TargetGps` | `Optional<GpsCoordinate?> TargetGps { get; init; }` |  |
| `Title` | `Optional<string> Title { get; init; }` |  |
| `WebStatementOfRights` | `Optional<string> WebStatementOfRights { get; init; }` |  |
| `WorldRegionCreated` | `Optional<string> WorldRegionCreated { get; init; }` |  |

#### `MetadataEditDto`

Serializable DTO that captures the subset of `MetadataEdit` fields that carry values. The queue persists this instead of the full `MetadataEdit` because `Optional` uses a private constructor that `JsonSerializer` can't roundtrip without a custom converter. This DTO only stores fields that were actually set, sidestepping the three-state problem entirely.

Implements `IEquatable<MetadataEditDto>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MetadataEditDto` | `MetadataEditDto()` |  |
| `Caption` | `string Caption { get; init; }` |  |
| `City` | `string City { get; init; }` |  |
| `ColorLabel` | `string ColorLabel { get; init; }` |  |
| `Copyright` | `string Copyright { get; init; }` |  |
| `CountryCode` | `string CountryCode { get; init; }` |  |
| `Country` | `string Country { get; init; }` |  |
| `Creator` | `string Creator { get; init; }` |  |
| `DateCreated` | `DateTime? DateCreated { get; init; }` |  |
| `GpsAltitude` | `double? GpsAltitude { get; init; }` |  |
| `GpsLatitude` | `double? GpsLatitude { get; init; }` |  |
| `GpsLongitude` | `double? GpsLongitude { get; init; }` |  |
| `HasCaption` | `bool HasCaption { get; init; }` |  |
| `HasCity` | `bool HasCity { get; init; }` |  |
| `HasColorLabel` | `bool HasColorLabel { get; init; }` |  |
| `HasCopyright` | `bool HasCopyright { get; init; }` |  |
| `HasCountryCode` | `bool HasCountryCode { get; init; }` |  |
| `HasCountry` | `bool HasCountry { get; init; }` |  |
| `HasCreator` | `bool HasCreator { get; init; }` |  |
| `HasDateCreated` | `bool HasDateCreated { get; init; }` |  |
| `HasGps` | `bool HasGps { get; init; }` |  |
| `HasIsPick` | `bool HasIsPick { get; init; }` |  |
| `HasIsReject` | `bool HasIsReject { get; init; }` |  |
| `HasKeywords` | `bool HasKeywords { get; init; }` |  |
| `HasLocation` | `bool HasLocation { get; init; }` |  |
| `HasRating` | `bool HasRating { get; init; }` |  |
| `HasState` | `bool HasState { get; init; }` |  |
| `HasTitle` | `bool HasTitle { get; init; }` |  |
| `IsPick` | `bool? IsPick { get; init; }` |  |
| `IsReject` | `bool? IsReject { get; init; }` |  |
| `Keywords` | `List<string> Keywords { get; init; }` |  |
| `Location` | `string Location { get; init; }` |  |
| `Rating` | `int? Rating { get; init; }` |  |
| `State` | `string State { get; init; }` |  |
| `Title` | `string Title { get; init; }` |  |
| `FromEdit` | `static MetadataEditDto FromEdit(MetadataEdit edit)` | Snapshots a `MetadataEdit` into this DTO, capturing only set fields. |
| `ToMetadataEdit` | `MetadataEdit ToMetadataEdit()` | Converts this DTO back to a live `MetadataEdit`. |

#### `MetadataReader`

Implements `IMetadataReader`.

| Member | Signature | Summary |
| --- | --- | --- |
| `MetadataReader` | `MetadataReader()` |  |
| `ReadAsync` | `Task<FullMetadata> ReadAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

#### `Optional<T>`

Three-state optional: not set (leave field alone), set to null (clear field), or set to a value (write value). Used instead of plain nullable so callers can express "delete this field" distinctly from "don't touch this field".

Implements `IEquatable<Optional<T>>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `HasValue` | `bool HasValue { get; }` |  |
| `Value` | `T Value { get; }` |  |
| `Set` | `static Optional<T> Set(T value)` |  |
| `implicit operator Optional<T>` | `static implicit operator Optional<T>(T value)` |  |

#### `SidecarPath`

Resolves the sidecar file location for a given image. We use the darktable convention: `photo.jpg.xmp` rather than Adobe's `photo.xmp`. This keeps sidecars unique when RAW + JPG pairs live in the same directory.

| Member | Signature | Summary |
| --- | --- | --- |
| `Suffix` | `const string Suffix` |  |
| `For` | `static FileInfo For(FileInfo imageFile)` |  |

#### `WriteBackItem`

A single pending metadata write: the file to patch, the edit to apply, and bookkeeping for retry logic. Serialized to disk so writes survive crashes.

Implements `IEquatable<WriteBackItem>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `WriteBackItem` | `WriteBackItem()` |  |
| `Description` | `string Description { get; init; }` | Optional human-readable description shown in the UI queue list. Not used by the write logic itself. |
| `Edit` | `MetadataEditDto Edit { get; init; }` |  |
| `EnqueuedUtc` | `DateTime EnqueuedUtc { get; init; }` |  |
| `FilePath` | `string FilePath { get; init; }` |  |
| `RetryCount` | `int RetryCount { get; init; }` |  |

#### `WriteBackQueue`

Crash-safe retry queue for metadata writes. Pending items are persisted to JSON on disk so they survive process termination. A single background consumer drains the channel, retrying with exponential backoff on transient I/O failures and moving permanently-failed items to a separate list.

Implements `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `WriteBackQueue` | `WriteBackQueue(IMetadataWriter writer, DirectoryInfo storeDir = null, Func<int, TimeSpan> backoffProvider = null)` | Creates a new queue backed by the given directory. If the directory contains a `pending.json` from a previous session, those items are loaded and re-queued automatically. |
| `MaxRetries` | `const int MaxRetries` |  |
| `DiscardFailed` | `void DiscardFailed(WriteBackItem item)` | Permanently removes a failed item. The write is abandoned. |
| `Dispose` | `void Dispose()` |  |
| `Enqueue` | `void Enqueue(FileInfo imageFile, MetadataEdit edit, string description = null)` | Convenience overload: wraps a file + edit into a `WriteBackItem` and enqueues it. |
| `Enqueue` | `void Enqueue(WriteBackItem item)` | Adds a metadata write to the queue. Persists immediately. |
| `GetFailed` | `IReadOnlyList<WriteBackItem> GetFailed()` |  |
| `GetPending` | `IReadOnlyList<WriteBackItem> GetPending()` |  |
| `RetryFailed` | `void RetryFailed(WriteBackItem item)` | Moves a failed item back into the pending queue for another round of retries. |
| `Start` | `void Start()` | Starts the background consumer. Call once after construction. |
| `QueueChanged` | `event Action QueueChanged` | Raised on the thread-pool whenever the pending or failed lists change. |

#### `XmpSidecarFormatter`

Serializes and parses XMP sidecar files (RDF/XML). The minimal schema this formatter handles covers GPS, rating, color label, keywords, title and caption — the fields the app exposes for editing. Unknown elements are preserved verbatim on round-trip so interop with other tools (Lightroom, darktable, exiftool) doesn't lose data the user wrote elsewhere.

| Member | Signature | Summary |
| --- | --- | --- |
| `Parse` | `static ValueTuple<FullMetadata, XDocument> Parse(string xml)` | Parses a sidecar document and returns both the decoded state and the original `XDocument` so callers can pass it back into `Serialize` to preserve foreign fields. |
| `Serialize` | `static string Serialize(FullMetadata state, XDocument preserveSourceDocument = null)` | Renders a `FullMetadata` snapshot to an XMP sidecar document. If `preserveSourceDocument` is supplied, unknown elements from it are preserved on the output so foreign fields survive round-trips. |

#### `XmpSidecarWriter`

Implements `IMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `XmpSidecarWriter` | `XmpSidecarWriter()` |  |
| `ApplyAsync` | `Task<FileInfo> ApplyAsync(FileInfo imageFile, MetadataEdit edit, CancellationToken cancellationToken = null)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Metadata.Containers`

[`ContainerWriteResult`](#containerwriteresult) · [`IContainerMetadataWriter`](#icontainermetadatawriter) · [`JpegContainerExifBridge`](#jpegcontainerexifbridge) · [`JpegContainerMetadataWriter`](#jpegcontainermetadatawriter) · [`PngContainerMetadataWriter`](#pngcontainermetadatawriter) · [`TiffContainerMetadataWriter`](#tiffcontainermetadatawriter) · [`WebpContainerMetadataWriter`](#webpcontainermetadatawriter)

#### `ContainerWriteResult`

| Value | Numeric | Summary |
| --- | --- | --- |
| `NotSupported` | `0` | The format isn't handled by this writer. |
| `Written` | `1` | The XMP payload was successfully embedded. |
| `Failed` | `2` | The format is supported but the particular file is malformed. |

#### `IContainerMetadataWriter`

Strategy for writing metadata directly inside an image file (as opposed to into an `.xmp` sidecar). Implementations return `NotSupported` when asked about a format they can't handle — callers use this to fall through to the sidecar writer.

| Member | Signature | Summary |
| --- | --- | --- |
| `SupportsExtension` | `bool SupportsExtension(string extension)` |  |
| `WriteXmpAsync` | `Task<ContainerWriteResult> WriteXmpAsync(FileInfo imageFile, byte[] xmpBytes, CancellationToken cancellationToken = null)` | Writes `xmpBytes` into `imageFile` without re-encoding the image. Only the XMP metadata segment is touched; pixel data is byte-for-byte preserved. |

#### `JpegContainerExifBridge`

Combines EXIF + XMP + IPTC updates into a single JPEG rewrite. All three metadata segments are replaced/inserted in one pass so the file is only written once. Used by `CompositeMetadataWriter` for JPEGs where we want native EXIF + legacy IPTC (which many tools read instead of XMP) kept in sync with our canonical XMP.

| Member | Signature | Summary |
| --- | --- | --- |
| `WriteAsync` | `static Task<bool> WriteAsync(FileInfo imageFile, byte[] xmpBytes, ExifPatch exifPatch, IptcFields iptc = null, CancellationToken cancellationToken = null)` |  |

#### `JpegContainerMetadataWriter`

Implements `IContainerMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `JpegContainerMetadataWriter` | `JpegContainerMetadataWriter()` |  |
| `SupportsExtension` | `bool SupportsExtension(string extension)` |  |
| `WriteXmpAsync` | `Task<ContainerWriteResult> WriteXmpAsync(FileInfo imageFile, byte[] xmpBytes, CancellationToken cancellationToken = null)` |  |

#### `PngContainerMetadataWriter`

Embeds our XMP packet into a PNG's iTXt chunk so metadata travels with the file without a sidecar. PNG has no EXIF IFD — XMP is the only in-file metadata route — so there's no EXIF-bridge equivalent here.

Implements `IContainerMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PngContainerMetadataWriter` | `PngContainerMetadataWriter()` |  |
| `SupportsExtension` | `bool SupportsExtension(string extension)` |  |
| `WriteXmpAsync` | `Task<ContainerWriteResult> WriteXmpAsync(FileInfo imageFile, byte[] xmpBytes, CancellationToken cancellationToken = null)` |  |

#### `TiffContainerMetadataWriter`

Implements `IContainerMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `TiffContainerMetadataWriter` | `TiffContainerMetadataWriter()` |  |
| `SupportsExtension` | `bool SupportsExtension(string extension)` |  |
| `WriteXmpAsync` | `Task<ContainerWriteResult> WriteXmpAsync(FileInfo imageFile, byte[] xmpBytes, CancellationToken cancellationToken = null)` |  |

#### `WebpContainerMetadataWriter`

Implements `IContainerMetadataWriter`.

| Member | Signature | Summary |
| --- | --- | --- |
| `WebpContainerMetadataWriter` | `WebpContainerMetadataWriter()` |  |
| `SupportsExtension` | `bool SupportsExtension(string extension)` |  |
| `WriteXmpAsync` | `Task<ContainerWriteResult> WriteXmpAsync(FileInfo imageFile, byte[] xmpBytes, CancellationToken cancellationToken = null)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Ml`

[`AutoKeywordTagger`](#autokeywordtagger) · [`AutoKeywordVocabulary`](#autokeywordvocabulary) · [`ImageEmbeddingCache`](#imageembeddingcache) · [`OnnxClipImageEncoder.NormalizationMode`](#onnxclipimageencodernormalizationmode) · [`OnnxClipTextEncoder`](#onnxcliptextencoder) · [`OnnxClipTextEncoder.ITokenizer`](#onnxcliptextencoderitokenizer) · [`OnnxClipTextEncoder.WhitespaceByteTokenizer`](#onnxcliptextencoderwhitespacebytetokenizer)

#### `AutoKeywordTagger`

Pure-logic cosine-similarity ranker. Given an image embedding and a set of vocabulary embeddings, returns the top-K vocabulary words by similarity, filtered by a minimum-similarity threshold. All embeddings are assumed to be L2-normalised by the caller (the ONNX encoders normalise their output). With unit vectors, cosine similarity reduces to a plain dot product.

| Member | Signature | Summary |
| --- | --- | --- |
| `L2Normalize` | `static float[] L2Normalize(float[] vector)` | L2-normalises `vector` in place-style (returns a fresh array). |
| `Tag` | `static IReadOnlyList<ValueTuple<string, float>> Tag(float[] imageEmbedding, IReadOnlyList<float[]> vocabEmbeddings, IReadOnlyList<string> words, int topK = 5, float minSimilarity = 0.22)` | Ranks `words` by cosine similarity between `imageEmbedding` and each `vocabEmbeddings`. Returns the top `topK` matches whose score is at or above `minSimilarity`, in descending similarity order. |
| `TryReadCache` | `static IReadOnlyList<float[]> TryReadCache(FileInfo source, int expectedCount)` | Reads a cache written by `WriteCache`. Returns null if the file is missing, malformed, or has a row count that disagrees with `expectedCount`. |
| `WriteCache` | `static void WriteCache(FileInfo target, IReadOnlyList<float[]> embeddings)` | Writes a vocabulary-embedding cache: 4-byte magic, int32 count, int32 dim, float32 matrix in row-major order. Returns true on success. |

#### `AutoKeywordVocabulary`

Default vocabulary loaded from the embedded `AutoKeywordVocabulary.txt` resource. The list is organised in source-control as one word per line so we don't ship a giant compile-time string literal — the .txt is generated at scaffold time and embedded via `<EmbeddedResource>`. Buckets covered: subjects, places, scenes, weather/light, seasons/time, objects (incl. colour×object cross-terms), activities and aesthetics. Total is intentionally kept under ~800 words so vocabulary embedding stays fast on first run and stable for cache invalidation.

| Member | Signature | Summary |
| --- | --- | --- |
| `Default` | `static IReadOnlyList<string> Default { get; }` |  |
| `ComputeHash` | `static string ComputeHash(IReadOnlyList<string> words)` | Stable hash of the vocabulary contents — used as part of the cache file name so a vocabulary change invalidates the cached embeddings. |
| `LoadFromFile` | `static IReadOnlyList<string> LoadFromFile(FileInfo file)` | Reads `file` one word per line, skipping blank lines and lines starting with `#` so users can drop comments into a custom vocabulary. Whitespace is trimmed and the result is lower-cased. |

#### `ImageEmbeddingCache`

Disk-based cache for per-image CLIP/SigLIP vision embeddings. When users re-scan the same folder (e.g. after adjusting top-K or min-similarity) the expensive ONNX vision inference is skipped for images that haven't changed. Cache directory: `AppDataPaths.SubDirectory("ml-cache")` Key: SHA-256 of (file size as 8 LE bytes + first 64 KB of file content). This is fast — only a small prefix is read — and stable unless the file is re-saved. Cache file naming: `img-{hashHex}.emb` containing the raw float32 embedding vector (no header, just `dim * 4` bytes).

| Member | Signature | Summary |
| --- | --- | --- |
| `ImageEmbeddingCache` | `ImageEmbeddingCache(DirectoryInfo cacheDir = null)` |  |
| `Put` | `void Put(FileInfo file, float[] embedding)` | Stores `embedding` to disk for `file`. Failures are silently swallowed — the cache is a performance optimisation, not a correctness requirement. |
| `TryGet` | `float[] TryGet(FileInfo file)` | Returns a cached embedding for `file`, or null when no cache entry exists or the file can't be read. |

#### `OnnxClipImageEncoder.NormalizationMode`

| Value | Numeric | Summary |
| --- | --- | --- |
| `SigLIP` | `0` |  |
| `ImageNet` | `1` |  |

#### `OnnxClipTextEncoder`

CLIP / SigLIP text encoder. Encodes vocabulary words once and caches the resulting matrix on disk under `AppDataPaths.SubDirectory("ml-cache")/vocab-{model-hash}-{vocab-hash}.bin` so subsequent runs skip re-embedding. Production CLIP / SigLIP text encoders expect a tokenizer-specific input shape (BPE for CLIP, SentencePiece for SigLIP). To keep this slice dependency-light, the encoder exposes `ITokenizer` as a hook so callers can plug in the right tokenizer for their chosen model. The built-in `WhitespaceByteTokenizer` is a placeholder that's good enough to round-trip cache logic in tests, but a real model needs a real tokenizer to produce useful text embeddings.

Implements `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `OnnxClipTextEncoder` | `OnnxClipTextEncoder(FileInfo modelFile = null, ITokenizer tokenizer = null, int maxTokens = 64)` |  |
| `DefaultMaxTokens` | `const int DefaultMaxTokens` |  |
| `DefaultModelFileName` | `const string DefaultModelFileName` |  |
| `IsAvailable` | `bool IsAvailable { get; }` |  |
| `Dispose` | `void Dispose()` |  |
| `EmbedAllCached` | `IReadOnlyList<float[]> EmbedAllCached(IReadOnlyList<string> words)` | Embeds `words` with a disk cache. The cache key combines the model file's hash and the vocabulary hash so any change invalidates. |
| `EmbedAll` | `IReadOnlyList<float[]> EmbedAll(IReadOnlyList<string> words)` | Embeds every word in `words`, returning one L2-normalised vector per word. Returns null when the model isn't loaded. |
| `ResolveCacheFile` | `FileInfo ResolveCacheFile(IReadOnlyList<string> words)` |  |

#### `OnnxClipTextEncoder.ITokenizer`

| Member | Signature | Summary |
| --- | --- | --- |
| `Identifier` | `string Identifier { get; }` | Stable identifier — included in the cache filename. |
| `Encode` | `long[] Encode(string text, int maxTokens)` | Returns int64 token ids padded / truncated to `maxTokens`. |

#### `OnnxClipTextEncoder.WhitespaceByteTokenizer`

Placeholder tokenizer: lower-cases, splits on whitespace, encodes each resulting token as the sum of its UTF-8 bytes mod 32000 (a typical vocabulary size). This is NOT a real BPE / SentencePiece tokenizer; it exists so the encoder is wireable end-to-end without binding to a specific tokenizer library. Callers running a real CLIP / SigLIP model must inject a matching tokenizer.

Implements `ITokenizer`.

| Member | Signature | Summary |
| --- | --- | --- |
| `WhitespaceByteTokenizer` | `WhitespaceByteTokenizer()` |  |
| `Identifier` | `string Identifier { get; }` |  |
| `Encode` | `long[] Encode(string text, int maxTokens)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Models`

[`ExternalDataFile`](#externaldatafile) · [`FileToImport`](#filetoimport) · [`ImportFileResult`](#importfileresult) · [`ImportProgress`](#importprogress) · [`ImportResult`](#importresult) · [`ImportSettings`](#importsettings) · [`ModelDownloadProgress`](#modeldownloadprogress) · [`ModelDownloader`](#modeldownloader) · [`ModelInfo`](#modelinfo) · [`ModelRegistry`](#modelregistry)

#### `ExternalDataFile`

Companion data file for a multi-file ONNX export (e.g. DeOldify's 244 MB `deoldify-artistic.onnx.data` referenced by the 423 KB `deoldify-artistic.onnx` graph). Lives next to the primary file in the models directory.

Implements `IEquatable<ExternalDataFile>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ExternalDataFile` | `ExternalDataFile(string FileName, string DownloadUrl, long ApproximateSizeBytes)` | Companion data file for a multi-file ONNX export (e.g. DeOldify's 244 MB `deoldify-artistic.onnx.data` referenced by the 423 KB `deoldify-artistic.onnx` graph). Lives next to the primary file in the models directory. |
| `ApproximateSizeBytes` | `long ApproximateSizeBytes { get; init; }` |  |
| `DownloadUrl` | `string DownloadUrl { get; init; }` |  |
| `FileName` | `string FileName { get; init; }` |  |

#### `FileToImport`

| Member | Signature | Summary |
| --- | --- | --- |
| `FileToImport` | `FileToImport(FileInfo source)` |  |
| `FileName` | `string FileName { get; }` |  |
| `Source` | `FileInfo Source { get; }` |  |
| `GetExifIfd0DateAsync` | `IAsyncEnumerable<DateTime> GetExifIfd0DateAsync()` |  |
| `GetExifSubIfdDateAsync` | `IAsyncEnumerable<DateTime> GetExifSubIfdDateAsync()` |  |
| `GetFileCreatedAt` | `Task<DateTime> GetFileCreatedAt()` |  |
| `GetFileLastWrittenAt` | `Task<DateTime> GetFileLastWrittenAt()` |  |
| `GetGpsDateAsync` | `IAsyncEnumerable<DateTime> GetGpsDateAsync()` |  |

#### `ImportFileResult`

Implements `IEquatable<ImportFileResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImportFileResult` | `ImportFileResult(FileInfo SourcePath, FileInfo DestinationPath, bool Success, string ErrorMessage, DateTime? DetectedDate)` |  |
| `DestinationPath` | `FileInfo DestinationPath { get; init; }` |  |
| `DetectedDate` | `DateTime? DetectedDate { get; init; }` |  |
| `ErrorMessage` | `string ErrorMessage { get; init; }` |  |
| `SourcePath` | `FileInfo SourcePath { get; init; }` |  |
| `Success` | `bool Success { get; init; }` |  |
| `FromException` | `static ImportFileResult FromException(FileInfo source, string errorMessage)` |  |

#### `ImportProgress`

Implements `IEquatable<ImportProgress>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImportProgress` | `ImportProgress()` |  |
| `CurrentFileName` | `string CurrentFileName { get; init; }` |  |
| `CurrentFile` | `int CurrentFile { get; init; }` |  |
| `PercentComplete` | `double PercentComplete { get; }` |  |
| `TotalFiles` | `int TotalFiles { get; init; }` |  |

#### `ImportResult`

Implements `IEquatable<ImportResult>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImportResult` | `ImportResult()` |  |
| `ElapsedTime` | `TimeSpan ElapsedTime { get; init; }` |  |
| `Failed` | `int Failed { get; init; }` |  |
| `FileResults` | `List<ImportFileResult> FileResults { get; init; }` |  |
| `Skipped` | `int Skipped { get; init; }` |  |
| `SuccessfullyProcessed` | `int SuccessfullyProcessed { get; init; }` |  |
| `TotalFiles` | `int TotalFiles { get; init; }` |  |

#### `ImportSettings`

Implements `IEquatable<ImportSettings>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImportSettings` | `ImportSettings()` |  |
| `DateFormatPattern` | `string DateFormatPattern { get; init; }` |  |
| `DefaultDatesToIgnore` | `HashSet<DateTime> DefaultDatesToIgnore { get; init; }` |  |
| `DestinationDirectory` | `DirectoryInfo DestinationDirectory { get; init; }` |  |
| `DryRun` | `bool DryRun { get; init; }` |  |
| `DuplicateHandling` | `DuplicateHandling DuplicateHandling { get; init; }` |  |
| `MaxParallelism` | `int MaxParallelism { get; init; }` |  |
| `MinimumValidDate` | `DateTime MinimumValidDate { get; init; }` |  |
| `PreserveOriginals` | `bool PreserveOriginals { get; init; }` |  |
| `Recursive` | `bool Recursive { get; init; }` |  |
| `SourceDirectory` | `DirectoryInfo SourceDirectory { get; init; }` |  |

#### `ModelDownloadProgress`

Incremental progress reported during a model download. `BytesReceived` always grows; `TotalBytes` is null until the server responds with a Content-Length header.

Implements `IEquatable<ModelDownloadProgress>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ModelDownloadProgress` | `ModelDownloadProgress(long BytesReceived, long? TotalBytes)` | Incremental progress reported during a model download. `BytesReceived` always grows; `TotalBytes` is null until the server responds with a Content-Length header. |
| `BytesReceived` | `long BytesReceived { get; init; }` |  |
| `Fraction` | `double? Fraction { get; }` |  |
| `TotalBytes` | `long? TotalBytes { get; init; }` |  |

#### `ModelDownloader`

Downloads an ONNX model to the app-data models directory. Writes to a temp file and renames on completion so a half-finished download never leaves a corrupt model in place; errors and cancellations clean up. Streams the response body and reports incremental progress so the UI can show a real bar for the multi-megabyte YOLO weights.

Implements `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ModelDownloader` | `ModelDownloader(HttpClient httpClient = null, string userAgent = "PhotoManager/1.0 (https://github.com/Hawkynt/PhotoManager)")` |  |
| `Dispose` | `void Dispose()` |  |
| `DownloadAsync` | `Task<FileInfo> DownloadAsync(ModelInfo model, IProgress<ModelDownloadProgress> progress = null, CancellationToken cancellationToken = null)` | Downloads the given `model` to its resolved destination under the app-data models directory. For multi-file ONNX exports (`ExternalDataFiles` non-empty) the primary graph file is fetched first, then each companion data file in order. Progress is reported as cumulative bytes across all files so the UI bar advances smoothly through the whole sequence. |
| `DownloadAsync` | `Task<FileInfo> DownloadAsync(string url, FileInfo destination, IProgress<ModelDownloadProgress> progress = null, CancellationToken cancellationToken = null)` | Downloads `url` into `destination`, writing via a `.part` temp file and atomically swapping on completion. Exposed so tests (and any future offline-mode tooling) can target an arbitrary path without routing through `AppDataPaths`. |

#### `ModelInfo`

A downloadable ONNX model plus where it lives on disk after download. `FileName` must match the filename the consuming detector expects (e.g. `yolov8n.onnx` or `face-detector.onnx`) so a successful download is immediately usable. Some ONNX exports (DeOldify, BLIP-2, Florence-2 mini) split the graph from the weights — the .onnx file references a sibling .onnx.data file that ONNX Runtime resolves at session-open time. Those models declare their companion files in `ExternalDataFiles`; the downloader fetches them all and `IsInstalled` requires every file to be present before reporting available.

Implements `IEquatable<ModelInfo>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ModelInfo` | `ModelInfo(string Name, string FileName, string DisplayName, string Description, string DownloadUrl, long ApproximateSizeBytes, IReadOnlyList<ExternalDataFile> ExternalDataFiles = null)` | A downloadable ONNX model plus where it lives on disk after download. `FileName` must match the filename the consuming detector expects (e.g. `yolov8n.onnx` or `face-detector.onnx`) so a successful download is immediately usable. Some ONNX exports (DeOldify, BLIP-2, Florence-2 mini) split the graph from the weights — the .onnx file references a sibling .onnx.data file that ONNX Runtime resolves at session-open time. Those models declare their companion files in `ExternalDataFiles`; the downloader fetches them all and `IsInstalled` requires every file to be present before reporting available. |
| `ApproximateSizeBytes` | `long ApproximateSizeBytes { get; init; }` |  |
| `Description` | `string Description { get; init; }` |  |
| `DisplayName` | `string DisplayName { get; init; }` |  |
| `DownloadUrl` | `string DownloadUrl { get; init; }` |  |
| `ExternalDataFiles` | `IReadOnlyList<ExternalDataFile> ExternalDataFiles { get; init; }` |  |
| `FileName` | `string FileName { get; init; }` |  |
| `Name` | `string Name { get; init; }` |  |
| `TotalDownloadBytes` | `long TotalDownloadBytes { get; }` | Total bytes downloaded across primary + every external data file. Used by the download dialog progress. |
| `IsInstalled` | `bool IsInstalled()` |  |
| `ResolveDestination` | `FileInfo ResolveDestination()` | Resolves where the model will land after download. |

#### `ModelRegistry`

Catalog of downloadable detection models. URLs point at stable, public mirrors (ONNX Model Zoo on GitHub for UltraFace, HuggingFace for YOLOv8n which the onnx-community organisation maintains). If a URL ever stops working, the user-facing failure message includes the local path so they can drop the file in manually.

| Member | Signature | Summary |
| --- | --- | --- |
| `AestheticScorers` | `static readonly IReadOnlyList<ModelInfo> AestheticScorers` | Aesthetic-scoring models. UI surfaces these for the "Score photo" feature + smart-album rules. |
| `All` | `static readonly IReadOnlyList<ModelInfo> All` |  |
| `AnimeSharpX4` | `static readonly ModelInfo AnimeSharpX4` | 4x-AnimeSharp fp32 opset17 ONNX — ESRGAN-arch anime upscaler, ~67 MB. |
| `AodNetDehazer` | `static readonly ModelInfo AodNetDehazer` | AOD-Net dehazing ONNX — atmospheric-scattering-model dehazer, ~9 KB. |
| `ArcFace` | `static readonly ModelInfo ArcFace` | ArcFace ResNet-100 — face embedding for recognition, 512-D output. |
| `ArtifactRemoverFbcnnColor` | `static readonly ModelInfo ArtifactRemoverFbcnnColor` | FBCNN colour ONNX — blind JPEG / DCT / wavelet artifact remover, ~120 MB. Exported from jiaxi-jiang/FBCNN PyTorch weights to ONNX. Input: 1×3×H×W float32 [0,1] NCHW. Output: same shape (deartifacted image). Hosted on huggingface.co/Hawkynt/photomanager-models. |
| `ArtifactRemovers` | `static readonly IReadOnlyList<ModelInfo> ArtifactRemovers` | Every JPEG / compression-artifact remover registered with the app. UI dropdown order = this list's order. |
| `BopbScratchDetector` | `static readonly ModelInfo BopbScratchDetector` | BOPB scratch detector ONNX — Microsoft's purpose-built scratch / damage detector, ~144 MB. |
| `CodeFormer` | `static readonly ModelInfo CodeFormer` | CodeFormer ONNX — face restoration via codebook lookup transformer (NeurIPS 2022), ~377 MB. |
| `ColorizeDDColorArtistic` | `static readonly ModelInfo ColorizeDDColorArtistic` | DDColor artistic ONNX — full-quality variant at 512×512, ~934 MB. |
| `ColorizeDDColorPaperTiny` | `static readonly ModelInfo ColorizeDDColorPaperTiny` | DDColor paper-tiny ONNX — state-of-the-art (ECCV 2023) colorizer, ~258 MB. |
| `ColorizeDeOldifyArtistic` | `static readonly ModelInfo ColorizeDeOldifyArtistic` | DeOldify Artistic ONNX — colorise B&W photos. Multi-file ONNX (~244 MB external weights). |
| `ColorizeDeOldifyStable` | `static readonly ModelInfo ColorizeDeOldifyStable` | DeOldify Stable ONNX — colorise B&W photos with more conservative output. Multi-file ONNX (~833 MB external weights). |
| `Colorizers` | `static readonly IReadOnlyList<ModelInfo> Colorizers` | Every colorisation model registered with the app. UI dropdown order = this list's order. DDColor variants first because they materially out-perform DeOldify on real photos. |
| `Dehazers` | `static readonly IReadOnlyList<ModelInfo> Dehazers` | Image-dehazing models. UI surfaces these for the "Remove haze" feature. |
| `Denoisers` | `static readonly IReadOnlyList<ModelInfo> Denoisers` | Every denoise / restoration model registered with the app. UI dropdown order = this list's order. |
| `DepthAnythingV2Small` | `static readonly ModelInfo DepthAnythingV2Small` | Depth Anything V2 small ONNX — monocular depth estimation (DINOv2 backbone), ~99 MB. Input: 1×3×518×518 ImageNet-normalised float32. Output: 1×518×518 relative depth (closer = larger). Powers depth-aware bokeh blur, depth-based masking, and parallax effects. |
| `DepthEstimators` | `static readonly IReadOnlyList<ModelInfo> DepthEstimators` | Monocular depth-estimation models. UI surfaces these for the depth-aware bokeh / masking features. |
| `EdsrX2` | `static readonly ModelInfo EdsrX2` | EDSR ×2 ONNX — Enhanced Deep Super-Resolution, ~5 MB. |
| `FaceRestorers` | `static readonly IReadOnlyList<ModelInfo> FaceRestorers` | Every face-restoration model registered with the app. UI dropdown order = this list's order. |
| `GfpGanV14` | `static readonly ModelInfo GfpGanV14` | GFPGAN v1.4 ONNX — face-only restoration (deblur, scratches, sharpen), ~324 MB. |
| `HatSX4` | `static readonly ModelInfo HatSX4` | HAT-S ×4 ONNX — Hybrid Attention Transformer, fp16, ~57 MB. |
| `Inpainters` | `static readonly IReadOnlyList<ModelInfo> Inpainters` | Every inpainting model registered with the app. UI dropdown order = this list's order. |
| `LamaInpaint` | `static readonly ModelInfo LamaInpaint` | LaMa Inpainting ONNX — fills user-marked damaged / missing regions, ~88 MB. |
| `LowLightEnhancers` | `static readonly IReadOnlyList<ModelInfo> LowLightEnhancers` | Low-light / exposure-correction models. UI surfaces these for the "Brighten low-light" feature. |
| `NafnetGoPro` | `static readonly ModelInfo NafnetGoPro` | NAFNet GoPro-trained ONNX — motion-blur restoration, ~262 MB. |
| `NafnetSiddPure` | `static readonly ModelInfo NafnetSiddPure` | NAFNet SIDD-trained ONNX — gold-standard low-light denoiser, ~446 MB. Big. |
| `NafnetSidd` | `static readonly ModelInfo NafnetSidd` | NAFNet ONNX — AI image restoration (denoise / deblur), ~92 MB. Default denoise model. |
| `NimaMobileNetV2` | `static readonly ModelInfo NimaMobileNetV2` | NIMA MobileNetV2 aesthetic scorer ONNX — distribution over 1..10 score bins, ~9 MB. |
| `RealEsrganX4Anime` | `static readonly ModelInfo RealEsrganX4Anime` | Real-ESRGAN x4 plus anime 6B (lightweight) — anime/illustration upscaler, ~4 MB. |
| `RealEsrganX4Fixed128` | `static readonly ModelInfo RealEsrganX4Fixed128` | Real-ESRGAN x4plus 128 — fixed 128×128 input variant, ~67 MB. |
| `RealEsrganX4` | `static readonly ModelInfo RealEsrganX4` | Real-ESRGAN x4 ONNX — 4× super-resolution upscaler, ~67 MB. Default model. |
| `ScratchDetectors` | `static readonly IReadOnlyList<ModelInfo> ScratchDetectors` | Every scratch-detection model registered with the app. UI dropdown order = this list's order. |
| `ScuNetGan` | `static readonly ModelInfo ScuNetGan` | SCUNet GAN ONNX — Swin-Conv-UNet for blind image restoration, ~87 MB. |
| `SegformerAde150` | `static readonly ModelInfo SegformerAde150` | SegFormer-B0 ADE20K 150-class ONNX — semantic segmentation across all ADE20K classes (grass / water / road / building / person / sand / rock / mountain / …), ~15 MB. Exported from nvidia/segformer-b0-finetuned-ade-512-512. Input: 1×3×512×512 float32 [0,1]. Output: 1×150×128×128 logits. Hosted on huggingface.co/Hawkynt/photomanager-models. |
| `SemanticSegmenters` | `static readonly IReadOnlyList<ModelInfo> SemanticSegmenters` | Multi-class semantic segmentation models (ADE20K 150-class etc.). UI surfaces these for the per-class mask flyout. |
| `SiglipText` | `static readonly ModelInfo SiglipText` | SigLIP base patch16-224 text encoder — encodes vocabulary words for cosine matching. |
| `SiglipVision` | `static readonly ModelInfo SiglipVision` | SigLIP base patch16-224 vision encoder — produces image embeddings for F3 auto-keyword tagging. |
| `SkySegmenter` | `static readonly ModelInfo SkySegmenter` | Sky segmentation ONNX — predicts a per-pixel sky probability mask, ~25 MB. Exported from a UNet-based sky segmentation model to ONNX. Input: 1×3×512×512 float32 [0,1]. Output: 1×1×512×512 float32 [0,1]. Hosted on huggingface.co/Hawkynt/photomanager-models. |
| `SkySegmenters` | `static readonly IReadOnlyList<ModelInfo> SkySegmenters` | Every sky segmentation model registered with the app. UI dropdown order = this list's order. |
| `SubjectMaskMODNet` | `static readonly ModelInfo SubjectMaskMODNet` | MODNet — lightweight portrait/subject alpha matting (~25 MB ONNX). |
| `SwinIrX4` | `static readonly ModelInfo SwinIrX4` | SwinIR x4 GAN ONNX — Swin transformer-based upscaler, ~61 MB. |
| `UltraFaceRfb320` | `static readonly ModelInfo UltraFaceRfb320` | UltraFace RFB-320 ONNX — face detection at 320×240, ~1.3 MB. |
| `Upscalers` | `static readonly IReadOnlyList<ModelInfo> Upscalers` | Every upscale model registered with the app. UI dropdown order = this list's order. |
| `Waifu2xPhotoX4` | `static readonly ModelInfo Waifu2xPhotoX4` | Waifu2x SwinUNet photo ×4 ONNX — older arch with different texture character, ~18 MB. |
| `YoloV8n` | `static readonly ModelInfo YoloV8n` | YOLO v8 Nano ONNX — 80 COCO classes, ~12 MB. |
| `ZeroDcePp` | `static readonly ModelInfo ZeroDcePp` | Zero-DCE++ low-light enhancement ONNX — pixel-wise tonal curve estimator, ~52 KB. |
| `FindByName` | `static ModelInfo FindByName(string name)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Previews`

[`InMemoryPreviewCache`](#inmemorypreviewcache) · [`PreviewCacheKey`](#previewcachekey) · [`RawPreviewExtractor`](#rawpreviewextractor) · [`RegionThumbnailExtractor`](#regionthumbnailextractor) · [`ThumbnailPreCacheService`](#thumbnailprecacheservice) · [`ThumbnailPreCacheService.ThumbnailGenerator`](#thumbnailprecacheservicethumbnailgenerator)

#### `InMemoryPreviewCache`

Bounded in-memory LRU cache for decoded preview JPEG byte buffers. No disk persistence — on app restart, previews regenerate on demand. Bounded simultaneously by entry count and total byte size; whichever limit trips first triggers eviction of the least-recently-used entry.

| Member | Signature | Summary |
| --- | --- | --- |
| `InMemoryPreviewCache` | `InMemoryPreviewCache(int maxEntries = 64, long maxTotalBytes = 268435456)` |  |
| `Count` | `int Count { get; }` |  |
| `CurrentBytes` | `long CurrentBytes { get; }` |  |
| `Clear` | `void Clear()` |  |
| `Set` | `void Set(PreviewCacheKey key, byte[] bytes)` |  |
| `TryGet` | `bool TryGet(PreviewCacheKey key, out byte[] bytes)` |  |

#### `PreviewCacheKey`

A cache key that changes whenever the file changes on disk. Combines the full path with the file's size and last-write time so edits, deletes, or replacements invalidate the cached preview automatically.

Implements `IEquatable<PreviewCacheKey>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `PreviewCacheKey` | `PreviewCacheKey(string FullPath, long SizeBytes, long LastWriteTicks, int MaxEdgePixels)` | A cache key that changes whenever the file changes on disk. Combines the full path with the file's size and last-write time so edits, deletes, or replacements invalidate the cached preview automatically. |
| `FullPath` | `string FullPath { get; init; }` |  |
| `LastWriteTicks` | `long LastWriteTicks { get; init; }` |  |
| `MaxEdgePixels` | `int MaxEdgePixels { get; init; }` |  |
| `SizeBytes` | `long SizeBytes { get; init; }` |  |
| `For` | `static PreviewCacheKey For(FileInfo file, int maxEdgePixels)` |  |

#### `RawPreviewExtractor`

Extracts the largest embedded JPEG preview from a RAW file. Every modern camera writes a full-resolution JPEG preview alongside the raw sensor data for in-camera playback, which we can lift out without touching the mosaic'd pixel data. This avoids shipping LibRaw or dcraw while covering NEF, CR2, CR3, ARW, DNG, RAF, ORF, RW2, PEF, SRW, and friends.

| Member | Signature | Summary |
| --- | --- | --- |
| `ExtractLargestJpegAsync` | `static Task<byte[]> ExtractLargestJpegAsync(FileInfo file, CancellationToken cancellationToken = null)` | Scans `file` for embedded JPEG byte-streams and returns the bytes of the largest one, or null if no JPEG is found. |

#### `RegionThumbnailExtractor`

Cuts a `NormalizedBoundingBox` out of an image and encodes it as JPEG bytes suitable for Avalonia's `Bitmap` constructor. Adds a small margin so the cropped face/object isn't flush against the edge, then caps the long side at `MaxEdgePixels` so giant RAW previews don't blow up the UI.

| Member | Signature | Summary |
| --- | --- | --- |
| `MaxEdgePixels` | `const int MaxEdgePixels` |  |
| `CropAsync` | `static Task<byte[]> CropAsync(FileInfo imageFile, NormalizedBoundingBox box, CancellationToken cancellationToken = null)` |  |

#### `ThumbnailPreCacheService`

Background worker that pre-generates thumbnails for a batch of files so the grid can scroll without blocking on first-time thumbnail extraction. Thumbnails are pushed into an `InMemoryPreviewCache` that the on-demand loader also reads from, so pre-cached entries are served instantly when the user scrolls to them. Uses `Channel` as a bounded work queue and a single long-running consumer task. Memory is bounded by the cache's own limits; duplicate enqueues for the same file path are suppressed.

Implements `IDisposable`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ThumbnailPreCacheService` | `ThumbnailPreCacheService(InMemoryPreviewCache cache, ThumbnailGenerator generator, int maxEdge = 1600, int channelCapacity = 10000)` |  |
| `IsRunning` | `bool IsRunning { get; }` | Whether the background consumer is still running. False once cancelled or the channel is drained after completion. |
| `ProcessedCount` | `int ProcessedCount { get; }` | Number of files that have been processed (successfully or not). |
| `TotalQueued` | `int TotalQueued { get; }` | Total number of files that were queued via `Enqueue`. |
| `Cancel` | `void Cancel()` | Cancel any in-progress background work and reset state so a new batch can be enqueued (e.g., when the user changes the source folder). |
| `Dispose` | `void Dispose()` |  |
| `Enqueue` | `void Enqueue(IEnumerable<FileInfo> files)` | Adds files to the background processing queue. Starts the consumer task if it isn't already running. Duplicate paths (case-insensitive) within the same session are silently skipped. |
| `TryGetCached` | `byte[] TryGetCached(FileInfo file)` | Returns the cached JPEG bytes for `file` if they have been pre-generated, or null if the file hasn't been processed yet. |

#### `ThumbnailPreCacheService.ThumbnailGenerator`

Delegate that decodes + resizes a file into JPEG bytes suitable for caching. Injected so the service can be tested without touching disk. The production callsite wires this to the same decode path as `ImagePreviewLoader`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ThumbnailGenerator` | `Task<byte[]> ThumbnailPreCacheService.ThumbnailGenerator(FileInfo file, CancellationToken cancellationToken)` | Delegate that decodes + resizes a file into JPEG bytes suitable for caching. Injected so the service can be tested without touching disk. The production callsite wires this to the same decode path as `ImagePreviewLoader`. |

### Namespace `Hawkynt.PhotoManager.Core.Regions`

[`RegionCategory`](#regioncategory) · [`RegionCategoryExtensions`](#regioncategoryextensions) · [`RegionService`](#regionservice) · [`RegionStatus`](#regionstatus) · [`TaggedRegion`](#taggedregion) · [`YoloRegionProposer`](#yoloregionproposer)

#### `RegionCategory`

Broad categories of tagged regions. Drives UI color coding (one color per category) and influences how a label is interpreted (a "Person" label is a person's name; an "Animal" label is "cat", "dog", etc.). Keep the set small — users shouldn't have to pick from dozens.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Person` | `0` |  |
| `Animal` | `1` |  |
| `Item` | `2` |  |
| `Place` | `3` |  |
| `Other` | `4` |  |

#### `RegionCategoryExtensions`

| Member | Signature | Summary |
| --- | --- | --- |
| `ToHexColor` | `static string ToHexColor(this RegionCategory category)` | Stable hex color for each category, intended for UI overlays and list swatches. Colors are chosen to be distinguishable under both light and dark backgrounds. |

#### `RegionService`

High-level operations on tagged regions. Wraps the sidecar read/write pattern so callers (CLI + UI) don't have to deal with MetadataEdit construction for simple add/accept/discard flows. Accepted region labels propagate to `dc:subject` keywords so search picks them up.

| Member | Signature | Summary |
| --- | --- | --- |
| `RegionService` | `RegionService(IMetadataReader reader, IMetadataWriter writer)` |  |
| `AcceptAsync` | `Task AcceptAsync(FileInfo imageFile, int index, CancellationToken cancellationToken = null)` | Flips the Nth region from Proposed to Accepted and promotes its label into `dc:subject` keywords. |
| `AppendAsync` | `Task AppendAsync(FileInfo imageFile, IReadOnlyList<TaggedRegion> newRegions, CancellationToken cancellationToken = null)` | Appends `newRegions` to the existing regions in the sidecar, deduping by exact box + category overlap so running a proposer repeatedly doesn't pile up duplicates. Labels on any already-accepted regions flow into `dc:subject` keywords so search picks them up — proposed regions stay out of keywords until the user explicitly accepts them. |
| `DiscardAsync` | `Task DiscardAsync(FileInfo imageFile, int index, CancellationToken cancellationToken = null)` | Removes the Nth region from the sidecar entirely. Keywords are not removed — if the region's label happens to appear in keywords it probably came from another source (user tag, another region) and silently dropping it would surprise. |
| `ListAsync` | `Task<IReadOnlyList<TaggedRegion>> ListAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |
| `RelabelAsync` | `Task RelabelAsync(FileInfo imageFile, int index, string newLabel, CancellationToken cancellationToken = null)` |  |

#### `RegionStatus`

Lifecycle state of a tagged region. - `Proposed`: a detector (YOLO, face detector, etc.) suggested this region but the user hasn't reviewed it yet. Proposed regions are saved to the sidecar so the review queue survives app restart. - `Accepted`: the user confirmed the region. Its label goes into keywords for search. Discard deletes the region from the sidecar rather than marking it Rejected, so the history of rejections isn't carried around forever.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Proposed` | `0` |  |
| `Accepted` | `1` |  |

#### `TaggedRegion`

A labeled rectangular area in an image — generalized from the face-only model. Handles faces, objects, animals, places, or anything else the user wants to tag. `Box` is normalized (0..1) so the region is resolution- independent. `Label` is free-form — a person's name for Person regions, an animal type (cat/dog) for Animal, etc. `Source` identifies who proposed the region so UIs can show "this was suggested by the object detector" vs "you drew this". `Embedding` is only populated for Person regions that have been run through a face embedder — it's what the clustering view uses to group unknown faces.

Implements `IEquatable<TaggedRegion>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `TaggedRegion` | `TaggedRegion(NormalizedBoundingBox Box, RegionCategory Category, string Label = null, RegionStatus Status = 1, string Source = null, float[] Embedding = null)` | A labeled rectangular area in an image — generalized from the face-only model. Handles faces, objects, animals, places, or anything else the user wants to tag. `Box` is normalized (0..1) so the region is resolution- independent. `Label` is free-form — a person's name for Person regions, an animal type (cat/dog) for Animal, etc. `Source` identifies who proposed the region so UIs can show "this was suggested by the object detector" vs "you drew this". `Embedding` is only populated for Person regions that have been run through a face embedder — it's what the clustering view uses to group unknown faces. |
| `FaceDetectorSource` | `const string FaceDetectorSource` |  |
| `ManualSource` | `const string ManualSource` |  |
| `YoloSource` | `const string YoloSource` |  |
| `Box` | `NormalizedBoundingBox Box { get; init; }` |  |
| `Category` | `RegionCategory Category { get; init; }` |  |
| `Embedding` | `float[] Embedding { get; init; }` |  |
| `IsNamed` | `bool IsNamed { get; }` |  |
| `Label` | `string Label { get; init; }` |  |
| `Source` | `string Source { get; init; }` |  |
| `Status` | `RegionStatus Status { get; init; }` |  |

#### `YoloRegionProposer`

Adapter that converts detection results (object labels + bounding boxes) into `TaggedRegion`s at `Proposed` status. The UI surfaces proposed regions with Accept/Discard buttons so the user decides what ends up in the sidecar. COCO classes are mapped to the closest broad category — persons go in Person, animals go in Animal, everything else in Item. Unrecognized labels fall through to `Other`.

| Member | Signature | Summary |
| --- | --- | --- |
| `YoloRegionProposer` | `YoloRegionProposer(IDetector detector, float minConfidence = 0.3)` |  |
| `ProposeAsync` | `Task<IReadOnlyList<TaggedRegion>> ProposeAsync(FileInfo imageFile, CancellationToken cancellationToken = null)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Segmentation`

[`Ade20kClasses`](#ade20kclasses) · [`Ade20kClasses.AdeClass`](#ade20kclassesadeclass) · [`AestheticScore`](#aestheticscore) · [`DepthMap`](#depthmap)

#### `Ade20kClasses`

Subset of ADE20K's 150 semantic classes exposed in the UI for mask generation. Indices match the SegFormer-B0 (ADE20K) ONNX output's channel order — same as the upstream `id2label` mapping in the nvidia/segformer-b0-finetuned-ade-512-512 config. The full 150-class list is intentionally NOT enumerated here — only the classes a typical photo editor wants to mask. Add more as new use cases come up; the OnnxAdeSegmenter accepts any 0..149 index.

| Member | Signature | Summary |
| --- | --- | --- |
| `Bed` | `const int Bed` |  |
| `Building` | `const int Building` |  |
| `Cabinet` | `const int Cabinet` |  |
| `Ceiling` | `const int Ceiling` |  |
| `Door` | `const int Door` |  |
| `Earth` | `const int Earth` |  |
| `Field` | `const int Field` |  |
| `Floor` | `const int Floor` |  |
| `FlyoutClasses` | `static readonly IReadOnlyList<AdeClass> FlyoutClasses` |  |
| `Grass` | `const int Grass` |  |
| `Hill` | `const int Hill` |  |
| `House` | `const int House` |  |
| `Lake` | `const int Lake` |  |
| `Mirror` | `const int Mirror` |  |
| `Mountain` | `const int Mountain` |  |
| `Painting` | `const int Painting` |  |
| `Path` | `const int Path` |  |
| `Person` | `const int Person` |  |
| `Plant` | `const int Plant` |  |
| `Pool` | `const int Pool` |  |
| `River` | `const int River` |  |
| `Road` | `const int Road` |  |
| `Rock` | `const int Rock` |  |
| `Rug` | `const int Rug` |  |
| `Sand` | `const int Sand` |  |
| `Sea` | `const int Sea` |  |
| `Sidewalk` | `const int Sidewalk` |  |
| `Sky` | `const int Sky` |  |
| `Snow` | `const int Snow` |  |
| `Table` | `const int Table` |  |
| `Tree` | `const int Tree` |  |
| `Wall` | `const int Wall` |  |
| `Water` | `const int Water` |  |
| `WaterfallC` | `const int WaterfallC` |  |
| `Windowpane` | `const int Windowpane` |  |

#### `Ade20kClasses.AdeClass`

User-facing class set surfaced in the EditImageWindow class flyout. Display order is the order users will pick from. Sky / subject have their own dedicated buttons so they are intentionally omitted here.

Implements `IEquatable<AdeClass>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `AdeClass` | `AdeClass(int Index, string DisplayName, string Emoji)` | User-facing class set surfaced in the EditImageWindow class flyout. Display order is the order users will pick from. Sky / subject have their own dedicated buttons so they are intentionally omitted here. |
| `DisplayName` | `string DisplayName { get; init; }` |  |
| `Emoji` | `string Emoji { get; init; }` |  |
| `Index` | `int Index { get; init; }` |  |

#### `AestheticScore`

One NIMA prediction: mean score in [1, 10], standard deviation, and the raw 10-bin distribution.

Implements `IEquatable<AestheticScore>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `AestheticScore` | `AestheticScore(double Mean, double StdDev, float[] Distribution)` | One NIMA prediction: mean score in [1, 10], standard deviation, and the raw 10-bin distribution. |
| `Distribution` | `float[] Distribution { get; init; }` |  |
| `Mean` | `double Mean { get; init; }` |  |
| `StdDev` | `double StdDev { get; init; }` |  |

#### `DepthMap`

Raw depth-estimator output: a row-major float[] aligned to the source image's dimensions. Values are in arbitrary relative units (larger = closer for Depth Anything). Use the Min/Max helpers to normalise to [0, 1] when you need a colour-mappable display.

Implements `IEquatable<DepthMap>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DepthMap` | `DepthMap(float[] Values, int Width, int Height)` | Raw depth-estimator output: a row-major float[] aligned to the source image's dimensions. Values are in arbitrary relative units (larger = closer for Depth Anything). Use the Min/Max helpers to normalise to [0, 1] when you need a colour-mappable display. |
| `Height` | `int Height { get; init; }` |  |
| `Max` | `float Max { get; }` | Largest depth value present in `Values`. |
| `Min` | `float Min { get; }` | Smallest depth value present in `Values`. |
| `Values` | `float[] Values { get; init; }` |  |
| `Width` | `int Width { get; init; }` |  |
| `NormalisedAt` | `float NormalisedAt(int x, int y)` | Normalised value at (x, y) in [0, 1] where 0 = farthest, 1 = closest. |

### Namespace `Hawkynt.PhotoManager.Core.Services`

[`DateTimeParser`](#datetimeparser) · [`FileOrganizer`](#fileorganizer) · [`ImportManager`](#importmanager) · [`SupportedFormatsService`](#supportedformatsservice)

#### `DateTimeParser`

Implements `IDateTimeParser`.

| Member | Signature | Summary |
| --- | --- | --- |
| `DateTimeParser` | `DateTimeParser()` |  |
| `GetSupportedDateFormats` | `string[] GetSupportedDateFormats()` |  |
| `ParseDateFromFileName` | `IAsyncEnumerable<DateTime> ParseDateFromFileName(FileToImport fileToImport, ImportSettings settings)` |  |

#### `FileOrganizer`

Implements `IFileOrganizer`.

| Member | Signature | Summary |
| --- | --- | --- |
| `FileOrganizer` | `FileOrganizer()` |  |
| `AreFilesIdenticalAsync` | `Task<bool> AreFilesIdenticalAsync(FileInfo filePath1, FileInfo filePath2)` |  |
| `CopyFileAsync` | `Task<bool> CopyFileAsync(FileInfo sourcePath, FileInfo targetPath, bool overwrite = false)` |  |
| `GenerateTargetPath` | `Task<FileInfo> GenerateTargetPath(FileToImport file, DateTime dateTime, ImportSettings settings)` |  |
| `MoveFileAsync` | `Task<bool> MoveFileAsync(FileInfo sourcePath, FileInfo targetPath, bool overwrite = false)` |  |
| `ProcessFileAsync` | `Task<ValueTuple<FileOperationResult, FileInfo, string>> ProcessFileAsync(FileToImport file, DateTime dateTime, ImportSettings settings)` |  |

#### `ImportManager`

Implements `IImportManager`.

| Member | Signature | Summary |
| --- | --- | --- |
| `ImportManager` | `ImportManager(IDateTimeParser dateTimeParser = null, IFileOrganizer fileOrganizer = null, ISupportedFormatsService supportedFormatsService = null)` |  |
| `EnumerateDateTimes` | `IAsyncEnumerable<ValueTuple<DateTimeSource, DateTime>> EnumerateDateTimes(FileToImport fileToImport)` |  |
| `EnumerateDirectory` | `IAsyncEnumerable<FileToImport> EnumerateDirectory(DirectoryInfo root, bool recursive)` |  |
| `GetMostLogicalCreationDateAsync` | `Task<DateTime?> GetMostLogicalCreationDateAsync(FileToImport fileToImport)` |  |
| `GetMostLogicalCreationDateWithSourceAsync` | `Task<ValueTuple<DateTime?, DateTimeSource>> GetMostLogicalCreationDateWithSourceAsync(FileToImport fileToImport)` |  |
| `ProcessDirectoryAsync` | `Task<ImportResult> ProcessDirectoryAsync(ImportSettings settings, IProgress<ImportProgress> progress = null, CancellationToken cancellationToken = null)` |  |
| `ProcessSingleFile` | `Task<ImportFileResult> ProcessSingleFile(FileToImport fileToImport, ImportSettings settings)` |  |

#### `SupportedFormatsService`

Implements `ISupportedFormatsService`.

| Member | Signature | Summary |
| --- | --- | --- |
| `SupportedFormatsService` | `SupportedFormatsService()` |  |
| `GetExtensionsByFormatAsync` | `Task<Dictionary<string, string[]>> GetExtensionsByFormatAsync()` |  |
| `GetSupportedExtensionsAsync` | `Task<string[]> GetSupportedExtensionsAsync()` |  |
| `GetSupportedExtensionsWithoutWildcardsAsync` | `Task<string[]> GetSupportedExtensionsWithoutWildcardsAsync()` |  |
| `IsExtensionSupported` | `bool IsExtensionSupported(string extension)` |  |

### Namespace `Hawkynt.PhotoManager.Core.Stitching`

[`EdgeMatch`](#edgematch) · [`EdgeMatcher`](#edgematcher) · [`PieceAssembler`](#pieceassembler) · [`PieceAssembler.PlacedPiece`](#pieceassemblerplacedpiece) · [`ScannerBackground`](#scannerbackground)

#### `EdgeMatch`

One candidate alignment between two pieces. The transform takes a point in piece B's local coordinate space (relative to its own bounding-box origin) and maps it into piece A's local coordinate space: `p_in_A = R · p_in_B + T`. `Score` is "lower is better" — it's the mean residual (in pixels) over the matched correspondence pairs, so a perfect alignment scores ~0.

| Member | Signature | Summary |
| --- | --- | --- |
| `EdgeMatch` | `EdgeMatch()` |  |
| `PieceA` | `int PieceA { get; init; }` |  |
| `PieceB` | `int PieceB { get; init; }` |  |
| `Rotation` | `double[] Rotation { get; init; }` | 2×2 rotation matrix row-major: [r00, r01, r10, r11]. |
| `Score` | `double Score { get; init; }` | Mean residual (px) over the correspondence pairs. Lower is better. |
| `Support` | `int Support { get; init; }` | How many contour samples participated in the fit. More = stronger. |
| `TranslationX` | `double TranslationX { get; init; }` |  |
| `TranslationY` | `double TranslationY { get; init; }` |  |

#### `EdgeMatcher`

For every pair of pieces, finds the best rigid transform that maps one piece onto the other along a shared torn boundary segment. Two-stage matcher: 1) Coarse-grained sliding-window Kabsch search over both pieces' resampled cyclic contours (piece B walked in REVERSE because their boundaries trace opposite directions when correctly placed adjacent). For each (offsetA, offsetB) pair we fit a small (default 16-sample) rigid transform and check whether its residual is below a strict "lock-on" threshold. 2) For every candidate that locks on, greedily extend the matched arc forward and backward as long as each new sample's residual stays below the threshold. Re-fit Kabsch on the full extended set. The score combines mean residual with a -log(arcLength) bonus so longer matches outrank short coincidences. The matcher only returns transforms — non-overlapping global layout is the assembler's job.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultExtensionResidualCeiling` | `const double DefaultExtensionResidualCeiling` | Per-sample residual ceiling during arc extension. Sample is added to the matched arc only if its post-fit residual is below this. |
| `DefaultMinSupport` | `const int DefaultMinSupport` | Minimum extended-arc length (in resampled samples) for a match to count. ~1/4th of the perimeter — true shared edges run substantially longer than coincidence matches that pass only a tiny seed window. |
| `DefaultProfileSamples` | `const int DefaultProfileSamples` | How many evenly-spaced contour points each piece is resampled to. |
| `DefaultSeedResidualCeiling` | `const double DefaultSeedResidualCeiling` | Per-sample residual ceiling (px) the seed must beat to lock on. Tight enough to filter out coincidental shape similarity; loose enough to handle 1-2 px contour jitter from anti-aliasing. |
| `DefaultSeedWindow` | `const int DefaultSeedWindow` | Initial Kabsch window for the coarse alignment search. Bigger seeds disambiguate rotation: a long curve segment has enough variation to lock the rotation, where short segments admit a continuum of near-equivalent fits. |
| `BuildProfile` | `static EdgeProfile BuildProfile(DetectedPiece piece, int samples = 240)` |  |
| `FindBestMatch` | `static EdgeMatch FindBestMatch(EdgeProfile a, EdgeProfile b, int seedWindow = 60, double seedResidualCeiling = 1.5, double extensionResidualCeiling = 2, int minSupport = 60, double residualCeiling = 1.7976931348623157E+308)` | Returns the best alignment between piece A and piece B (or null if nothing locks on). The transform maps points in B's local space into A's local space. |

#### `PieceAssembler`

Reassembles detected pieces into a coherent canvas. Two-stage strategy: 1) Anchor every piece at its source-detected bounding-box origin. For a "torn paper laid out in place" scan (think: scanner-bed photo of a torn page that hasn't been physically scrambled), this is already the correct global layout — the segmenter split touching pieces but their relative positions are right. The result canvas tightly bounds the union of source positions, which is a recognisable reassembled image. 2) For pairs of pieces that the `EdgeMatcher` finds a strong rigid alignment for AND whose deduced relative pose differs from their source positions by less than a few pixels (i.e. the matcher AGREES the source layout is correct), we keep the source layout. Pieces whose detected match strongly disagrees (suggesting one piece was actually moved) are flagged in the unplaced list for the caller to handle (e.g. fall back to manual placement). The assembler intentionally does NOT try to RANSAC-fit a globally consistent layout from match transforms in the general case — the matcher's score landscape is too noisy for greedy chaining of arbitrary rigid transforms to converge for real-world scans. Source layout is a stronger prior than the matcher's evidence for in-place torn paper. For "shuffled pieces on a scanner bed" use cases, callers can implement a graph-based assembler on top of `EdgeMatcher`'s pairwise matches in a future slice.

| Member | Signature | Summary |
| --- | --- | --- |
| `DefaultMatchResidualCeiling` | `const double DefaultMatchResidualCeiling` | Pass-through, kept for API stability — see `DefaultExtensionResidualCeiling`. |
| `DefaultMaxOverlapFraction` | `const double DefaultMaxOverlapFraction` | How much of a candidate piece's *mask* (not bounding box) is allowed to overlap another placed piece's mask. Bounding boxes of neighbouring torn-paper pieces routinely overlap heavily — a torn page with diagonal tears has bboxes that wrap each others' notches — so the mask-level test is what matters. ~15 % handles real-world contour noise without letting the assembler stack pieces directly on top of each other. |
| `Assemble` | `static AssemblyResult Assemble(IReadOnlyList<DetectedPiece> pieces, double maxOverlapFraction = 0.15, double matchResidualCeiling = 2)` |  |

#### `PieceAssembler.PlacedPiece`

| Member | Signature | Summary |
| --- | --- | --- |
| `PlacedPiece` | `PlacedPiece()` |  |
| `Piece` | `DetectedPiece Piece { get; init; }` |  |
| `Rotation` | `double[] Rotation { get; init; }` | 2×2 row-major rotation matrix — currently always identity for source-layout placement. |
| `TranslationX` | `double TranslationX { get; init; }` |  |
| `TranslationY` | `double TranslationY { get; init; }` |  |

#### `ScannerBackground`

Hint for the segmenter about what colour the gaps between pieces / the scanner bed are. A black background is the easy case (white-paper pieces on a flatbed with the lid open); white bed needs an inverted threshold. `Auto` picks based on the median brightness of the four image corners — works for clean scans but can be wrong if a piece touches a corner.

| Value | Numeric | Summary |
| --- | --- | --- |
| `Auto` | `0` |  |
| `Black` | `1` |  |
| `White` | `2` |  |

### Namespace `Hawkynt.PhotoManager.Core.Video`

[`FfmpegLocator`](#ffmpeglocator) · [`VideoExtractOptions`](#videoextractoptions) · [`VideoFrameExtractor`](#videoframeextractor)

#### `FfmpegLocator`

Cross-platform discovery of an installed ffmpeg binary. Tries the system PATH first via which/where, then falls back to a curated list of common install locations on each OS.

| Member | Signature | Summary |
| --- | --- | --- |
| `Find` | `static FileInfo Find()` |  |
| `IsAvailable` | `static bool IsAvailable()` |  |

#### `VideoExtractOptions`

Tunables for `VideoFrameExtractor`. Defaults match the "panorama sweep" workflow: 2 fps gives plenty of overlap for the stitcher without exploding disk usage.

Implements `IEquatable<VideoExtractOptions>`.

| Member | Signature | Summary |
| --- | --- | --- |
| `VideoExtractOptions` | `VideoExtractOptions()` |  |
| `EndTime` | `TimeSpan? EndTime { get; init; }` |  |
| `Fps` | `double Fps { get; init; }` |  |
| `JpegQuality` | `int JpegQuality { get; init; }` |  |
| `MaxLongEdge` | `int? MaxLongEdge { get; init; }` |  |
| `StartTime` | `TimeSpan? StartTime { get; init; }` |  |

#### `VideoFrameExtractor`

Drives ffmpeg as a subprocess to crack a video into a sequence of JPEG frames suitable for the panorama stitcher. ffmpeg is detected via `FfmpegLocator`; missing binary throws so callers can show install instructions.

| Member | Signature | Summary |
| --- | --- | --- |
| `VideoFrameExtractor` | `VideoFrameExtractor()` |  |
| `VideoFrameExtractor` | `VideoFrameExtractor(FileInfo ffmpegOverride)` |  |
| `BuildCommandLine` | `static IReadOnlyList<string> BuildCommandLine(FileInfo videoFile, DirectoryInfo outputDir, VideoExtractOptions options)` | Returns the argument list (excluding the executable itself) that would be passed to ffmpeg for the given inputs. Pure / side-effect free so unit tests can pin the wire format. |
| `ExtractAsync` | `Task<IReadOnlyList<FileInfo>> ExtractAsync(FileInfo videoFile, DirectoryInfo outputDir, VideoExtractOptions options, IProgress<int> progress, CancellationToken cancellationToken)` |  |
